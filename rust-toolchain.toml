[toolchain]
# Pins to ≥1.85 for edition2024 support (home v0.5.12 dep).
# Also satisfies ≥1.77.2 for CVE-2024-24576 (BatBadBut Windows arg injection).
# We also reject .bat/.cmd engine paths in code (F7).
channel = "stable"
