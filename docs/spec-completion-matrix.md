# NEditor Specification Completion Matrix

Updated: 2026-05-21

Status vocabulary:

- Complete: current code and tests provide direct evidence.
- Partial: implementation exists, but scope or proof is incomplete.
- Unverified: implementation appears to exist, but there is no current direct
  workflow, artifact, or platform proof.
- Missing: no current implementation evidence was found.
- Deferred: the specification marks the item as later/non-goal, or the project
  has intentionally deferred it.

This matrix is intentionally conservative. If evidence is indirect, the status
is not "Complete".

GitHub Actions is not an active verification surface for this project. Any
older run IDs in this matrix are historical context only; current completion
requires local command evidence, committed artifacts, rendered/manual QA, or
explicit platform checks.

## Core Product Scope

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 1 Purpose | Local-first business Markdown workbench | Partial | `README.md`; `docs/user-guide.md`; Tauri/Vue/Rust project layout; compiler/export modules | Full buildout still needs requirement-by-requirement verification and workflow tests. |
| 2 Source Prompt Extension | Tauri 2, Vue 3, Pinia, Vite, vanilla CSS | Complete | `package.json`; `src/main.ts`; `src/App.vue`; `src-tauri/Cargo.toml` | Keep dependency admission docs current as test dependencies are added. |
| 2 Source Prompt Extension | Side-by-side Markdown editor and live preview | Partial | `src/App.vue` editor/preview panes; CodeMirror setup; `pnpm run test:e2e` passed 42 Chromium workflows locally on 2026-05-21, including source edit to live preview update, large-document edit/preview responsiveness, synchronized editor/preview scrolling, and preview heading click-to-source; `tests/frontend-unit.test.ts` proves preview debounce timing stays within the small-document target budget and coalesces rapid edits | Native desktop interaction proof. |
| 2 Source Prompt Extension | Split, preview-only, focus/source modes | Partial | `src/App.vue` mode controls; `pnpm run test:e2e` passed 42 Chromium workflows locally on 2026-05-21, including split/source/preview/focus/export/review/presentation modes with sidebar routing for export/review/presentation | Desktop execution still needs proof. |
| 2 Source Prompt Extension | Local new/open/save/save-as flows | Partial | `src/stores/documents.ts`; `src-tauri/src/filesystem.rs`; file command tests; `pnpm run test:e2e` passed 42 Chromium workflows locally on 2026-05-21, including open, save, save as, duplicate, rename, reveal, revert, stale-save conflict, and recent-file flows | Native desktop workflow proof. |
| 2 Source Prompt Extension | Window title reflects file and dirty state | Partial | `windowTitle` getter and `setWindowTitle` in `src/App.vue`; Playwright harness asserts clean, dirty, and saved browser document titles | Native desktop title assertion. |
| 2 Source Prompt Extension | Toolbar commands and keyboard shortcuts | Partial | `src/App.vue` toolbar, command palette, shortcut handler; `pnpm run test:e2e` passed 42 Chromium workflows locally on 2026-05-21, including search/replace, Markdown formatting commands, multi-cursor commands, command-palette heading/citation/glossary/index/include/open-document/workspace navigation, and toolbar insertion workflows | Native desktop command execution proof. |
| 2 Source Prompt Extension | Light/dark/system theme and typography | Partial | Settings in `src/App.vue`; persisted preferences in store; Playwright harness asserts theme, preview theme, high contrast, reduced motion, and editor/preview typography application plus reload persistence | Visual/accessibility verification across themes in a real browser/native runtime. |
| 2 Source Prompt Extension | Cross-platform packaging | Partial | `README.md` packaging note; local Tauri build commands; `./node_modules/.bin/tauri build --bundles app` builds `src-tauri/target/release/bundle/macos/NEditor.app` on this macOS host; `pnpm run test:desktop-bundle` verifies the `.app` Info.plist metadata, bundle identifier, version, executable, icon, copyright, and high-resolution flag while writing `.tmp/desktop-bundle/macos-app-report.json`; `pnpm run test:desktop-dmg` classifies this host's sandboxed `hdiutil create`/`hdiejectd` failure and writes `.tmp/desktop-bundle/macos-dmg-report.json` | Full local/manual bundle evidence for Windows/Linux and signing/notarization stance. |
| 3 Product Positioning | Local-first document workbench, not cloud suite | Partial | `README.md`; `docs/user-guide.md`; local file/snapshot/Git/export design | Fixture workflows and platform proof still need to reinforce positioning. |
| 4 Target Users | Business, technical, research, product users | Complete | Example project front matter declares consultants, technical writers, researchers and analysts, product and engineering teams, executives and managers, students and academics, developers, and teams using AI chat output; `example_fixture_tests::example_project_fixtures_compile_and_export` verifies each persona compiles, every README example link maps to an executable fixture, and audience metadata survives HTML, PDF, DOCX, PPTX, and Markdown bundle exports | Keep new personas represented by executable example fixtures. |

## Carry-Forward MacDown Improvements

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 5.1 External File Refresh | Watch open files | Partial | `src-tauri/src/filesystem_watch.rs`; `src/stores/documents.ts`; native-watch local check; stale-save conflict recovery is browser-proven in archived workflow run `26139678118`; clean root-file watcher reload is browser-proven in archived workflow run `26140882880`; clean included-file recompile and dirty included-file conflict flows are browser-proven in archived workflow run `26145509141` | Desktop workflow tests. |
| 5.1 External File Refresh | Non-destructive conflicts: compare, accept external, keep local, save copy | Partial | Conflict modal in `src/App.vue`; `src/lib/conflict.ts`; `src/lib/workflows.ts`; frontend unit tests cover conflict diff alignment, merge-line helpers, and merge composition ordering including blank lines; Playwright harness lists stale-save conflict copy/merge/keep-local/accept-external workflows, including explicit merge composition add/reorder/remove assertions; archived browser workflow run `26139678118` covers stale-save compare visibility, local-copy preservation, merge-back recovery, keep-local, and accept-external; archived browser workflow run `26140882880` covers watcher-originated dirty root-file conflict compare visibility | Native desktop workflow proof, current-host browser execution, and deeper manual UX QA for conflict composition. |
| 5.1 External File Refresh | Watch included files and recompile master docs | Partial | Watch setup includes manifest included files; include tests; References sidebar include graph navigation; Playwright harness lists `navigates include graph entries from references and commands`; archived browser workflow run `26145509141` covers clean included-file recompile and dirty included-file conflict workflows | Native desktop workflow proof and broader platform coverage. |
| 5.2 Build Toolchain Lessons | Pinned local JS/Rust dependencies | Partial | `pnpm-lock.yaml`; `src-tauri/Cargo.lock`; README commands; `pnpm run verify:local -- --list` and `pnpm run verify:local:full -- --list` expose the local quick/release verification baselines now that GitHub Actions is not active; `pnpm run check:deps` proves all 39 `package.json` and `src-tauri/Cargo.toml` dependencies are represented in `docs/dependency-admission.md`, the root license contains the MIT terms, and npm/Cargo/Tauri package metadata remains MIT licensed | Extend the same proof into release packaging and optional engine platform evidence. |
| 5.2 Build Toolchain Lessons | Diagnostics for missing external engines | Partial | `transforms/external.rs`; external transform tests; `compiler_reports_missing_external_engine_path_before_embedded_fallback` proves optional external-capable fences emit setup diagnostics when no engine path is configured before using the embedded fallback | Cross-platform diagnostic proof and UI workflow coverage. |
| 5.3 Business Export Customization | HTML/PDF/DOCX/PPTX exports plus blog/Substack/LaTeX/Google Docs handoffs | Partial | `src-tauri/src/export/*`; export tests; `export_document_writes_blog_and_substack_publish_packages` proves `blog` and `substack` ZIP packages contain `post.md`, `post.html`, `substack-copy.html`, `post.txt`, `metadata.json`, `rss-item.xml`, `README.md`, and embedded `manifest.json`; `export_document_writes_latex_and_google_docs_outputs` proves `latex` writes `.tex` metadata, headings, tables, links, labels, and equations while `google-docs` writes a ZIP with `document.docx`, `document.html`, `document.md`, `document.txt`, `metadata.json`, `README.md`, and embedded `manifest.json`; `export_conformance_fixture_maps_business_features` asserts release metadata and legal disclaimer across HTML, PDF, DOCX, PPTX, plain text, and Markdown bundle outputs; `representative_rendered_export_artifacts_are_package_inspectable` proves a board-style fixture through inspectable HTML, PDF object structure, DOCX/PPTX package anatomy, custom/core properties, comments/provenance appendices, transform artifacts, Markdown bundle manifest entries, blog/Substack package anatomy, LaTeX source, and Google Docs package anatomy; `pnpm run test:rendered-exports` writes a local audit bundle under `.tmp/rendered-export-audit` with HTML, PDF, DOCX, PPTX, Markdown bundle, blog package, Substack package, LaTeX, Google Docs package, hashes, manual checklist, `viewer-proof.json` executable HTML/PDF/DOCX/PPTX/package assertions, and a `pdflatex` compile proof when TeX is installed; `export_option_matrix_is_preserved_across_targets_and_bundle_evidence` proves one explicit option matrix across HTML, PDF, DOCX, PPTX, text, and Markdown bundle evidence; `rich_markdown_blocks_survive_cross_target_exports` proves block quotes, callouts, lists, task lists, code, tables, figures, equations, generated figure/table lists, and cross references across the same target family; `example_project_fixtures_compile_and_export` proves every README example exports with title/audience/feature evidence across HTML, PDF, DOCX, PPTX, and Markdown bundle artifacts; `pnpm run test:e2e` passed 42 Chromium workflows locally on 2026-05-21, including export readiness, success, failure, output path, manifest path, readiness-blocked export before write, and UI selection/export handoff for blog, Substack, LaTeX, and Google Docs package targets | Broader manual/viewer checks for more fixtures, more option combinations, live Google Docs import proof, and native/platform export proof. |
| 5.3 Business Export Customization | Page numbering, headers/footers, logo, brand color, cover, watermark, presets, metadata | Partial | Export option tests; export shared helpers; frontend settings; `export_conformance_fixture_maps_business_features` asserts headers/footers, watermark, package core/custom properties, approval metadata, legal disclaimer, and bundle metadata; `export_option_matrix_is_preserved_across_targets_and_bundle_evidence` asserts cover/page-number omissions, compact preset, watermark, legal disclaimer, and package/body effects across targets; `example_project_fixtures_compile_and_export` proves `targetPersona` audience metadata is carried into HTML cover metadata, Markdown bundle metadata/text, and DOCX/PPTX custom properties | Template/profile persistence and broader visual artifact proof. |
| 5.4 Master Documents And Includes | Include syntaxes, relative resolution, strip child front matter, circular diagnostics | Partial | Compiler/document structure tests; `compiler_reports_circular_and_too_deep_includes` proves circular include diagnostics, include-chain graph depth, and maximum include depth enforcement; missing include diagnostics are covered by `compiler_reports_missing_include_without_panicking`; `compiler_reports_unreadable_include_targets_with_context` proves unreadable/invalid include target diagnostics include source, suggestion, include target, and resolved path context; References sidebar renders parent-child include edges with depth plus open-child/directive navigation; Playwright harness lists `navigates include graph entries from references and commands` | Native desktop workflow proof and broader platform coverage. |
| 5.5 Table Of Contents | `[TOC]`, front matter TOC, depth, numbering, export formatting | Partial | Generated sections and export tests; `generated_toc_exports_page_numbers_for_pdf_and_docx` proves front matter TOC depth/numbering plus page-numbered PDF TOC lines and DOCX TOC field output; Playwright harness covers command-palette insertion of `[TOC]` | Browser preview tests, native workflow proof, and rendered/manual PDF/DOCX TOC QA. |
| 5.6 Native Tab Organization | Multiple docs, tabs, pinning, recents, restore | Partial | `src/App.vue`; `src/stores/documents.ts`; Playwright harness covers document-set grouping, folder grouping, dragging a document into a set, closing a group, tab activation, dirty-close confirmation, stale recent cleanup, workspace restore, scroll restore, and missing restored-file warnings | Native desktop proof and deeper drag/reorder edge cases. |
| 5.6 Native Tab Organization | Split editor panes later | Deferred | Spec says "later versions" | Record as deferred/non-goal for current release. |

## Core Application Requirements

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 6.1 Application Shell | Tauri IPC commands exposed from Rust | Complete | `src-tauri/src/lib.rs`; `docs/ipc-command-coverage.md`; `ipc_command_tests::spec_25_4_ipc_commands_are_registered_and_documented` verifies every spec 25.4 command is registered and every registered Tauri command has a synchronized coverage row with implementation/evidence columns | Keep coverage synchronized as new IPC commands are added. |
| 6.1 Application Shell | Vue SFC block order template/script/style | Complete | `src/App.vue` follows template/script/style order; `pnpm run check:structure` verifies the top-level Vue SFC block order and `<script setup>` contract | Keep the guard current if the primary shell is split into smaller SFCs. |
| 6.2 Primary Layout | Toolbar, status bar, sidebar, editor, preview | Partial | `src/App.vue`; Playwright harness covers outline-sidebar heading navigation to source | Browser layout/accessibility checks across viewport sizes and native execution proof. |
| 6.2 Primary Layout | Export preview, review, presentation outline modes | Partial | Mode controls and sidebars in `src/App.vue`; export mode now renders an export preview summary in the preview pane; Playwright harness covers export preview, review, and presentation outline routing | Native execution proof and product QA for each mode. |
| 6.3 Editor | CodeMirror 6 engine | Complete | CodeMirror dependencies and imports in `src/App.vue` | None beyond ongoing behavior tests. |
| 6.3 Editor | Markdown highlighting, diagnostics gutter, decorations | Partial | CodeMirror markdown/linter/lintGutter/decorations; Playwright harness covers diagnostics-panel source navigation with precise line/column metadata | Local browser/native execution proof for diagnostic range rendering and visual states. |
| 6.3 Editor | Line numbers, word wrap, spellcheck, find/replace, word count | Partial | Settings and CodeMirror extensions; status bar stats; Playwright harness covers spellcheck/autocapitalize editor attributes and word/character/reading-time status metrics; archived browser workflow run `26154535588` covers line-number and word-wrap persistence plus CodeMirror find/replace | Local browser/native execution proof for status metrics and spellcheck behavior. |
| 6.3 Editor | Smart list continuation and auto-pairing | Partial | `src/lib/markdownEditing.ts`; `continueMarkdownList`; `closeBrackets`; Markdown format commands in `src/App.vue`; frontend unit coverage proves bullet, numbered, task-list, blockquote-list, quoted-numbered-list, and empty-marker exit behavior; Playwright harness covers list continuation, bracket pairing, quote pairing, bold/italic/inline-code pairing, and code-fence insertion; archived browser workflow run `26154535588` covers basic Markdown list continuation and bracket auto-pairing | Local browser/native execution proof and additional Markdown shortcut edge cases. |
| 6.3 Editor | Multi-cursor support | Partial | Explicit CodeMirror `addCursorAbove`, `addCursorBelow`, and `selectNextOccurrence` commands in `src/App.vue`; Playwright harness includes a multi-cursor edit workflow | Local browser/native execution proof. |
| 6.3 Editor | Vim/emacs keybindings | Deferred | Spec says later if feasible | Record deferred unless implemented later. |
| 6.4 Preview | Live debounced preview | Partial | Store compile on editor update; debounce in `src/App.vue`; Playwright harness covers preview update after source typing | Large-document behavior and timing tests. |
| 6.4 Preview | Scroll sync and heading click-to-source | Partial | Preview/editor scroll handlers; click handler; archived browser workflow run `26153224371` covers editor-to-preview scroll sync, preview-to-editor scroll sync, and rendered preview heading click-through to the source line | Desktop workflow proof and edge cases for non-heading anchors. |
| 6.4 Preview | Separate preview theme, inline warnings, transform blocks, export preview | Partial | Preview theme setting; source-map-backed preview diagnostic callouts; diagnostics sidebar; transform rendering; transform artifact preview inventory; export preview summary; modes; Playwright harness asserts preview theme/typography attributes and lists `navigates compiler diagnostics to the source range` plus `runs export readiness, success, and failure workflows`, covering preview diagnostic source jumps, export summary state, and transform artifact source jumps | Native workflow execution and deeper rendered visual QA. |
| 6.5 File Operations | New, open file, open folder, save, save as, revert, rename, duplicate, reveal | Partial | Store actions and Rust file commands; file command tests; `file_command_tests::reveal_command_for_existing_path_is_platform_specific_and_argument_safe` proves reveal path validation and platform-specific file-manager arguments without shell interpolation; `desktop_native_command_workflow_smoke_uses_real_files_and_exports` proves save-as/open/watch/compile/readiness/export/reveal command flow against real local files; Playwright mocked workflows cover open/save/save-as/duplicate/rename/pin/reveal/revert in archived workflow run `26137556147`; stale-save conflict copy/merge/keep-local/accept-external recovery in archived workflow run `26139678118`; watcher-originated root reload/conflict proof in archived workflow run `26140882880`; included-file watcher conflict proof in archived workflow run `26145509141` | Native desktop dialog/WebDriver workflow tests. |
| 6.5 File Operations | Recent docs/folders, workspace restore | Partial | Persisted workspace store; Playwright mocked workflows cover workspace listing after open and recently closed reopening in archived workflow run `26137556147`; archived browser workflow run `26147556750` covers restart-style restore for open tabs, active tab, pinned state, mode/sidebar, workspace root, and recent files; archived browser workflow run `26148828614` covers scroll restore and missing-restored-file warning coverage; archived browser workflow run `26151184228` covers tab activation, dirty close confirmation, renamed recent cleanup, and deleted recently-closed pruning coverage; archived browser workflow run `26152255407` covers recent folder reopen/prune behavior and externally moved recently-closed path pruning | Native desktop dialog/recent-folder proof and deeper folder/document-set grouping behavior. |
| 6.5 File Operations | External change detection/conflict handling | Partial | Watch/conflict code and tests; archived browser workflow run `26139678118` covers stale-save conflict blocking, compare, save-copy preservation, merge-back recovery, keep-local, and accept-external; archived browser workflow run `26140882880` covers clean root reload and watcher-originated dirty root conflict; archived browser workflow run `26145509141` covers clean included-file recompile and dirty included-file conflict flows | Desktop workflow proof. |

## Compiler And Document Model

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 7.1 Pipeline | Load, front matter, includes, source map, variables, transforms, formulas, citations, refs, generated sections, semantic model, preview/export | Partial | `src-tauri/src/compiler.rs`; supporting modules; many backend tests | Replace remaining string-heavy paths where correctness depends on semantic structure. |
| 7.2 Outputs | Compiled Markdown, HTML, semantic model, diagnostics, include graph, source map, metadata, bibliography, index, formula graph, transform artifacts, manifest | Partial | `CompileResponse` types and compiler tests; Markdown bundle tests prove manifest, semantic, metadata, AST, source map, diagnostics, bibliography, formula graph, transform artifacts, media map, per-figure media-use map, and include graph entries; `export_option_matrix_is_preserved_across_targets_and_bundle_evidence` proves bundle `document.txt` and `manifest.json` preserve export option intent | Completion evidence for every output in matrix and UI consumers. |
| 7.3 Diagnostics | Severity/message/source/range/suggested fix/related items | Partial | `diagnostics.rs`; diagnostic tests | Ensure all diagnostic families have precise ranges and suggested fixes where possible. |
| 8 Front Matter And Variables | YAML metadata, layout/export controls, variable resolution, filters | Partial | `front_matter.rs`; `variables.rs`; export options; `compiler_reports_malformed_front_matter_with_source_ranges` proves invalid YAML and non-mapping front matter diagnostics include source locations and fixes; `compiler_formats_document_variables_and_reports_bad_filters` proves `default`, `trim`, `upper`, `title`, `currency`, `percent`, and `round` filters plus unsupported/nonnumeric filter diagnostics; `formatted_document_variables_survive_cross_target_exports` proves formatted variable output survives HTML, PDF, DOCX, PPTX, and Markdown bundle text | Broader UI docs/workflow proof and more front matter metadata edge cases. |

## Twenty High-Value Business Improvements

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 9.1 Git versioning | Detect repo, branch/dirty, commit, history, diff, restore, tag | Partial | `git.rs`; `git_support.rs`; versioning UI; `git_history_diff_commit_tag_and_restore_workflow`; `git_restore_and_tag_reject_option_shaped_refs`; `git_restore_refuses_symlink_targets` | Browser/desktop workflow tests and Git-free UX validation. |
| 9.1 Git-free snapshots | Local snapshots for non-Git users | Partial | `snapshot.rs`; snapshot UI/tests; Versioning panel snapshot controls; Playwright harness lists `runs snapshot restore and release tagging workflows`; `snapshot_restore_is_scoped_to_active_document_store` and `snapshot_restore_rejects_out_of_scope_and_mismatched_sources` prove restore is limited to Markdown snapshots inside the active document's configured store with matching metadata | Native restore workflow proof and broader storage-mode QA. |
| 9.2 Export snapshots | Manifest with hashes/options/app/version/status/timestamp | Partial | `manifest.rs`; `export_commands.rs`; export command tests; archived browser workflow run `26159396761` covers manifest path reporting and target-specific readiness manifest preview; manifests now include include graph evidence, an explicit readiness summary with ready/error/warning/info counts, direct-export dirty-Git readiness warnings, pre-write target/extension mismatch blocking, focused proof that Markdown bundle manifests preserve target-specific export options exactly, and `export_document_writes_optional_sidecar_manifest` proves every direct export target writes a sidecar JSON manifest that matches the returned manifest, records the exact output path, and carries the actual output SHA-256; the same direct-export test now proves `includeManifest=false` on HTML suppresses sidecar output but emits a manifest-audit info diagnostic | Add rendered/manual manifest audit evidence and native workflow proof. |
| 9.3 Release workflow | Status values, badge, draft export warning, approval metadata, release tagging | Partial | Review UI; titlebar release badge; validation; Git tag command; Playwright harness lists `runs snapshot restore and release tagging workflows`, covering status changes, approval metadata entry, release badge update, Git status/diff display, and release tag submission | Native workflow execution and export artifact release-state QA. |
| 9.4 AI paste cleanup | Normalize chat output, code fences, bullets, tables, links, citations, insert modes | Partial | `ai_cleanup.rs`; AI modal; AI cleanup tests; `ai_cleanup_normalizes_ai_code_fence_variants` proves common AI `Copy code`, spaced backtick, and tilde fence variants normalize without citation TODOs inside code; `ai_cleanup_converts_rich_html_pre_code_blocks_to_fences` proves rich `<pre><code>` clipboard blocks become Markdown fences while preserving escaped code text; shared insertion helper tests; Playwright coverage for insert, quote, appendix, replace document, merge into section, replace selection, citation TODO preview/insertion, draft marking, and provenance toggle workflows | Runtime clipboard behavior and non-sandboxed execution. |
| 9.5 AI provenance | `ai-source`, AI-assisted sections, export appendix | Partial | `provenance.rs`; review/provenance tests; Review sidebar toggles for `ai-source` and AI-assisted section review state; Playwright harness lists `toggles AI review state and clears provenance readiness warnings`; AI paste governance workflow covers `ai-source` insertion and draft review markers; `prepare_for_export_reports_ai_provenance_audit_metadata` and `prepare_for_export_reports_invalid_ai_review_statuses` prove readiness/manifest diagnostics for missing provenance metadata and invalid AI review statuses; export option tests prove AI provenance appendix inclusion/exclusion across targets | Native workflow execution and richer appendix QA. |
| 9.6 Includes | Include graph, diagnostics, re-render, export single doc | Partial | Document structure tests; compiler support; `compiler_reports_circular_and_too_deep_includes` proves circular include and max-depth diagnostics with include graph depth evidence; `compiler_reports_unreadable_include_targets_with_context` proves invalid include target diagnostics; export manifests carry `include_graph`; Markdown bundles include `include-graph.json` plus include file maps; References sidebar and command palette include navigation are covered by `navigates include graph entries from references and commands`; watcher workflow proof remains in the browser harness | Native desktop workflow proof, current host browser execution, and broader platform coverage. |
| 9.7 Business table editor | Visual editor, rows/cols, alignment, paste, sort, formats, readable Markdown, export | Partial | `src/lib/tables.ts`; table UI; frontend/backend tests; `table draft sorting preserves summary rows and typed ordering` proves table-editor sort semantics for text, currency, and date columns while retaining summary/formula rows at the bottom; `edited_table_permutation_exports_alignment_escapes_and_formula_rows`, `merged_table_cells_flow_through_semantic_exports`, and `imported_html_table_spans_flow_through_semantic_exports` now prove Markdown bundle/text table exports preserve readable header rows, alignment separators, escaped pipes, formula output rows, and merged-cell span annotations alongside native PDF/DOCX/PPTX table evidence; Playwright coverage for table insertion, pasted table import, numeric sorting, formula rows, merged-cell metadata, row/column add-remove behavior, column format totals, cancel-without-applying behavior, and apply-back-to-editor behavior | Remaining non-sandboxed browser execution and rendered/manual table QA. |
| 9.8 Calculations | Calc blocks, inline formulas, table formulas, named values/tables, dependency graph | Partial | `calculations.rs`; table tests; compiler tests; `table_formulas_reference_named_tables` proves named table/range resolution; `named_table_formulas_survive_cross_target_exports` proves resolved named table formulas across HTML, PDF, DOCX, PPTX, Markdown bundle text, and bundle AST; `calc_blocks_resolve_forward_refs_and_report_cycles` and `table_formulas_resolve_forward_refs_and_report_cycles` prove dependency/cycle diagnostics | UI proof and deeper mixed formula/table editor workflows. |
| 9.9 Equations | Inline/display math, numbering, references, export support | Partial | `rich_blocks.rs`; `compiler_renders_block_and_inline_equations` proves inline/display math, numbering, labels, captions, AST capture, and references; `compiler_renders_broader_latex_equation_syntax` proves sums, superscripts, subscripts, approximation/infinity/arrows, Omega, `bmatrix` rendering, preview/export CSS, AST capture, and DOCX/PPTX text evidence; `compiler_renders_extended_latex_equation_notation` proves optional-index roots, `\text{}` labels, overline/underline/hat/vector wrappers, left/right delimiters, set/logic symbols, ellipses, preview/export CSS, AST capture, and DOCX/PPTX text evidence; `captioned_equations_survive_cross_target_exports` proves equation captions and references across HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `prepare_for_export_reports_missing_caption_labels` proves readiness/manifest diagnostics for equations missing stable labels or captions | Rendered visual QA, deeper LaTeX permutations beyond the native subset, and editor UX tests. |
| 9.10 TOC | Automatic TOC with marker/front matter | Partial | `generated_sections.rs`; document/export tests; `generated_toc_exports_page_numbers_for_pdf_and_docx` proves numbered front matter TOC links and page-numbered PDF/DOCX export formatting | Native workflow execution, browser preview proof, and richer UI controls. |
| 9.11 Index | Automatic index and exclusions | Partial | `indexing.rs`; compiler tests; Playwright harness covers command-palette insertion of `[INDEX]`; `compiler_generates_index_from_front_matter_without_marker` proves `index.enabled: true` front matter insertion, linked terms, exclusions, and marker stripping; `front_matter_index_survives_cross_target_exports` proves `index: true` generated index content across HTML, PDF, DOCX, PPTX, and Markdown bundle artifacts | Native workflow execution and richer index manager UX. |
| 9.12 Bibliography | BibTeX/CSL JSON, citation syntax, rendered bibliography | Partial | `bibliography.rs`; citation tests; Playwright harness covers command-palette insertion of `[BIBLIOGRAPHY]`, resolved reference display, missing-key UI, and duplicate-key UI with source locations; `tests/frontend-unit.test.ts` proves bibliography-manager helpers normalize citation keys, generate locator citations, preserve resolved entry metadata, and deduplicate missing-key BibTeX stubs; the References sidebar now exposes bibliography marker/template insertion, missing-key stub insertion, repeat citation insertion, and resolved-entry copy actions; `citation_export_conformance_covers_required_cases` proves resolved, missing, multi-key, and locator citations render focusable citation-detail popovers in preview/full HTML while DOCX/PPTX citation export remains intact; `prepare_for_export_reports_missing_citation_sources_with_precise_ranges` proves no-bibliography citation readiness ranges and manifest parity; `compiler_reports_duplicate_bibliography_keys` proves duplicate-key source ranges and first-occurrence related context; `compiler_reports_csl_and_hayagriva_duplicate_key_locations` proves CSL JSON and Hayagriva YAML duplicate-key source ranges; `compiler_reports_duplicate_keys_across_external_bibliography_files` proves multiple bibliography front matter paths and duplicate diagnostics across separate external files; `compiler_parses_better_bibtex_edge_cases_without_splitting_at_symbols` proves Better BibTeX-style metadata skipping, parenthesis entries, dotted keys, protected title braces, and `@` characters inside values; `compiler_loads_csl_json_object_variants_with_full_author_dates` proves CSL JSON wrapper objects, single DOI-style keys, full given/family author names, literal authors, raw dates, and string `date-parts`; `compiler_maps_common_csl_style_aliases_to_native_renderers` proves APA/Chicago/Harvard-style aliases map to author-year output and IEEE/Vancouver/Nature/AMA-style aliases map to numeric output without unsupported-style warnings; `compile_options_supply_csl_alias_default_citation_style` and `prepare_for_export_validates_brand_and_default_style_options` prove CSL aliases can be export defaults while unknown styles remain blocked; frontend unit coverage now proves persisted bibliography defaults preserve supported aliases such as `APA` -> `apa` instead of falling back to title; References and Settings selectors expose common aliases; `compiler_renders_numeric_citations_and_numbered_bibliography` and `compiler_warns_and_falls_back_for_unsupported_csl_style` prove built-in numeric style rendering and unsupported CSL-style warnings | Full CSL processor fidelity beyond deterministic native aliases, native workflow execution, and deeper rendered/manual citation-manager QA. |
| 9.13 Cross references | Figures/tables/equations/headings, broken ref diagnostics, export links | Partial | `references.rs`; citation/export tests; readiness tests now warn when figure/table/equation targets lack stable labels before export; duplicate reference labels are compile errors with source ranges and first-occurrence context, and `prepare_for_export_blocks_duplicate_reference_labels_in_manifest` proves the diagnostic blocks readiness and is copied into manifests; `rich_markdown_blocks_survive_cross_target_exports` proves table, figure, and equation cross references in compiled Markdown, HTML links, PDF text, DOCX text, PPTX slide text, and Markdown bundle text/AST evidence; `heading_appendix_and_decision_references_survive_cross_target_exports` proves section, appendix, and decision reference labels across HTML, PDF, DOCX, PPTX, and Markdown bundle artifacts | Native workflow execution and richer manager/navigation UX. |
| 9.14 Captions | Figures/table captions, numbering, list support | Partial | AST/export/table/figure tests; `compiler_generates_lists_of_figures_and_tables` covers `[LIST_OF_FIGURES]`, `[LIST_OF_TABLES]`, caption numbering, anchor links, fenced-example exclusion, preview HTML, and DOCX artifact text; `rich_markdown_blocks_survive_cross_target_exports` extends figure/table generated-list evidence across HTML, PDF, DOCX, PPTX, and Markdown bundle outputs; `media_export_tests` now prove Markdown bundles include `media-uses.json` with per-figure IDs, captions, alt text, source files, source ranges, bundle paths, hashes, and fit/crop metadata; Playwright harness covers command-palette insertion of both caption-list markers; `prepare_for_export_reports_missing_caption_labels` proves readiness/manifest diagnostics for missing figure/table/equation labels or captions | Native workflow execution and rendered/manual artifact QA. |
| 9.15 Advanced layout | Page size/orientation/margins/columns/breaks/headers/footers/keeps/floats | Partial | `layout.rs`; `paged_document.rs`; export tests | Rendered/manual QA and overflow hardening. |
| 9.16 Brand templates | Brand name/color/logo/font/header/footer/watermark/legal disclaimer | Partial | Settings UI; export option tests; `prepare_for_export_validates_brand_and_default_style_options` proves readiness rejects malformed brand color/default profile option shapes; `export_conformance_fixture_maps_business_features` asserts watermark, headers/footers, brand package metadata, and legal disclaimer sections across export targets; example fixture proof now keeps audience metadata alongside branded export metadata across HTML, DOCX, PPTX, and Markdown bundles | Template/profile persistence and broader cross-target visual proof. |
| 9.17 Review comments/change notes | Comments, unresolved validation, exports | Partial | `review.rs`; review UI; export conformance tests; `export_conformance_fixture_maps_business_features` asserts release approval metadata and legal disclaimer package evidence; `prepare_for_export_reports_review_change_note_audit_metadata` proves readiness/manifest diagnostics for missing review and change-note audit metadata | UI workflow tests and native target fidelity. |
| 9.18 Document variables | Front matter/project/data variables | Partial | `variables.rs`; project variable tests; `compiler_formats_document_variables_and_reports_bad_filters` proves document variable defaults, chained text filters, numeric filters, and filter diagnostics; `formatted_document_variables_survive_cross_target_exports` proves formatted variables survive the export artifact family | UI workflow proof and additional filter edge cases. |
| 9.19 Pikchr diagrams | Native fallback/external setup and diagnostics | Partial | `transforms/diagram.rs`; `external.rs`; transform tests; `pikchr_native_fallback_handles_semicolon_shapes_and_arrow_labels` proves semicolon-separated native Pikchr statements, connector labels, and box/diamond/cylinder/file shape rendering without requiring an external engine; `visual_data_transforms_survive_cross_target_exports` now carries the same native Pikchr artifact through HTML, PDF, DOCX, PPTX, and Markdown bundle evidence | Cross-platform optional engine proof and deeper full-Pikchr fidelity via installed engines. |
| 9.20 Validation | One-click prepare report across metadata/includes/citations/formulas/figures/transforms/settings/links/comments | Partial | `validation.rs`; `export_commands.rs`; readiness UI; `prepare_for_export_carries_broad_readiness_audit_to_manifest` proves broad report/manifest parity for metadata, approval, layout, citations, comments, AI provenance, links/media, formulas, captions, transform settings, and progress state; `prepare_for_export_validates_transform_engine_paths_before_export` proves configured transform engine paths must be real executable files while disabled engines are ignored; `prepare_for_export_reports_target_specific_pptx_blockers` proves PPTX readiness blocks unapproved presentation exports and copies the diagnostic into manifest readiness; `prepare_for_export_reports_target_specific_option_info` proves valid-but-ignored target options and disabled sidecar manifests become non-blocking info diagnostics in readiness and manifests; `prepare_for_export_reports_empty_appendix_options_as_info` proves enabled but empty glossary/comment/provenance appendix options become manifest-backed info diagnostics; `prepare_for_export_validates_brand_and_default_style_options` proves citation-style defaults, brand colors, brand profile fields, dirty-Git toggles, and option aliases are validated before export; `prepare_for_export_reports_empty_generated_reference_sections` proves requested-but-empty generated index/glossary sections warn and copy diagnostics into manifest readiness; `prepare_for_export_reports_missing_citation_sources_with_precise_ranges` proves no-bibliography citation readiness ranges and manifest parity; `prepare_for_export_blocks_duplicate_reference_labels_in_manifest` proves duplicate reference labels block readiness and retain manifest audit context; export manifest tests prove include graph evidence is preserved; archived browser workflow run `26159396761` covers target-specific readiness diagnostics and blocked export behavior; Rust readiness tests cover malformed review/comment, AI provenance audit metadata, missing figure/table/equation caption labels, explicit manifest readiness summaries, and dirty-Git warnings in direct export manifests | Remaining readiness work is more target-specific option blockers, further reference edge cases, plus non-sandboxed/native execution. |

## Fenced-Code Transform System

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 10.1 Architecture | Registry, option validation, artifact cache, diagnostics | Partial | `transforms/renderer.rs`; `transforms/options.rs`; `transforms/external.rs`; transform registry metadata now declares aliases and output kinds; `transform_aliases_render_through_canonical_artifacts` proves `vegalite`, `jsonschema`, `schema`, `yml`, and `graph` aliases render through canonical artifacts; external transform tests cover adapter invocation shape, Graphviz alias settings inheritance from `dot`, missing-engine-path setup diagnostics, disabled-engine fallback, cache diagnostics, persistent cache reuse, and same-path executable cache invalidation; `prepare_for_export_validates_transform_engine_options` covers disabled-engine option validation | More structured artifact use in exports and broader option combinations. |
| 10.2 Safety | No network, no shell, trust, timeout, output limits, source hash cache | Partial | External transform runner and tests; `external_transform_cache_invalidates_when_trusted_executable_changes` proves cache identity changes when a trusted executable is rewritten at the same path, using engine file size/mtime plus adapter/input/source identity; `compiler_reports_missing_external_engine_path_before_embedded_fallback` proves missing optional engine paths are reported before embedded fallback; `compiler_skips_disabled_external_transform_without_trust_warning` proves disabled external engines are skipped before trust/path execution and fall back to embedded rendering | Cross-platform process behavior proof and broader executable edge cases. |
| 10.3 Core transforms | `calc`, `mermaid`, `pikchr` | Partial | Renderer and transform tests; native Pikchr fallback now covers compact semicolon-separated statements plus common business diagram shapes and connector labels, with cross-target export evidence in `visual_data_transforms_survive_cross_target_exports` | Fidelity limitations documented; optional engine proof for Pikchr. |
| 10.4.1 DOT/Graphviz | SVG diagrams and engines | Partial | Native fallback; external adapter; transform registry and trusted external execution now cover `dot`, `graphviz`, `circo`, `neato`, `fdp`, `osage`, and `twopi`; `compiler_uses_trusted_graphviz_variant_transform_preferences` proves Graphviz variant preferences route through no-shell external execution; macOS installed-engine conformance now verifies every Graphviz variant listed above | Windows installed-engine proof for every variant and rendered/manual diagram QA. |
| 10.4.2 PlantUML | SVG/PNG enterprise diagrams | Partial | External adapter and fallback; `compiler_uses_plantuml_png_fence_output_format` proves `format=png` fences request a trusted PlantUML PNG sidecar and embed it as a PNG data URL; adapter tests prove `-tsvg` and `-tpng` no-shell invocation shapes | Real PlantUML PNG installed-engine proof and cross-platform proof. |
| 10.4.3 D2 | SVG diagrams | Partial | External adapter and fallback | Cross-platform proof. |
| 10.4.4 Vega-Lite | Charts | Partial | Native SVG subset in `visual_data.rs`; `vega_lite_area_mark_renders_static_svg_preview` proves `area` joins `bar`, `line`, and `point` static rendering; `vega_lite_color_encoding_renders_grouped_series_preview` proves `encoding.color.field` grouped/series rendering with legend and per-series marks; `vega_lite_unsupported_marks_report_supported_static_subset` proves unsupported marks report the supported native subset; `visual_data_transforms_survive_cross_target_exports` proves Vega-Lite static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `docs/markdown-extensions.md` documents the native static subset | Broader Vega-Lite grammar support and rendered/manual visual QA. |
| 10.4.5 Chart | Bar/line/pie/area/KPI | Partial | `chart.rs`; transform tests; `chart_transform_renders_pie_area_and_kpi_specs` proves chart variants; `visual_data_transforms_survive_cross_target_exports` proves chart static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence | Rendered/manual chart QA and broader chart styling/options. |
| 10.4.6 GeoJSON | Static map preview | Partial | `visual_data.rs`; transform tests; `geojson_transform_preserves_geometry_types_in_static_svg_preview` proves FeatureCollection, Polygon, and MultiPoint render as typed SVG shapes; `visual_data_transforms_survive_cross_target_exports` proves GeoJSON static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `docs/markdown-extensions.md` documents the supported native subset | Rendered/manual map QA and broader CRS/projection expectations. |
| 10.4.7 TopoJSON | Static map preview | Partial | `visual_data.rs`; transform tests; `topojson_transform_resolves_object_arc_references` proves Topology objects resolve polygon arc references, including reversed arcs; `visual_data_transforms_survive_cross_target_exports` proves TopoJSON static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `docs/markdown-extensions.md` documents the supported native subset | Rendered/manual map QA and broader topology edge cases. |
| 10.4.8 STL | Static preview | Partial | `visual_data.rs`; transform tests; `stl_transform_renders_ascii_static_svg_preview` proves ASCII STL projection; `visual_data_transforms_survive_cross_target_exports` proves STL static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence | 3D fidelity limits and rendered/manual visual QA. |
| 10.4.9 CSV | Tables and formulas | Partial | `tables.rs`; transform/table tests; `compiler_loads_front_matter_csv_data_sources` proves local CSV front matter data source loading with formula evaluation and manifest include evidence; `compiler_blocks_data_sources_outside_document_folder` proves absolute paths and `..` parent-directory escapes are rejected before file reads; `compiler_blocks_symlinked_data_sources_outside_document_folder` proves resolved symlink escapes are blocked on Unix | UI proof and broader data source validation. |
| 10.4.10 TSV | Tables and formulas | Partial | `tables.rs`; transform tests; `compiler_loads_front_matter_json_and_yaml_data_sources` proves the front matter data source collector now accepts the same external-file path flow used by CSV/TSV aliases; `compiler_blocks_data_sources_outside_document_folder` and `compiler_blocks_symlinked_data_sources_outside_document_folder` prove local relative path boundaries for front matter data sources | UI proof and broader data source validation. |
| 10.4.11 JSON | Structured rendering | Partial | `structured.rs`; transform tests; `compiler_loads_front_matter_json_and_yaml_data_sources` proves JSON files referenced from front matter render through structured table artifacts; `front_matter_data_sources_survive_cross_target_exports` proves JSON data source rows survive HTML, PDF, DOCX, PPTX, Markdown bundle text, and manifest evidence; `compiler_blocks_data_sources_outside_document_folder` proves absolute paths and parent escapes are blocked; `compiler_blocks_symlinked_data_sources_outside_document_folder` proves canonicalized symlink escapes are blocked on Unix | Larger/nested docs, schema interactions, and UI proof. |
| 10.4.12 YAML | Structured rendering | Partial | `structured.rs`; transform tests; `compiler_loads_front_matter_json_and_yaml_data_sources` proves YAML files referenced from front matter render through structured tree artifacts; `front_matter_data_sources_survive_cross_target_exports` proves YAML data source content survives HTML, PDF, DOCX, PPTX, Markdown bundle text, and manifest evidence; `compiler_blocks_data_sources_outside_document_folder` proves absolute paths and parent escapes are blocked; `compiler_blocks_symlinked_data_sources_outside_document_folder` proves canonicalized symlink escapes are blocked on Unix | Larger/nested docs, schema interactions, and UI proof. |
| 10.4.13 OpenAPI | API docs | Partial | `structured.rs`; `document_ast.rs`; `compiler_renders_openapi_and_json_schema_tables` covers title/version, servers, global security requirements, operation IDs, deprecated operations, external docs, tags, path and operation parameters, request bodies, response media examples, response headers, links, callbacks, webhooks, discriminators, security schemes, media types, and component schemas; `api_schema_transforms_survive_cross_target_exports` proves OpenAPI transform artifacts, output hashes, source ranges, semantic table caption preservation, security/header/link/example/callback/webhook/discriminator content, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence | Broader OpenAPI edge cases such as callback nesting depth, webhook parameter combinations, rendered/manual QA, and UI proof. |
| 10.4.14 JSON Schema | Schema docs | Partial | `structured.rs`; `document_ast.rs`; `compiler_renders_openapi_and_json_schema_tables` covers title/description, nested field paths, array item paths, required flags, descriptions, refs, enums, formats, defaults, constraints, pattern properties, tuple `prefixItems`, object `additionalProperties`, dependent required/dependent schemas, `oneOf` composition, conditionals, `$defs`, `propertyNames`, `contains` with `minContains`/`maxContains`, `unevaluatedItems`/`unevaluatedProperties`, and content annotations with `contentEncoding`, `contentMediaType`, and nested `contentSchema`; `api_schema_transforms_survive_cross_target_exports` proves JSON Schema transform artifacts, output hashes, source ranges, semantic table caption preservation, composition/object-keyword/conditional/definition/content-schema content, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence | Broader schema dialect edge cases, rendered/manual QA, and UI proof. |
| 10.4.15 BibTeX | Bibliography rendering | Partial | `business.rs`; `bibliography.rs`; `bibtex_transform_renders_bibliography_preview` proves inline BibTeX title/author/year/date metadata parsing and transform preview rendering; `bibtex_transform_survives_cross_target_exports_with_metadata` proves BibTeX transform artifacts, output hashes, source ranges, bibliography metadata, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `compiler_parses_better_bibtex_edge_cases_without_splitting_at_symbols` proves the bibliography service handles Better BibTeX-style metadata records, parenthesis-delimited entries, dotted keys, protected title braces, and `@` characters inside values | Citation-manager UX, deeper CSL style fidelity, and native/browser workflow execution. |
| 10.4.16 Glossary | Definitions and term rendering | Partial | `business.rs`; glossary/index tests; `compiler_generates_glossary_sections_from_marker_and_metadata` proves `[GLOSSARY]` marker replacement, front matter-driven generated glossary insertion, preview hover preservation, and DOCX glossary artifact text; References sidebar includes a Glossary manager for generated glossary insertion, glossary definition templates, export glossary option enablement, term navigation, and index-marker insertion; Playwright harness lists command-palette insertion plus manager workflows for glossary/index controls | Native workflow execution and deeper rendered UX QA. |
| 10.4.17 Timeline | Timeline SVG | Partial | `business.rs`; `timeline_transform_renders_static_svg_preview`; `visual_data_transforms_survive_cross_target_exports` proves timeline static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence | Rendered/manual timeline QA and native/browser workflow execution. |
| 10.5 Later transforms | roadmap, ADR, diff, QR, etc. | Partial | `roadmap`, `adr`, `diff`, and `qr` are first-release native safe transforms; `business_workflow_transforms_render_static_html` proves their renderer classes, roadmap metadata, ADR key classes, and diff summaries; `safe_business_transforms_survive_cross_target_exports` proves roadmap/ADR/diff/QR artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `docs/markdown-extensions.md` documents supported syntax and explicitly defers execution-heavy `python`, `r`, `sql`, `wavedrom`, `nomnoml`, `latex`, and raw `html` transforms until safe sandbox/static renderers exist | Native/browser workflow execution and rendered/manual visual QA for safe business transforms. |

## AI, Versioning, Tables, Equations, Citations

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 11 AI workflow | Paste cleanup preview and governance | Partial | AI modal, store actions, backend tests; AI cleanup now proves chat labels, headings, lists, CSV/TSV tables, rich HTML links/tables, rich HTML code blocks, common AI code fence variants, citation TODO insertion, provenance blocks, and draft markers; browser harness lists insert, quote, appendix, replace-document, merge-into-section, replace-selection, citation TODO, draft marker, provenance block, and review-state workflows | Runtime clipboard proof, non-sandboxed browser execution, native workflow execution, and richer rendered/manual QA. |
| 12 Versioning | Snapshots, Git integration, version metadata | Partial | Snapshot/Git modules and UI; Versioning panel now groups Git status, diff, history, release tagging, snapshot creation, snapshot listing, and snapshot restore; Playwright harness lists the snapshot restore and release tagging workflow | Native execution and broader Git/non-Git storage-mode proof. |
| 13 Tables/data | Table editing, formulas, data sources | Partial | Table lib/UI/backend tests; frontend unit coverage now proves table-editor sort semantics for text, currency, and date formats with summary/formula rows retained at the bottom; `named_table_formulas_survive_cross_target_exports` proves named table/range formulas survive the export artifact family; `compiler_loads_front_matter_json_and_yaml_data_sources` and `front_matter_data_sources_survive_cross_target_exports` prove front matter CSV/TSV/JSON/YAML local data source flows through compile, transform artifacts, manifests, and export targets; `compiler_reports_malformed_front_matter_data_sources` proves missing path, unsupported type, and unreadable source diagnostics; `compiler_blocks_data_sources_outside_document_folder` and `compiler_blocks_symlinked_data_sources_outside_document_folder` prove data source imports stay inside the document folder | Interaction tests, UI proof, and more edge-case validation. |
| 14 Equations | Math authoring/render/export | Partial | Rich block parsing/export tests; `compiler_renders_broader_latex_equation_syntax` proves broader native math syntax including matrices and common operators; `compiler_renders_extended_latex_equation_notation` proves optional-index roots, text spans, accent/line wrappers, delimiters, set/logic symbols, ellipses, CSS, AST, and DOCX/PPTX text evidence; `captioned_equations_survive_cross_target_exports` proves captioned equation references and caption text across HTML, PDF, DOCX, PPTX, and Markdown bundle artifacts | Rendered visual QA and deeper LaTeX permutations beyond the native subset. |
| 15 Bibliography/citations | BibTeX/CSL/citation rendering | Partial | Bibliography/citation tests; `compiler_parses_better_bibtex_edge_cases_without_splitting_at_symbols` and `compiler_loads_csl_json_object_variants_with_full_author_dates` prove richer BibTeX and CSL JSON import compatibility; `compiler_maps_common_csl_style_aliases_to_native_renderers` proves deterministic CSL alias rendering for common author-year and numeric style families; `citation_export_conformance_covers_required_cases` proves citation preview popovers carry resolved bibliography titles, locator context, multi-key details, and missing-entry messages in preview/full HTML while DOCX/PPTX export evidence remains intact; frontend unit coverage proves citation-manager repair snippet generation and the References sidebar exposes marker/template/stub/cite/copy actions for bibliography repair; frontend persistence now preserves supported CSL aliases in bibliography defaults and the References/Settings selectors expose common aliases; duplicate reference label diagnostics block ambiguous anchors; `malformed_reference_markers_are_reported_with_source_ranges` proves malformed labels/cross references reject empty keys, spaces, slash characters, and unclosed markers with source-ranged errors | Full CSL processor fidelity beyond native aliases, native workflow execution, and deeper rendered/manual citation-manager QA. |
| 16 Index/glossary | Index and glossary generation | Partial | Indexing/glossary modules/tests; generated glossary marker and front matter section proof; front matter-generated index proof; export conformance covers glossary appendix output and index artifacts; `prepare_for_export_reports_empty_generated_reference_sections` proves readiness warns when generated index/glossary sections are requested without terms or definitions; References sidebar includes Index and Glossary manager controls for generated section insertion, front matter index enablement, term navigation, and glossary-to-index marker creation; Playwright harness lists the citation/glossary/index workflow with manager assertions | Native workflow execution and deeper rendered UX QA. |
| 17 Layout/reflow | Layout model/directives/export mapping | Partial | Layout, paged document, export tests; rich block export tests now include block quotes, callouts, lists, code blocks, tables, figures, equations, and generated sections across target artifacts | Rendered visual QA and overflow/performance work. |
| 18 Export system | Targets, options, manifest | Partial | Export modules/tests and UI; `export_option_matrix_is_preserved_across_targets_and_bundle_evidence` proves one explicit disabled export option matrix across every target artifact family plus Markdown bundle manifest evidence; `enabled_export_option_matrix_survives_cross_target_artifacts` proves enabled cover, page numbers, styles, syntax highlighting, watermark, layout preset, appendices, PPTX agenda, text output, and Markdown bundle manifest evidence across HTML/PDF/DOCX/PPTX/Markdown bundle artifacts; `prepare_for_export_reports_target_specific_pptx_blockers` proves target-aware readiness blocks PPTX exports without approved release metadata; `prepare_for_export_reports_target_specific_option_info` proves target-aware option diagnostics for non-PPTX agenda options and Markdown bundle render-only options; `prepare_for_export_reports_markdown_bundle_manifest_sidecar_info` proves target-aware readiness distinguishes the Markdown bundle embedded manifest from optional sidecar manifests; `prepare_for_export_reports_empty_appendix_options_as_info` proves content-sensitive appendix option diagnostics for glossary/comment/provenance exports; `prepare_for_export_validates_brand_and_default_style_options` proves malformed export option shapes are blocked before writing; `prepare_for_export_reports_empty_generated_reference_sections` proves generated reference-section readiness diagnostics are copied into export manifests; `prepare_for_export_blocks_malformed_reference_markers_in_manifest` proves malformed reference marker errors are copied into export manifests; `export_document_blocks_target_extension_mismatches_before_writing` proves direct exports refuse mismatched target/output extensions before writing artifacts or sidecar manifests; `export_document_writes_optional_sidecar_manifest` proves direct HTML/PDF/DOCX/PPTX/Markdown bundle exports return and write matching manifests with exact output paths, hashes, readiness, and progress steps, and now proves Markdown bundles with sidecar manifests disabled still embed `manifest.json`; `rich_markdown_blocks_survive_cross_target_exports` proves common Markdown/business blocks across every export target artifact family; `example_project_fixtures_compile_and_export` proves README-linked business examples carry title, audience, feature markers, package metadata, source hash custom properties, semantic metadata, and bundle evidence across target artifacts | Artifact-level completion audit, more option combinations, rendered/manual QA, and cross-platform packaging proof. |

## Workspace, Commands, Preferences, Security, Accessibility, Performance

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 19 Workspace/tab groups | Tabs, groups, pinning, recents, restore | Partial | `src/App.vue`; store persistence; Playwright harness covers folder/document-set tab groups, drag-to-document-set front matter updates, close-group behavior, pinning, recents, and restore | Native desktop proof, drag/reorder edge cases, and saved workspace/package evidence. |
| 20 Command palette | Search commands/headings/citations/glossary/index/documents/workspace files | Partial | `commands` computed in `src/App.vue`; Playwright table insertion command test; archived browser workflow run `26154535588` covers heading command navigation; archived browser workflow run `26155535210` covers citation, glossary, and index command navigation; archived browser workflow run `26156393184` covers open-document switching and workspace-file opening | Broader keyboard shortcuts and non-sandboxed/native execution. |
| 21 Preferences | Theme, typography, export, Git, AI, transforms, recents | Partial | Settings UI; schema-versioned persisted workspace migration in `src/lib/workspacePersistence.ts`; `tests/frontend-unit.test.ts` covers migration/schema normalization for legacy aliases, clamped settings, records, transform settings including disabled external engines, AI cleanup defaults, and scroll positions; archived browser workflow run `26157446711` covers external transform path changes, input mode, timeout, trust prompt, denied trust reset, successful probe UI, and missing-executable diagnostics | Broader settings workflow coverage and native runtime proof. |
| 22 Security/privacy | Local-first, trust-gated executable transforms, no shell | Partial | `docs/security-threat-model.md`; external transform runner/tests; local file design; Git tag/revision validation and symlink restore guards in `git_support.rs`; snapshot restore store/source validation in `snapshot.rs`; `git_restore_and_tag_reject_option_shaped_refs`; `git_restore_refuses_symlink_targets`; `snapshot_restore_is_scoped_to_active_document_store`; `snapshot_restore_rejects_out_of_scope_and_mismatched_sources`; `compiler_skips_disabled_external_transform_without_trust_warning` proves disabled engines do not execute or emit trust-failure noise; `prepare_for_export_validates_transform_engine_paths_before_export` proves readiness rejects missing, directory, and non-executable configured engine paths before export while allowing disabled engines to remain ignored; archived browser workflow run `26157446711` covers transform trust prompts, path-change trust clearing, denied trust reset, and missing executable diagnostics | Broader independent security review and cross-platform process proof. |
| 23 Accessibility | Keyboard, ARIA, contrast, reduced motion | Partial | Some labels/roles/settings in UI; `pnpm run check:a11y` statically checks Vue template button names, form-control labels, dialog labeling, dialog focusability/keyboard handling, required skip-link targets, status/progress live regions, diagnostic list labels, conflict diff cell group labels, table editor grid/cell/total/action labels, high-contrast CSS contracts, reduced-motion CSS contracts, and primary editor/preview surface labels; modal close buttons, command-palette search, conflict merge-line controls, merge composition reorder/remove controls, diagnostics, conflict diff rows, table editor controls, CodeMirror editor content, and the rendered preview document now have explicit labels/roles; the workbench now exposes visible-on-focus skip links to commands, workspace, sidebar, source, preview, and status landmarks; AI paste, command palette, and conflict dialogs now receive initial focus, trap Tab at the dialog edge, close on Escape, and restore focus to the invoking control; the status bar now exposes status messages, watch status, compile progress, export progress, and errors as labeled live regions; Playwright harness discovery lists `exposes keyboard skip links to primary workbench regions`, `manages modal focus and Escape return paths`, and `exposes status and progress messages as live regions`; the diagnostics, conflict, table, settings, and boot workflows now assert accessible list/listitem/group/output/textbox/document names plus high-contrast/reduced-motion computed state in the browser harness | Full keyboard-only/manual audit, current-host browser execution, native workflow proof, and broader editor/preview interaction coverage. |
| 24 Performance | Large docs, debounced preview, transform cache, progress | Partial | Debounce/cache/progress code; `performance_tests::compiler_stress_handles_large_documents_with_many_artifacts` stress-compiles nested includes, many tables, formulas, transform artifacts, source maps, and diagnostics; `performance_tests::repeated_export_loop_keeps_large_artifacts_stable` repeatedly renders a large compiled document across HTML, PDF, DOCX, PPTX, and Markdown bundle outputs with structural and artifact-size assertions; `performance_tests::repeated_editing_sessions_reuse_external_transform_cache` repeatedly recompiles edited document text while proving a stable trusted external DOT transform is served from cache after the first run; `performance_tests::repeated_compile_export_cycles_keep_memory_growth_bounded` repeats large compile/export cycles while retaining only summaries and bounding process RSS growth on macOS/Linux hosts; `export_command_tests::export_readiness_and_manifest_report_progress_steps` proves readiness/export manifests carry compile, transform, readiness, render, and manifest progress steps; the export UI now displays readiness and last-export progress steps; `tests/frontend-unit.test.ts` covers latest-document task cancellation so stale/cancelled compile results cannot overwrite newer editor state and covers preview debounce timing/coalescing within the small-document preview target; `e2e/app-workflows.spec.ts` contains a large-document edit/preview responsiveness workflow | Run browser performance workflow on a host with Playwright Chromium installed, add deeper long-running memory profiling, and add native desktop performance proof. |

## Architecture, Storage, Phases, Acceptance

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 25.1 Frontend architecture | Views/stores/components | Partial | App/store exist; few extracted components | Modularize oversized frontend after workflow tests. |
| 25.1.1 Tauri contract | File ops, compile, export, snapshots, Git, transform commands | Partial | `src-tauri/src/lib.rs`; `docs/ipc-command-coverage.md`; store invokes commands; `ipc_command_tests::spec_25_4_ipc_commands_are_registered_and_documented` covers the spec IPC subset and the full registered Tauri command surface; the coverage table now records implementation/evidence rows for file, watcher, workspace, compile, export, snapshot, Git, transform, and AI cleanup commands; `git_history_diff_commit_tag_and_restore_workflow` directly exercises `get_git_status`, diff, commit, history, tag, and restore | Broader UI workflow proof for every extended command path. |
| 25.2 State stores | Documents/workspace/editor/preview/compiler/diagnostics/exports/preferences/versioning/transforms/bibliography/AI | Partial | Mostly centralized in `documents.ts` | Split store by domain after behavior is locked. |
| 25.3 Rust backend | Filesystem, watcher, compiler, transform, export, Git, snapshot, diagnostics, external runner | Partial | Rust modules exist; full registered IPC command coverage audit is executable via `ipc_command_tests::spec_25_4_ipc_commands_are_registered_and_documented`; command rows map each registered native command to implementation and current direct or registration evidence | Continue modularization and deeper command behavior coverage. |
| 25.4 IPC commands | Required command list | Complete | `docs/ipc-command-coverage.md`; `src-tauri/src/lib.rs`; `ipc_command_tests::spec_25_4_ipc_commands_are_registered_and_documented` parses the spec, coverage table, and Tauri handler registration, proves all spec-required commands are registered/documented, and proves the table exactly matches every registered command | Keep the table and guardrail test current if the spec command list changes. |
| 26 Data storage | Preferences, recents, brand profiles, transform paths, snapshots, sidecars | Partial | `docs/storage-model.md`; Store plugin usage; schema-versioned workspace persistence migration; frontend unit migration/schema test; snapshot/export manifest modules; snapshot restore source/store scope tests; `export_document_writes_optional_sidecar_manifest` proves sidecar manifests across HTML, PDF, DOCX, PPTX, Markdown bundle, and disabled-manifest behavior | Native settings/snapshot storage proof and sidecar workflow proof in a real desktop run. |
| 27 Implementation phases | Phase deliverables | Partial | Most phase surfaces exist | Phase completion must be proven by matrix and tests. |
| 28 Acceptance criteria | Concrete app acceptance | Partial | Current build/test surfaces; `pnpm run check:e2e-env` passes the project-local Playwright Chromium launch preflight; `pnpm run test:e2e` passes 42 Chromium browser workflow tests locally; `pnpm run test:desktop-smoke` checks desktop build artifacts and the native command workflow smoke; `NEDITOR_DESKTOP_SMOKE_LAUNCH=1 pnpm run test:desktop-smoke` proves bounded macOS native GUI startup and writes `.tmp/desktop-smoke/launch-report.json` with PID, elapsed window, captured output, and `processAlive: true` evidence; `pnpm run test:tauri-webdriver` is wired into full verification and provides a Windows/Linux Tauri-driver harness for native title, main command rendering, mode switching, command-palette opening, dirty-title state, export readiness, and preference persistence across a desktop restart; rendered export/package proof includes `representative_rendered_export_artifacts_are_package_inspectable` | Execute the WebDriver harness on supported Windows/Linux hosts and broaden platform/rendered proof. |
| 29 Non-goals | Cloud collaboration and overreach controls | Complete | `docs/specification.md` lists first-release non-goals; `README.md` now states what NEditor is not; `docs/user-guide.md` documents product boundaries around local-first files, no cloud sync, no real-time multiplayer collaboration, no mobile/web app, no server-side rendering, no full WYSIWYG editor, no enterprise identity layer, and no arbitrary plugin marketplace; `pnpm run check:docs` verifies the linked docs | Revisit only if the first-release scope changes. |
| 30 Architecture decisions | Licensing, editor, parser, PDF/DOCX/PPTX/citations/formulas/transforms/snapshots/dependency gate | Partial | `LICENSE`; `package.json`; `src-tauri/Cargo.toml`; `src-tauri/tauri.conf.json`; implementation follows many decisions; dependency doc exists; `pnpm run check:deps` guards manifest dependency admission coverage, canonical MIT license text, and MIT package metadata | Keep dependency admission current, document deviations, and broaden rendered/workflow proof for the remaining architecture decisions. |
| 31 First milestone | Prove architecture with representative features | Partial | Current code strongly exceeds first milestone in breadth | Still unverified until fresh baseline and workflow tests pass. |

## External Transform Setup Matrix

| Setup doc section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| Safety model | Real executable paths, per-engine trust, bounded execution, cache keys, fallback | Partial | `transforms/external.rs`; external transform tests; `prepare_for_export_validates_transform_engine_paths_before_export`; `compiler_skips_disabled_external_transform_without_trust_warning`; `external_transform_cache_invalidates_when_trusted_executable_changes`; archived browser workflow run `26157446711` covers settings-level path change trust clearing, trust prompts, denied trust reset, input mode/timeout probe details, cache identity display, and missing-executable diagnostics | Cross-platform process proof and deeper executable edge cases. |
| macOS setup | Graphviz, D2, Pikchr, Java/PlantUML paths | Partial | `docs/external-transform-platform-evidence.md`; `pnpm run check:engines` reports Graphviz `dot`, `circo`, `neato`, `fdp`, `osage`, `twopi`, D2, PlantUML, and Java on this Darwin arm64 host; `cargo test --locked external_transform_conformance_runs_installed_engines --lib -- --nocapture` verified those Graphviz engines, D2, and PlantUML through the external transform execution path | Install and verify Pikchr on macOS. |
| Linux setup | Packages and optional engines | Partial | Historical workflow installed Linux engines; current proof requires local installed-engine checks | Keep installed-engine conformance stable. |
| Windows setup | Winget paths and shim guidance | Unverified | Documentation exists | Add manual evidence on Windows. |
| Engine defaults | stdin/file modes by engine | Partial | Adapter profiles and tests; `compiler_uses_dot_settings_for_graphviz_alias` proves `graphviz` fences inherit the configured `dot` path, trust, and input mode; macOS installed-engine conformance verified dot stdin, d2 stdin, and PlantUML file mode in `docs/external-transform-platform-evidence.md`; Graphviz variant metadata now exposes separate default commands/version probes for `circo`, `neato`, `fdp`, `osage`, and `twopi` | Cross-platform confirmation and macOS Pikchr proof. |
| Troubleshooting | Permission, empty output, timeout, trust disabled, cache stale | Partial | Diagnostics/failure hints; external transform tests now cover missing engine path, permission, timeout, stderr, missing output, output limit, disabled-engine, and cache-stale diagnostics | UI docs and platform-specific cases. |

## Verification Coverage Summary

Current direct evidence:

- Backend Rust tests cover many compiler/export/transform/table/file/Git/snapshot
  behaviors under `src-tauri/src/tests/`.
- Backend Rust stress coverage now includes a large-document compiler stress
  case for nested includes, many tables, formulas, transform artifacts, source
  maps, and diagnostics.
- Frontend unit tests cover table parsing/serialization, conflict diff
  alignment, and conflict merge composition under `tests/frontend-unit.test.ts`.
- Local full verification covers frontend checks/tests, frontend production
  build, dependency/docs/accessibility guards, Rust formatting/checks/clippy,
  native-watch check, 213 Rust tests, optional engine probing, Tauri no-bundle
  release compile, macOS `.app` bundle build/smoke on this host, strengthened
  desktop smoke, and the desktop WebDriver harness step on the current host.
- `pnpm run check:docs` now discovers README plus all top-level docs Markdown
  files and checks local links, including the user guide, Markdown extensions
  reference, and `docs/specification.md` architecture figure target.
- `pnpm run check:a11y` now checks static Vue template accessibility guardrails
  for accessible control names, dialog labeling, dialog focusability/keyboard
  handling, required skip-link targets, and status/progress live regions.

Current major verification gaps:

- Archived remote workflow evidence on commit `02e832a` was green across browser workflows and
  Ubuntu/macOS/Windows desktop builds. The earlier Windows path-sensitive
  Rust-test failures, Ubuntu installed Pikchr conformance failure, and Ubuntu
  fake-`d2` stdin fixture failure were resolved there. This is historical
  evidence only, not the current verification source.
- Browser-level workflow harness previously passed in Linux Actions run `26159396761` with 28
  Chromium tests, including advanced table editor coverage, mocked file
  lifecycle coverage, save-as, recently closed reopening, and stale-save
  conflict copy/merge/keep-local/accept-external recovery plus watcher-originated
  clean reload, dirty root-conflict coverage, and AI paste insert/quote/
  appendix/replace-document/section-merge/replace-selection coverage, clean
  included-file recompile, dirty included-file conflict handling, and
  restart-style workspace restore coverage for open tabs, active tab, pinned
  state, mode/sidebar, workspace root, recent files, scroll-position restore,
  missing-restored-file warning coverage, tab activation, dirty close
  confirmation, renamed recent cleanup, deleted recently-closed pruning, recent
  folder reopen/prune behavior, and externally moved recently-closed path
  pruning, synchronized editor/preview scrolling, preview heading
  click-to-source, persisted editor word-wrap and line-number settings,
  CodeMirror find/replace, smart list continuation, bracket auto-pairing, and
  command-palette heading, citation, glossary, index, open-document, and
  workspace-file navigation, plus transform engine settings trust/probe
  diagnostics, target-specific export readiness manifest preview, export
  output/manifest path reporting, export success/failure diagnostics, and
  blocked export diagnostics before file write. The current checked-in
  browser harness now passes all 42 Chromium tests locally through
  `pnpm run test:e2e` after switching the e2e script to the project-local
  Playwright browser cache.
- `pnpm run test:desktop-smoke` now checks the built Vite artifact, Tauri
  configuration, package/license metadata, release desktop binary after
  `./node_modules/.bin/tauri build --no-bundle`, and a native command workflow
  smoke that saves, opens, watches, compiles, checks readiness, exports, and
  reveals real local files; the bounded native GUI launch smoke also passes on
  the current macOS host and writes `.tmp/desktop-smoke/launch-report.json`
  with PID, elapsed window, captured output, and `processAlive: true` evidence.
- macOS packaging now has current executable evidence: `./node_modules/.bin/tauri
  build --bundles app` builds `NEditor.app`, and `pnpm run test:desktop-bundle`
  verifies Info.plist metadata, bundle identifier, version, executable, icon,
  copyright, and high-resolution flag while writing
  `.tmp/desktop-bundle/macos-app-report.json`. `pnpm run test:desktop-dmg`
  now classifies this host's sandboxed `hdiutil create`/`hdiejectd` failure and
  writes `.tmp/desktop-bundle/macos-dmg-report.json`, so DMG failure is tracked
  as host-specific instead of an unclassified packaging note.
- Current committed browser workflow tests exist, but local browser execution
  depends on host permissions that allow Chromium launch. A Tauri-driver
  WebDriver harness now exists for supported Windows/Linux hosts; macOS records
  the official unsupported skip because the Tauri WebDriver stack has no
  WKWebView driver there. The supported-platform harness now reaches beyond
  shell smoke by checking dirty native titles, export readiness, and preference
  persistence across a desktop restart.
- Export tests now include package/text assertions plus a representative
  rendered export audit that inspects HTML, PDF object structure, DOCX/PPTX
  package anatomy, custom/core properties, comments/provenance appendices,
  transform artifacts, Markdown bundle manifests, blog/Substack packages,
  LaTeX source, and Google Docs packages.
  `pnpm run test:rendered-exports` also writes reviewable artifacts, a manual
  checklist, and executable viewer/package proof under
  `.tmp/rendered-export-audit`; broader native viewer/platform manual QA remains
  under-proven.
- Optional external transform engine evidence now includes current macOS
  Graphviz/DOT variants, D2, and PlantUML proof via `pnpm run check:engines`
  and `.tmp/external-engines/probe-report.json`; macOS Pikchr and Windows
  optional-engine evidence remain open.
- Accessibility has an initial static guard, but full keyboard/manual audit
  evidence is still missing. Performance now has compiler, repeated export
  loop, repeated edit/cache, export progress reporting, compile-result
  cancellation coverage, preview debounce timing/coalescing coverage, a bounded
  repeated compile/export memory-growth stress test, and native command
  workflow timing evidence in `.tmp/desktop-smoke/native-command-report.json`.
  Browser performance workflow execution still needs a host that permits
  Playwright Chromium launch, and deeper long-running memory profiling remains
  open.

## Next Matrix Work

1. Expand each "Partial" row with item-level checklist entries as the
   corresponding feature area is actively worked.
2. Link each row to exact test names once the fresh verification baseline is
   run.
3. Move rows to "Complete" only after direct evidence exists and the evidence is
   current.
4. Keep this matrix synchronized with `docs/todo.md` and `docs/progress.md`.
