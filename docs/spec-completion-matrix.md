# NEditor Specification Completion Matrix

Updated: 2026-05-27

Status vocabulary:

- Complete: current code and tests provide direct evidence.
- Partial: implementation exists, but scope or proof is incomplete.
- Unverified: implementation appears to exist, but there is no current direct
  workflow, artifact, or platform proof.
- Missing: no current implementation evidence was found.
- Deferred: the specification marks the item as later/non-goal, or the project
  has intentionally deferred it.

This matrix is intentionally conservative. If evidence is indirect, the status
is not "Complete".

GitHub Actions is an active external evidence surface only for the manually
dispatched `NEditor Release Evidence` workflow. Current completion still
requires local command evidence, committed artifacts, rendered/manual QA,
downloaded workflow artifacts, or explicit platform checks that match the
current app version, Git commit, and clean source tree.

## Core Product Scope

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 1 Purpose | Local-first business Markdown workbench | Complete | `README.md` and `docs/user-guide.md` define NEditor as a local-first business Markdown workbench with normal files/folders as the source of truth; `docs/specification.md` sections 1, 3, 25, 29, and 30 preserve the desktop/local-first/non-cloud scope; `src-tauri/Cargo.toml`, `package.json`, `src/App.vue`, `src/stores/documents.ts`, `src-tauri/src/compiler.rs`, and `src-tauri/src/export_commands.rs` implement the Tauri/Vue/Rust workbench, local document store, compiler, and export surfaces; `example_fixture_tests::example_project_fixtures_compile_and_export` proves local-first positioning metadata, Markdown source-of-truth metadata, and `cloudSync: false` survive HTML, DOCX, PPTX, and Markdown bundle outputs for every README-linked business example | Keep purpose/positioning evidence current as new product boundaries, examples, or release channels are added. |
| 2 Source Prompt Extension | Tauri 2, Vue 3, Pinia, Vite, vanilla CSS | Complete | `package.json`; `src/main.ts`; `src/App.vue`; `src-tauri/Cargo.toml` | Keep dependency admission docs current as test dependencies are added. |
| 2 Source Prompt Extension | Side-by-side Markdown editor and live preview | Complete | `src/App.vue` renders the CodeMirror source pane and preview pane in split mode; `tests/frontend-unit.test.ts` proves preview debounce timing stays within the small-document target budget and coalesces rapid edits; `pnpm run test:e2e` passed 64 Chromium workflows locally on 2026-05-26 through the workspace-local Playwright Chromium cache, including source edit to live preview update, large-document edit/preview responsiveness, synchronized editor/preview scrolling, and preview heading click-to-source; `.tmp/e2e-browser/report.json` records structured test totals and workflow flags; `NEDITOR_DESKTOP_SMOKE_LAUNCH=1 pnpm run test:desktop-smoke` writes `.tmp/desktop-smoke/native-ui-report.json` proving the native Tauri webview rendered source and preview surfaces plus the `Rendered preview for Market Entry Report, draft` label | Keep side-by-side proof current when editor/preview layout, CodeMirror integration, or compile debounce behavior changes. |
| 2 Source Prompt Extension | Split, preview-only, focus/source modes | Partial | `src/App.vue` mode controls; `pnpm run test:e2e` passed 64 Chromium workflows locally on 2026-05-26 through the workspace-local Playwright Chromium cache, including split/source/preview/focus/outline/export/review/presentation modes, sidebar routing for outline/export/review/presentation, dedicated outline mode CRUD for H1-H4 document structure, and clean inactive-tab external file refresh after switching modes/tabs; `NEDITOR_DESKTOP_SMOKE_LAUNCH=1 pnpm run test:desktop-smoke` writes `.tmp/desktop-smoke/native-workflow-report.json` with app-authored native Tauri webview proof for split/source/preview/focus/outline/export/review/presentation mode switching, source/preview pane visibility, outline/export/review/presentation sidebar routing, and concrete rendered content for outline structure, HTML export preview, review governance, and presentation outline modes | Broader native platform execution and manual mode visual QA. |
| 2 Source Prompt Extension | Local new/open/save/save-as flows | Partial | `src/stores/documents.ts`; `src-tauri/src/filesystem.rs`; file command tests; `pnpm run check:cli` proves `ned file.md` direct open routing, `ned open`, `ned init` `.neditor` project scaffolding for variables/snippets/local-agent handoffs, `ned new` business-document scaffolding, `ned inspect` no-write document inventory reporting for file and stdin input, `ned validate`/`ned check` no-write export readiness reporting with JSON and CI-oriented exit codes, `ned doctor --workspace . --json` setup reporting with app binary/default-reader, workspace-scaffold, and transform-handler coverage, batch conversion through the local export pipeline, terminal template/snippet/target/handler/completion discovery, workspace-profile-filled snippet rendering through `ned snippets --workspace . --fill-profile --markdown <id>` while preserving unresolved placeholders, and stdin/stdout text export automation for HTML and LaTeX while blocking binary stdout; `pnpm run test:e2e` passed 49 Chromium workflows locally on 2026-05-22 through the system-Chrome fallback, including open, save, save as, duplicate, rename, reveal, revert, stale-save conflict, and recent-file flows; the launched native workflow smoke now writes `.tmp/desktop-smoke/native-workflow-file.md`, creates a new document, reopens the saved file through the real `read_file` IPC path, dirties it, and reverts it before continuing | Native desktop dialog/WebDriver coverage for picker-driven flows. |
| 2 Source Prompt Extension | Window title reflects file and dirty state | Complete | `src/stores/documents.ts` computes `windowTitle`, and `setWindowTitle` in `src/App.vue` applies it to the browser/native document title; Playwright workflow `runs configurable Emacs and Vim-style editor keybinding modes` asserts clean and dirty titles (`Market Entry Report - NEditor` and `* Market Entry Report - NEditor`); static frontend coverage guards the native smoke title assertions; bounded native launch smoke records `* Market Entry Report - NEditor` in `.tmp/desktop-smoke/native-ui-report.json`; app-authored native workflow smoke writes `.tmp/desktop-smoke/native-workflow-report.json` and proves save clears the native dirty title, dirtying an opened real file prefixes the native title with `*`, revert clears the dirty title again, and a real template insertion mutates the native title to `* Market Entry Report - NEditor` | Keep title evidence current when document dirty-state, tab title, save/revert, or native smoke title handling changes. |
| 2 Source Prompt Extension | Toolbar commands and keyboard shortcuts | Partial | `src/App.vue` grouped icon command bar, visible in-app menu bar for File/Edit/View/Writing Tools/Quality/Export/Help, persisted icon/text/both toolbar display setting, persisted per-row toolbar collapse state, a single collapsed-toolbar tray that recovers vertical writing space, command palette, and shortcut handler for save/save-as, new/open/open-folder, search, generic export, direct HTML export, bold/italic, command palette, Docs Live, AI Agent Workspace, Review readiness, Export readiness, and shortcut help; `src-tauri/src/lib.rs` exposes richer native File, Writing Tools, Document Wizards, and Quality menu items; `tests/frontend-unit.test.ts` proves the command bar, app menus, wizard buttons, quality buttons, display controls, collapse/expand controls, shortcut help coverage, keyboard-routed review/export destinations, and native menu wiring; `collapses and restores command toolbars to recover writing space` proves collapsed rows move into one compact tray, hidden toolbar controls disappear, the command bar drops below 45% of its original height, and the document workspace gains more than 40px of vertical space; focused browser workflow proof opens the visible File/Writing Tools/Quality menus, uses menu-routed QA review, and exercises the expanded business-document wizard buttons; `pnpm run test:e2e` passed 64 Chromium workflows locally on 2026-05-26 through the workspace-local Playwright Chromium cache, including search/replace, Markdown formatting commands, multi-cursor commands, collapsible toolbar rows, command-palette heading/citation/glossary/index/include/open-document/workspace navigation, toolbar insertion workflows, outline-mode CRUD, and inactive-tab external refresh; the focused keyboard browser workflow directly exercises app-level shortcuts for CodeMirror search, Review readiness, Export readiness, AI Agent Workspace, Docs Live, and shortcut help; bounded native launch smoke records command labels including New/Open/Save/Templates/Commands in `.tmp/desktop-smoke/native-ui-report.json`; app-authored native workflow smoke proves command-palette opening inside the launched Tauri app and routes native View/Writing Tools menu commands through the launched Tauri webview, proving export-preview, outline, and exports sidebar routing, CodeMirror search opening, TOC/equation/code-fence/table insertion, Templates panel opening, AI Paste modal opening, and guarded native File -> Export -> HTML Export execution | Broader native desktop command execution proof across additional OS hosts and picker-driven/menu accelerators. |
| 2 Source Prompt Extension | Light/dark/system theme and typography | Partial | Settings in `src/App.vue`; persisted preferences in store; Playwright harness asserts theme, preview theme, high contrast, reduced motion, and editor/preview typography application plus reload persistence; app-authored native workflow smoke writes `.tmp/desktop-smoke/native-workflow-report.json` with computed desktop-webview evidence for dark theme, high contrast command borders, reduced-motion transition duration, editor font size, preview theme, preview font size, and preview line height | Broader visual/accessibility verification across additional native platforms and manual theme review. |
| 2 Source Prompt Extension | Cross-platform packaging | Partial | `README.md` packaging note; local Tauri build commands; `pnpm run check:platform-packaging` verifies synchronized npm/Cargo/Tauri version and MIT license metadata, `bundle.targets: "all"`, the packaged `ned` sidecar contract, Markdown file associations, macOS/Windows/Linux icon coverage including Windows tile PNGs, production desktop window dimensions, CSP guardrails, root MIT license linkage, and the explicit `unsigned-local-builds` signing stance while writing `.tmp/desktop-bundle/platform-package-config-report.json`; `pnpm run check:homebrew` verifies the macOS Homebrew cask template, tap/runbook documentation, Tauri app metadata, bundle target coverage, exposed `ned` CLI linkage, and optional final cask/artifact SHA evidence while reporting release blockers until a real SHA-pinned, signed, and notarized macOS artifact is supplied; release readiness now ignores the self-referential `homebrew-release-readiness` blocker from that report while still surfacing the real Homebrew cask, artifact, and signing blockers; `pnpm run check:platform-evidence` writes `.tmp/platform-evidence/report.json`, generates Windows/Linux package and WebDriver evidence templates, accepts valid copied supported-host reports only when they match the current `package.json` version, Git commit, and clean source tree, and fails malformed, stale, or dirty-source supplied evidence; `pnpm run collect:platform-evidence` now converts real supported-host package artifacts plus a passing same-version/same-commit/clean-tree `.tmp/desktop-webdriver/report.json` into the exact external JSON files consumed by `check:platform-evidence`; `pnpm run check:release-signing` writes `.tmp/release-signing/report.json`, generates macOS/Windows/Linux signing evidence templates, accepts valid credentialed signing/notarization proof only when it matches the current app version, Git commit, and clean source tree, and fails malformed, stale, or dirty-source supplied evidence; `pnpm run collect:release-signing` hashes signed artifacts, runs platform verifier commands, and writes validator-ready `signing-evidence.json` on credentialed release hosts; `./node_modules/.bin/tauri build --bundles app` rebuilds `src-tauri/target/release/bundle/macos/NEditor.app` on this macOS host; `pnpm run test:desktop-bundle` verifies the `.app` Info.plist metadata, bundle identifier, version, executable, packaged `ned` CLI sidecar, icon, copyright, and high-resolution flag while writing `.tmp/desktop-bundle/macos-app-report.json`; `pnpm run test:desktop-dmg` classifies this host's sandboxed `hdiutil create`/`hdiejectd` failure and writes `.tmp/desktop-bundle/macos-dmg-report.json`; `pnpm run check:release-readiness` now validates macOS app-bundle report structure against current executable/CLI/icon files, validates Info.plist identity/version/high-DPI fields, accepts the DMG host-limitation report only when it is newer than the app-bundle report and includes hdiutil sandbox classification plus app-bundle fallback proof, and recomputes native smoke/WebDriver fallback freshness against `src-tauri/target/release/neditor` before accepting current-host native proof | Execute package artifact collection on Windows/Linux, run credentialed release signing/notarization collection on release hosts, and publish Homebrew only after final cask SHA/artifact proof is supplied. |
| 3 Product Positioning | Local-first document workbench, not cloud suite | Complete | `README.md` states NEditor is deliberately scoped as a local-first document-file-centered desktop workbench and not a multiplayer/cloud/mobile/web suite; `docs/user-guide.md` documents local-first product boundaries; every README-linked executable example fixture now declares `positioning.model: local-first document-file workbench`, `positioning.sourceOfTruth: Markdown source file`, and `positioning.cloudSync: false`; `example_fixture_tests::example_project_fixtures_compile_and_export` proves that positioning metadata compiles and survives HTML metadata, DOCX/PPTX custom properties, and Markdown bundle metadata across the business example set; local file/snapshot/Git/export design preserves source-file ownership | Keep fixture positioning metadata current as new examples are added. |
| 4 Target Users | Business, technical, research, product users | Complete | Example project front matter declares consultants, technical writers, researchers and analysts, product and engineering teams, executives and managers, students and academics, developers, and teams using AI chat output; `example_fixture_tests::example_project_fixtures_compile_and_export` verifies each persona compiles, every README example link maps to an executable fixture, and audience metadata survives HTML, PDF, DOCX, PPTX, and Markdown bundle exports | Keep new personas represented by executable example fixtures. |

## Carry-Forward MacDown Improvements

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 5.1 External File Refresh | Watch open files | Partial | `src-tauri/src/filesystem_watch.rs`; `src/stores/documents.ts`; native-watch local check; `src-tauri/Cargo.toml` enables the existing `native-watch` feature by default for desktop builds; `WatchFileRequest` now accepts `open_roots`, and `syncFileWatcher` passes every other open saved document path as a root watch in addition to the active root and active include graph; root watch events route back to the matching open document, so clean inactive-tab external edits update that document without disrupting the active editor; `cargo test stable_file_ipc_aliases_open_save_as_and_watch_paths` proves the backend watch command returns the active root, additional open roots, direct includes, and nested includes with root/include roles; stale-save conflict recovery is browser-proven in archived workflow run `26139678118`; clean root-file watcher reload is browser-proven in archived workflow run `26140882880`; clean included-file recompile and dirty included-file conflict flows are browser-proven in archived workflow run `26145509141`; `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "detects external changes when switching back to an inactive clean tab|moves clean watcher roots after save-as path changes|moves watcher roots after closing the active watched tab" --project chromium` passed locally on 2026-05-27 and proves all open saved roots appear in `watchedPaths`, inactive clean root edits reload while the active editor remains unchanged, path-changing saves move the watcher root to the new file, stale old-root events are ignored, closing the active watched tab moves the watcher root to the newly active tab, and clean changes from the active root reload; the launched native workflow smoke mutates a real saved Markdown file outside the store, proves a clean external write reloads through `watchDriver: "native"`, proves the native watcher tracks an included file and recompiles after that include changes, blocks the stale save, records root conflict state, exercises keep-local/save-copy/merge/accept-external recovery branches, and restores the original file | Broader platform execution and more multi-include/native edge cases. |
| 5.1 External File Refresh | Non-destructive conflicts: compare, accept external, keep local, save copy | Partial | Conflict modal in `src/App.vue`; `src/lib/conflict.ts`; `src/lib/workflows.ts`; frontend unit tests cover conflict diff alignment, merge-line helpers, and merge composition ordering including blank lines; `pnpm run test:e2e` passed 49 Chromium workflows locally on 2026-05-22 through the system-Chrome fallback, including stale-save conflict copy/merge/keep-local/accept-external workflows, merge composition add/reorder/remove assertions, and watcher-originated dirty root-file conflict compare visibility; `NEDITOR_DESKTOP_SMOKE_LAUNCH=1 pnpm run test:desktop-smoke` now proves stale-save conflict blocking, rendered native conflict modal controls, local/external merge-base seeding in the launched modal, keep-local plus save, save-copy to `.tmp/desktop-smoke/native-workflow-export.md`, merge-back recovery, accept-external recovery, and final file restoration in the launched Tauri webview against `.tmp/desktop-smoke/native-workflow-file.md` | Native picker/WebDriver coverage for dialog-driven save-copy paths and broader manual UX QA. |
| 5.1 External File Refresh | Watch included files and recompile master docs | Complete | `src/stores/documents.ts` syncs file watching from the active document root plus the compiled include graph, and `src-tauri/src/filesystem_watch.rs` exposes watched root/include paths through the native watch command; `cargo test stable_file_ipc_aliases_open_save_as_and_watch_paths` proves the backend watch request returns the active root, additional open roots, direct includes, and nested includes with root/include roles; `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "recompiles clean master documents|recomputes watched include paths|opens included-file conflicts" --project chromium` passed on 2026-05-28 and proves clean child-file changes re-render the master preview without dirtying the source, include directive edits recompute watched child paths, old child events are ignored after replacement, new child events recompile the master document, and dirty master drafts open an included-file conflict instead of overwriting local edits; References and command-palette include graph navigation are covered by the focused include workflow evidence; launched native workflow smoke proves the Tauri webview watches `.tmp/desktop-smoke/native-workflow-file.include` with `watchDriver: "native"` and recompiles after the included file changes | Keep include watcher proof current when file-watch routing, include graph compilation, or conflict handling changes. |
| 5.2 Build Toolchain Lessons | Pinned local JS/Rust dependencies | Partial | `pnpm-lock.yaml`; `src-tauri/Cargo.lock`; README commands; `pnpm run verify:local -- --list` and `pnpm run verify:local:full -- --list` expose the local quick/release verification baselines now that GitHub Actions is not active, with the quick baseline directly running `node scripts/check-e2e-environment.mjs` and the full baseline directly running `node scripts/run-e2e.mjs`, `pnpm run check:a11y:runtime`, and `pnpm run check:a11y:manual`; `pnpm run check:deps` proves all 39 `package.json` and `src-tauri/Cargo.toml` dependencies are represented in `docs/dependency-admission.md`, the root license contains the MIT terms, and npm/Cargo/Tauri package metadata remains MIT licensed | Extend the same proof into release packaging and optional engine platform evidence. |
| 5.2 Build Toolchain Lessons | Diagnostics for missing external engines | Partial | `transforms/external.rs`; external transform tests; `compiler_reports_missing_external_engine_path_before_embedded_fallback` proves optional external-capable fences emit setup diagnostics when no engine path is configured before using the embedded fallback; Settings now shows per-engine setup status for no configured path/native fallback diagnostics, untrusted configured paths, trusted paths, and disabled external execution; the focused browser transform-engine workflow asserts those states while exercising trust, probe success, missing executable diagnostics, denied trust, and disablement | Cross-platform diagnostic proof beyond the local browser workflow and current-host backend proof. |
| 5.3 Business Export Customization | HTML/PDF/DOCX/PPTX exports plus blog/Substack/LaTeX/Google Docs/EPUB handoffs | Partial | `src-tauri/src/export/*`; export tests; the native File menu now exposes direct `File` -> `Export` commands for HTML and EPUB, and the File toolbar, command palette, Export sidebar, and guided help actions expose direct HTML/EPUB export through `exportDocumentAs("html")` and `exportDocumentAs("epub")`; `export_document_writes_blog_and_substack_publish_packages` proves `blog` and `substack` ZIP packages contain `post.md`, `post.html`, `substack-copy.html`, `post.txt`, publish workflow metadata in `metadata.json`, `rss-item.xml`, `README.md`, and embedded `manifest.json`; `export_document_applies_public_metadata_options_to_publish_packages` proves export-level canonical URL, description, and language options flow into publishing metadata, blog HTML, and RSS output; `export_document_writes_latex_and_google_docs_outputs` proves `latex` writes `.tex` metadata, headings, tables, links, labels, and equations while `google-docs` writes a ZIP with `document.docx`, `document.html`, `document.md`, `document.txt`, import workflow metadata in `metadata.json`, `README.md`, and embedded `manifest.json`; `export_document_writes_epub_package` proves `.epub` output contains the required `mimetype`, `META-INF/container.xml`, `OEBPS/content.opf`, `OEBPS/nav.xhtml`, `OEBPS/document.xhtml`, styles, plain-text fallback, embedded NEditor manifest, and export-option language metadata in OPF/nav/document XHTML; `export_conformance_fixture_maps_business_features` asserts release metadata and legal disclaimer across HTML, PDF, DOCX, PPTX, plain text, and Markdown bundle outputs; `representative_rendered_export_artifacts_are_package_inspectable` proves a board-style fixture through inspectable HTML, PDF object structure, DOCX/PPTX package anatomy, custom/core properties, comments/provenance appendices, transform artifacts, Markdown bundle manifest entries, blog/Substack package anatomy, LaTeX source, Google Docs package anatomy, and EPUB package anatomy; `pnpm run test:rendered-exports` writes a local audit bundle under `.tmp/rendered-export-audit` with HTML, PDF, DOCX, PPTX, Markdown bundle, blog package, Substack package, LaTeX, Google Docs package, EPUB ebook, hashes, manual checklist, `manual-review.html`, `viewer-proof.json` executable HTML/PDF/DOCX/PPTX/package assertions, Poppler `pdfinfo`/`pdftotext` PDF metadata/text proof when available, Poppler `pdftoppm` PDF raster thumbnails under `raster-proof/` when available, Chromium-rendered screenshot/DOM proof under `browser-visual-proof` for the primary HTML export, manual dashboard, and rich-block/option-heavy HTML review cases when a browser launches, generated Office preview dashboards and screenshots under `office-preview` for primary and review-case DOCX/PPTX artifacts, publishing/import workflow metadata checks, macOS `textutil` extraction for primary, nested Google Docs, and review-case DOCX files, macOS Quick Look PDF/DOCX/PPTX thumbnail-or-limitation classification, and a `pdflatex` compile proof when TeX is installed; `pnpm run check:google-docs-import` verifies the rendered Google Docs DOCX/package hashes, current app version, current Git commit, and clean source-tree provenance, writes `.tmp/google-docs-import/report.json`, emits a live import/readback evidence template, accepts valid current-source Google Drive import evidence, and fails malformed, stale, or dirty-source supplied evidence; rendered export and accessibility human sign-off templates now also carry current app version, source commit, and clean source-tree state, and their validators reject stale or dirty-source completed sign-offs; the current audit also writes `automated-visual-review.json` and `visual-review-summary.json` with `automatedVisualReview.status: "automated-reviewed"` on this host after proving complete browser visual screenshots, PDF raster proof, Office preview extraction/screenshots, and mapped primary/review-case target evidence; the same audit now writes `review-cases/rich-blocks` and `review-cases/option-heavy` HTML/PDF/DOCX/PPTX/Markdown-bundle artifacts, records them in `rendered-export-audit-report.json`, and verifies rich Markdown blocks plus option-heavy cover/page/watermark/appendix content in the package proof; live Google Drive `_import_document` attempts reached the connector but remain authorization-bound, most recently returning `token_expired`; `export_option_matrix_is_preserved_across_targets_and_bundle_evidence` proves one explicit option matrix across HTML, PDF, DOCX, PPTX, text, and Markdown bundle evidence; `rich_markdown_blocks_survive_cross_target_exports` proves block quotes, callouts, lists, task lists, code, tables, figures, equations, generated figure/table lists, and cross references across the same target family; `example_project_fixtures_compile_and_export` proves every README example exports with title/audience/feature evidence across HTML, PDF, DOCX, PPTX, and Markdown bundle artifacts; `pnpm run test:e2e` passed 49 Chromium workflows locally on 2026-05-22 through the system-Chrome fallback, including export readiness, success, failure, output path, manifest path, readiness-blocked export before write, and UI selection/export handoff for HTML, blog, Substack, LaTeX, and Google Docs package targets; the focused Chromium workflow `publishes and hands off extended export targets` now proves the Distribution metadata checklist surfaces release/publishing metadata needs and can scaffold owner, releaseTarget, description, tags, and language before blog/Substack/LaTeX/Google Docs/EPUB handoff; the app-authored native workflow smoke writes a real HTML export plus sidecar manifest/output-hash proof from the launched Tauri webview and now replays the guarded native menu event for `File` -> `Export` -> `HTML Export` through the same readiness-backed path; the supported Windows/Linux Tauri WebDriver harness now clicks the desktop `Export HTML` button through a guarded dialog-free smoke path and validates the written HTML artifact plus sidecar manifest/output hash in `.tmp/desktop-webdriver/report.json` | Live Google Docs import readback after refreshed Drive upload authorization, cross-platform native viewer proof, and optional real reviewer sign-off outside the automated current-host visual audit. |
| 5.3 Business Export Customization | Page numbering, headers/footers, logo, brand color, cover, watermark, presets, metadata | Partial | Export option tests; export shared helpers; frontend settings; `export_conformance_fixture_maps_business_features` asserts headers/footers, watermark, package core/custom properties, approval metadata, legal disclaimer, and bundle metadata; `export_option_matrix_is_preserved_across_targets_and_bundle_evidence` asserts cover/page-number omissions, compact preset, watermark, legal disclaimer, and package/body effects across targets; `example_project_fixtures_compile_and_export` proves `targetPersona` audience metadata is carried into HTML cover metadata, Markdown bundle metadata/text, and DOCX/PPTX custom properties; workspace persistence now normalizes named export profiles that capture target, export defaults, citation defaults, and brand defaults; the Export sidebar exposes save/apply/delete profile controls; `saves and reapplies reusable export profiles` proves Chromium UI creation, application, and reload persistence for branded PDF settings; `pnpm run test:rendered-exports` now requires the `review-cases/brand-layout` audit artifacts and verifies cover-logo rendering, inline brand logo source, brand color CSS, compact landscape Letter page layout, rendered header/footer templates, page-number fields, watermark metadata, legal disclaimer text, brand metadata, browser visual proof, Office preview proof, and dashboard links across HTML/PDF/DOCX/PPTX/Markdown-bundle outputs | Final human/native-viewer sign-off and broader cross-host visual review. |
| 5.4 Master Documents And Includes | Include syntaxes, relative resolution, strip child front matter, circular diagnostics | Complete | `src-tauri/src/source_mapping.rs` and `src-tauri/src/front_matter.rs` expand `!include`, `{{include ...}}`, and HTML-comment include directives relative to the current document, strip child front matter, preserve source maps, and populate `include_graph`; `include_expansion_strips_child_front_matter` proves included headings compile into the master document while child front matter is removed and source-map/AST entries point back to the child file; `include_expansion_supports_documented_directive_forms` proves all documented include syntaxes plus export-manifest included-file evidence; `compiler_reports_circular_and_too_deep_includes` proves circular and maximum-depth diagnostics with include graph depth evidence; `compiler_reports_unreadable_include_targets_with_context` proves unreadable include diagnostics carry source file, directive line, suggestion, target, and resolved-path context; `markdown_bundle_keeps_duplicate_include_basenames_distinct` proves bundled single-document export writes flattened compiled Markdown plus `manifest.json`, `include-graph.json`, and collision-safe `include-map.json`; focused browser workflows prove clean master documents re-render when included files change, include directive edits recompute watched child paths and ignore stale child events, and References/command-palette include navigation opens both include directives and child documents; launched native workflow smoke proves the Tauri webview watches an included file and recompiles the master document after that include changes | Keep master-document include syntax, source-map, watcher, and bundle evidence current when include expansion changes. |
| 5.5 Table Of Contents | `[TOC]`, front matter TOC, depth, numbering, export formatting | Partial | Generated sections and export tests; `generated_toc_exports_page_numbers_for_pdf_and_docx` proves front matter TOC depth/numbering plus page-numbered PDF TOC lines and DOCX TOC field output; `pnpm run test:rendered-exports` now requires the `review-cases/toc-page-numbers` audit artifacts and verifies numbered generated TOC links, PDF page-number leader text, native DOCX TOC-field guidance, browser visual proof, Office preview proof, and dashboard links across HTML/PDF/DOCX/PPTX/Markdown-bundle outputs; `pnpm run test:e2e` passed 49 Chromium workflows locally on 2026-05-22, including command-palette insertion of `[TOC]`, generated Table of Contents preview rendering, TOC links for nested headings, and TOC-link navigation back to the source heading | Native workflow proof across additional hosts and final human/native-viewer TOC sign-off. |
| 5.6 Native Tab Organization | Multiple docs, tabs, pinning, recents, restore | Partial | `src/App.vue`; `src/stores/documents.ts`; the Files sidebar now includes a Document Sets manager for assigning the active document to a set, renaming every open document in the active set, removing the active document from its set, normalizing legacy `document_set`/`set` aliases to `documentSet`, regrouping tabs immediately from current front matter text instead of waiting for inactive-document recompilation, and inserting/copying Markdown manifests with set source, timestamp, workspace, active document, open document count, document paths, status, save state, pinned state, and review handoff instructions; tab rows now expose accessible move-left/move-right controls backed by persisted document ordering, and drag-to-tab ordering aligns pinned/document-set grouping before placing the moved tab next to its target; Playwright harness covers document-set grouping, folder grouping, dragging a document into a set, saving generated `documentSet` front matter, renaming the open set, immediate regrouping, manifest insertion, removing a document from the set, closing a group, tab activation, tab reordering with workspace-restore persistence, dirty-close confirmation, stale recent cleanup, workspace restore, scroll restore, and missing restored-file warnings; `NEDITOR_DESKTOP_SMOKE_LAUNCH=1 pnpm run test:desktop-smoke` now writes `.tmp/desktop-smoke/native-workspace-board-one.md`, `.tmp/desktop-smoke/native-workspace-board-two.md`, and `.tmp/desktop-smoke/native-workspace-loose-note.md`, then records `workspaceTabEvidence` proving launched-webview document-set grouping, pinning into the pinned group, assigning a loose tab into `Native Board Pack`, closing the document-set tab group, reopening a recently closed tab, and restoring active/pinned/editor-scroll/preview-scroll state from real files | Native picker-driven workspace flows, native pointer reorder proof, and broader platform proof. |
| 5.6 Native Tab Organization | Split editor panes | Complete | `src/App.vue` exposes a persisted Dual source toggle in the View toolbar and Settings; the source pane renders two synchronized CodeMirror editors for the active Markdown document, with edits in either pane flowing through the same store, preview compile, save, watcher, and export paths; `src/stores/documents.ts` and `src/lib/workspacePersistence.ts` persist and migrate `splitSourcePanes`; frontend unit coverage guards the toggle, synchronized editor wiring, and persistence; `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "syncs split source panes" --project chromium` passed on 2026-05-28 and proves enabling Dual source on a long document, secondary-to-primary editing, primary-to-secondary editing, live preview refresh from both panes, primary editor scroll driving the preview, and secondary scroll isolation; `.tmp/e2e-browser/focused-report.json` records `workflowEvidence.splitSourcePanes: true` for the focused workflow; `NEDITOR_DESKTOP_SMOKE_LAUNCH=1 pnpm run test:desktop-smoke` writes `.tmp/desktop-smoke/native-workflow-report.json` with `splitSourcePaneEvidence` proving two launched-webview CodeMirror panes, secondary-to-primary/live-preview sync, primary-to-secondary/live-preview sync, primary-pane preview scroll sync, and secondary-pane scroll isolation | Keep split-pane proof current when editor mounting, synchronized editing, scroll behavior, or workspace persistence changes. |

## Core Application Requirements

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 6.1 Application Shell | Tauri IPC commands exposed from Rust | Complete | `src-tauri/src/lib.rs`; `docs/ipc-command-coverage.md`; `ipc_command_tests::spec_25_4_ipc_commands_are_registered_and_documented` verifies every spec 25.4 command is registered and every registered Tauri command has a synchronized coverage row with implementation/evidence columns | Keep coverage synchronized as new IPC commands are added. |
| 6.1 Application Shell | Vue SFC block order template/script/style | Complete | `src/App.vue` follows template/script/style order; `pnpm run check:structure` verifies the top-level Vue SFC block order and `<script setup>` contract | Keep the guard current if the primary shell is split into smaller SFCs. |
| 6.2 Primary Layout | Toolbar, status bar, sidebar, editor, preview | Partial | `src/App.vue`; Playwright harness covers outline-sidebar heading navigation to source, editable outline-first document planning through the Outline panel, dedicated outline mode CRUD for chapters/sections/subsections/subsubsections, and toolbar row collapse/restore with measured command-bar height reduction; `src/lib/documentOutline.ts` parses indented, numbered, bulleted, and Markdown-heading outline drafts into Markdown document skeletons with front matter, `[TOC]`, headings, and draft placeholders; `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "outline" --project chromium` passed locally on 2026-05-23 through the workspace-local Playwright Chromium cache; `pnpm run test:e2e` passed 64 Chromium workflows locally on 2026-05-26 through the workspace-local Playwright Chromium cache, including outline mode CRUD plus desktop and narrow viewport proof that command, sidebar, source, preview, and status regions remain rendered, the sidebar stays accessible on narrow screens, and the page avoids horizontal overflow; `.tmp/e2e-browser/report.json` now records structured test totals plus workflow evidence for Docs Live, outline planning, outline CRUD, and export workflows; bounded native launch smoke records command/sidebar/source/preview/status surface presence, `workspace mode-split`, and viewport dimensions in `.tmp/desktop-smoke/native-ui-report.json`; app-authored native workflow smoke now requires clicking an Outline sidebar heading in the launched Tauri webview and proving CodeMirror selection lands on the source heading | Deeper manual visual QA across real desktop window sizes. |
| 6.2 Primary Layout | Export preview, outline, review, presentation outline modes | Partial | Mode controls and sidebars in `src/App.vue`; outline mode renders only H1-H4 document structure with create/read/update/delete controls and hides source/preview panes; export mode renders an export preview summary in the preview pane; Playwright harness covers outline CRUD plus export preview, review, and presentation outline routing; native workflow report validation now requires rendered outline-mode structure evidence alongside export/review/presentation content | Broader cross-platform native execution and product QA for each mode. |
| 6.3 Editor | CodeMirror 6 engine | Complete | CodeMirror dependencies and imports in `src/App.vue` | None beyond ongoing behavior tests. |
| 6.3 Editor | Markdown highlighting, diagnostics gutter, decorations, folding | Partial | CodeMirror markdown/linter/lintGutter/decorations plus `foldGutter`, `codeFolding`, and fold/unfold command-bar and palette commands in `src/App.vue`; Playwright harness covers diagnostics-panel source navigation with precise line/column metadata and current browser proof folds/unfolds Markdown sections through both toolbar controls and command-palette commands; app-authored native workflow smoke now requires rendered fold-gutter evidence, source-ranged CodeMirror lint range/gutter marker evidence from a real broken-image compiler diagnostic, source-range navigation from both preview and Diagnostics sidebar controls, and native fold/unfold visual-state transitions that create then clear CodeMirror fold placeholders | Broader native OS-host proof and deeper manual assistive-technology review. |
| 6.3 Editor | Line numbers, word wrap, spellcheck, find/replace, word count | Partial | Settings and CodeMirror extensions; persisted word-wrap, line-number, and code-folding preferences; command-bar Navigate group exposes Search, previous/next match, Outline, Fold, and Unfold; status bar stats; Playwright harness covers spellcheck/autocapitalize editor attributes, word/character/reading-time status metrics, and fold/unfold command behavior; archived browser workflow run `26154535588` covers line-number and word-wrap persistence plus CodeMirror find/replace; app-authored native workflow smoke now requires launched-webview evidence for word statistics, spellcheck/autocapitalize/role attributes, line numbers, word wrap, search panel opening, and replacement mutation | Broader assistive-technology traversal and OS-host proof. |
| 6.3 Editor | Smart list continuation and auto-pairing | Partial | `src/lib/markdownEditing.ts`; `continueMarkdownList`; `closeBrackets`; Markdown format commands in `src/App.vue`; frontend unit coverage proves bullet, numbered, zero-padded numbered, task-list, uppercase checked task-list, blockquote-list, quoted-numbered-list, nested quoted-numbered-list, tab-indented list, and empty-marker exit behavior; Playwright harness covers list continuation, bracket pairing, quote pairing, bold/italic/inline-code pairing, and code-fence insertion; archived browser workflow run `26154535588` covers basic Markdown list continuation and bracket auto-pairing; app-authored native workflow smoke now requires Markdown list continuation and bracket-pair insertion inside the launched Tauri webview | Cross-platform native host proof and broader Markdown shortcut behavior beyond list/pair primitives. |
| 6.3 Editor | Multi-cursor support | Complete | Explicit CodeMirror `addCursorAbove`, `addCursorBelow`, and `selectNextOccurrence` commands in `src/App.vue`; NEditor also exposes **Select all occurrences** and **Split selection into line cursors** through the visible Edit menu, command palette, and writing toolbar; `src/lib/multiCursor.ts` owns exact-match occurrence selection, current-word fallback, and line-splitting range helpers so selection semantics are direct unit tested; `tests/frontend-unit.test.ts` includes `multi-cursor helpers select repeated terms and split selected lines` plus static command-palette guards for descriptions and "multi cursor"/"multiple cursors" aliases; `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "edits with explicit multi-cursor commands" --project chromium` passed locally on 2026-05-28 and proves command-palette discovery for Add Cursor Above, Add Cursor Below, Select Next Occurrence, Select All Occurrences, and Split Selection Into Line Cursors, then proves simultaneous two-line insertion, all-occurrence replacement, and splitting a full-document selection into one editable range per line; app-authored native workflow smoke records `native workflow edited multiple cursors in editor`, proving launched-Tauri-webview multi-selection editing inserts text on two lines | Keep multi-cursor workflow covered by focused browser/native smoke when editor command surfaces change. |
| 6.3 Editor | Vim/emacs keybindings | Partial | `src/App.vue` exposes a persisted Settings selector for Default, Emacs-style, and Vim-style editor keybindings; Emacs mode uses CodeMirror's `emacsStyleKeymap` plus NEditor's `src/lib/emacsKeybindings.ts` supplemental kill/yank and word-editing keymap for `Ctrl+K`, `Ctrl+Y`, `Ctrl+W`, `Alt+D`, `Alt+Backspace`, `Alt+F`, and `Alt+B`; the global shortcut router now reserves editor-local Emacs `Ctrl+K`/`Ctrl+Y`/`Ctrl+W` chords while focus is in CodeMirror so command-palette shortcuts cannot steal kill/yank/selection-kill editing; `src/lib/vimKeybindings.ts` now owns Vim normal-mode command handling, pending operators, a Vim register, linewise yank/paste ranges, pure word-motion helpers, operator-motion ranges, and line joining; Vim-style mode adds modal insert/normal state with status-bar feedback and normal-mode navigation/edit commands for `h/j/k/l`, `w`, `b`, `e`, `0`, `^`, `$`, `x`, `dd`, `dw`, `de`, `db`, `d0`, `d$`, `cc`, `cw`, `C`, `D`, `J`, `i`, `I`, `a`, `A`, `o`, `O`, `u`, `Ctrl+R`, `g`, `G`, `yy`, `yw`, `ye`, `yb`, `y0`, `y$`, `p`, and `P`, including explicit Vim-style word-start/word-end motion, pending `d`/`c`/`y` operator handling, and linewise paste placement; `src/stores/documents.ts` and `src/lib/workspacePersistence.ts` persist and migrate the keybinding mode; frontend unit coverage guards the setting, persistence, help text, Emacs kill-line/word-range semantics and supplemental wiring, pending Vim operators, word motions, operator-motion ranges, line deletion/change, editor wiring, pure Vim punctuation-aware word helper semantics, operator ranges for dotted and slash-delimited business identifiers, linewise yank ranges, before/after paste positions, and global-shortcut reservation for Emacs editor chords; focused browser workflow `runs configurable Emacs and Vim-style editor keybinding modes` proves Emacs `Ctrl+A`/`Ctrl+E` line editing, Emacs `Ctrl+K` kill-line plus explicit `Ctrl+Y` yank without opening the command palette, Vim insert/normal status transitions, blocked printable keys in normal mode, `0`/`i` line-start insertion, `$`/`a` append editing, `I`/`A` line insertion and append, pending `d` plus `dd` line deletion, `w` word-start motion, `db`, `C`, `J`, `cw`, `yy`, `p`, `P`, `b`, `e`, and reload persistence for Vim mode; `.tmp/e2e-browser/focused-report.json` records `workflowEvidence.editorKeybindingModes: true` for `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "runs configurable Emacs and Vim-style editor keybinding modes" --project chromium`, and the focused workflow now proves punctuation-aware Vim `d` plus `e`, `d` plus `w`, and `w` plus `i` behavior inside `client.name/value next`; `NEDITOR_DESKTOP_SMOKE_LAUNCH=1 pnpm run test:desktop-smoke` now writes `.tmp/desktop-smoke/native-workflow-report.json` with `editorKeybindingEvidence` proving Emacs mode attributes/status plus line editing, Vim mode attributes/status, normal-mode printable key blocking, Vim insert/append editing, and persisted Vim preference reload in the launched Tauri webview | Broader supported-host modal-editing parity across Windows/Linux and additional OS hosts. |
| 6.4 Preview | Live debounced preview | Partial | Store compile on editor update; debounce in `src/App.vue`; Playwright harness covers preview update after source typing; the workbench now records preview update duration, compiled character count, and preview update timestamp for the active document; the status bar exposes `Preview updated in ... ms for ... characters`; the large-document browser workflow proves a 120-section document edit reaches preview under the timing budget, exposes preview timing telemetry, and keeps editor-to-preview scroll sync responsive | Broader native/release-device sustained preview performance proof. |
| 6.4 Preview | Scroll sync and click-to-source | Complete | `src/App.vue` implements editor-to-preview and preview-to-editor scroll synchronization through `syncPreviewScrollFromEditor`, `syncEditorScrollFromPreview`, and ratio-based `syncScrollPosition`; `handlePreviewClick`, `sourceTargetForAnchor`, and `goToSourceTarget` map preview headings, anchors, figures, tables, and equations back to exact Markdown source lines using the compiled document AST/source map; `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "syncs editor and preview scrolling" --project chromium` passed locally on 2026-05-28 and proves editor-to-preview scroll sync, preview-to-editor scroll sync, rendered preview heading click-through to the source line, and direct click-through from anchored figure, table, and equation preview artifacts to their Markdown source lines; `pnpm run test:e2e` passed 64 Chromium workflows locally on 2026-05-26; app-authored native workflow smoke clicks rendered table and equation captions in the launched Tauri webview and proves the CodeMirror selection lands on the mapped Markdown source | Keep focused browser and native smoke coverage current when preview rendering or source-map structures change. |
| 6.4 Preview | Separate preview theme, inline warnings, transform blocks, export preview | Partial | Preview theme setting; source-map-backed preview diagnostic callouts; diagnostics sidebar; transform rendering; transform artifact preview inventory; export preview summary; modes; Playwright harness asserts preview theme/typography attributes and lists `navigates compiler diagnostics to the source range` plus `runs export readiness, success, and failure workflows`, covering preview diagnostic source jumps, export summary state, and transform artifact source jumps; app-authored native workflow smoke now proves preview diagnostic callouts render in the launched Tauri webview and their `Go to source` button selects the source-ranged Markdown diagnostic | Native transform/export-preview visual QA and broader rendered artifact review. |
| 6.5 File Operations | New, open file, open folder, save, save as, revert, rename, duplicate, reveal | Partial | Store actions and Rust file commands; file command tests; `file_command_tests::reveal_command_for_existing_path_is_platform_specific_and_argument_safe` proves reveal path validation and platform-specific file-manager arguments without shell interpolation; `desktop_native_command_workflow_smoke_uses_real_files_and_exports` proves save-as/open/watch/compile/readiness/export/reveal command flow against real local files; Playwright mocked workflows cover open/save/save-as/duplicate/rename/pin/reveal/revert in archived workflow run `26137556147`, and `pnpm run test:e2e` passed 72 Chromium workflows locally on 2026-05-27 after proving Save As/Rename/Pin tabs keep file identity visible beneath semantic document titles; stale-save conflict copy/merge/keep-local/accept-external recovery in archived workflow run `26139678118`; watcher-originated root reload/conflict proof in archived workflow run `26140882880`; included-file watcher conflict proof in archived workflow run `26145509141`; the app-authored native launch smoke now proves the real webview store can save a Markdown file, create a new document, reopen the saved file, dirty it, revert it, keep local conflict edits and save them, save a local conflict copy, merge conflict edits, accept external changes, and restore the on-disk file while the smoke validator checks the saved and copy file content; the supported Windows/Linux WebDriver harness now saves/reopens `.tmp/desktop-webdriver/native-workflow-file.md`, renames to `.tmp/desktop-webdriver/native-workflow-renamed.md`, duplicates to `.tmp/desktop-webdriver/native-workflow-duplicate.md`, and invokes Reveal through the real UI paths under the guarded smoke environment | Execute the expanded WebDriver file-operation harness on supported Windows/Linux hosts and keep truly picker-driven manual coverage open. |
| 6.5 File Operations | Recent docs/folders, workspace restore | Partial | Persisted workspace store; the Files sidebar Document Sets manager now lets users actively manage deliverable packs by assigning, renaming, removing, and generating Markdown manifests for document-set membership while the tab bar regroups from current front matter text; Playwright mocked workflows cover workspace listing after open, document-set/folder grouping, set rename/removal, manifest insertion, and recently closed reopening in archived/current workflows; archived browser workflow run `26147556750` covers restart-style restore for open tabs, active tab, pinned state, mode/sidebar, workspace root, and recent files; archived browser workflow run `26148828614` covers scroll restore and missing-restored-file warning coverage; archived browser workflow run `26151184228` covers tab activation, dirty close confirmation, renamed recent cleanup, and deleted recently-closed pruning coverage; archived browser workflow run `26152255407` covers recent folder reopen/prune behavior and externally moved recently-closed path pruning | Native desktop dialog/recent-folder proof and broader platform coverage. |
| 6.5 File Operations | External change detection/conflict handling | Partial | Watch/conflict code and tests; `pnpm run test:e2e` passed 64 Chromium workflows locally on 2026-05-26 through the workspace-local Playwright Chromium cache, including stale-save conflict blocking, compare, save-copy preservation, merge-back recovery, keep-local, accept-external, clean root reload, inactive clean-tab root reload on activation after external disk change, watcher-originated dirty root conflict, clean included-file recompile, and dirty included-file conflict flows; the app-authored native launch smoke now proves a clean disk-side root-file write reloads the open document through the native watcher, a disk-side included-file write triggers native recompile, a real stale-save conflict opens in the launched webview, the conflict modal renders diff/merge controls and seeds local/external merge bases, blocks overwrite, keeps local edits and saves them over the known external revision, saves a local conflict copy, merges local and external conflict text, accepts external text, and restores the original on-disk document | Broader platform execution and remaining native picker/manual edge cases. |

## Compiler And Document Model

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 7.1 Pipeline | Load, front matter, includes, source map, variables, transforms, formulas, citations, refs, generated sections, semantic model, preview/export | Partial | `src-tauri/src/compiler.rs`; supporting modules; many backend tests | Replace remaining string-heavy paths where correctness depends on semantic structure. |
| 7.2 Outputs | Compiled Markdown, HTML, semantic model, diagnostics, include graph, source map, metadata, bibliography, index, formula graph, transform artifacts, manifest | Complete | `src-tauri/src/compiler_types.rs` defines `CompileResponse` with compiled Markdown, HTML, semantic model, diagnostics, include graph, source map, metadata, bibliography, index terms, formula graph, transform artifacts, paged document, media map, media uses, and export manifest fields; `cargo test --manifest-path src-tauri/Cargo.toml --locked document_ast_models_transform_artifacts_semantically --lib` passed on 2026-05-28 and proves transform artifact structure in the document AST and Markdown bundle exports, including `document-ast.json`, `transform-artifacts.json`, output kinds, source ranges, cache keys, execution kind, and output hashes; `cargo test --manifest-path src-tauri/Cargo.toml --locked front_matter_data_sources_survive_cross_target_exports --lib` proves JSON/YAML/CSV/TSV data-source output persists through HTML, PDF, DOCX, PPTX, Markdown bundle text, and manifest evidence; Markdown bundle tests prove manifest, semantic, metadata, AST, source map, diagnostics, bibliography, formula graph, transform artifacts, media map, per-figure media-use map, and include graph entries; `export_option_matrix_is_preserved_across_targets_and_bundle_evidence` proves bundle `document.txt` and `manifest.json` preserve export option intent; the Diagnostics sidebar exposes a Compiler output inventory for compiled Markdown, HTML preview, semantic model, document AST, paged document, diagnostics, include graph, source map, metadata, bibliography, index terms, formula graph, transform artifacts, media map, figure media uses, and export manifest; `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "navigates compiler diagnostics" --project chromium` passed on 2026-05-28 and proves the inventory renders beside source-ranged diagnostics, surfaces a figure-backed media map plus per-figure media-use counts, and navigates diagnostics to source | Keep compiler output inventory and bundle artifact proof current when `CompileResponse`, Markdown bundle contents, or Diagnostics sidebar consumers change. |
| 7.3 Diagnostics | Severity/message/source/range/suggested fix/related items | Partial | `diagnostics.rs`; diagnostic tests; `transform_diagnostics_resolve_to_source_fence_ranges` proves whole-fence transform diagnostics carry source file, start/end lines, column bounds for the fenced block, suggestions, transform-name related context, and source-range breadcrumbs in both global diagnostics and per-artifact diagnostics; `csv_formula_diagnostics_report_absolute_fence_source_lines` proves row-level CSV/TSV formula diagnostics are translated from transform-body rows to absolute Markdown source lines while retaining the exact bad-row end line; `compiler_reports_malformed_front_matter_data_sources` and `compiler_blocks_data_sources_outside_document_folder` now prove malformed front matter data-source diagnostics include structured related context for source name, path, type, and resolved path where available; `calc_blocks_resolve_forward_refs_and_report_cycles` now proves calc block formula diagnostics carry source file, source line, column/end-column range, formula-name related context, and dependency related context for cyclic named formulas; `compiler_reports_broken_local_markdown_links` now proves broken front-matter logo diagnostics point at the actual `brand.logo` source value range with the root Markdown file as `source_file`, a suggested fix, and resolved logo-target related context instead of pointing at the missing asset path; `compiler_reports_missing_include_without_panicking`, `compiler_reports_circular_and_too_deep_includes`, and `compiler_reports_unreadable_include_targets_with_context` now prove include diagnostics range the exact include target path while preserving severity, source file, source line, suggested fix, include target, canonical/resolved path, and include-graph context; `unknown_transform_attempts_report_source_ranged_diagnostics` proves explicit unsupported transform attempts preserve plain-code output while reporting a warning with source file, exact fence-name column range, suggested fix, transform context, and original fence info, without warning on ordinary code fences such as `python` | Add direct local proof for any remaining diagnostic families that still lack precise ranges, suggested fixes, or related context. |
| 8 Front Matter And Variables | YAML metadata, layout/export controls, variable resolution, filters | Partial | `front_matter.rs`; `variables.rs`; export options; `compiler_reports_malformed_front_matter_with_source_ranges` proves invalid YAML and non-mapping front matter diagnostics include source locations and fixes; `compiler_formats_document_variables_and_reports_bad_filters` proves `default`, `trim`, `upper`, `title`, `currency`, `percent`, and `round` filters plus unsupported/nonnumeric filter diagnostics; `compiler_resolves_literal_dotted_front_matter_keys` proves literal dotted keys such as `client.name` resolve through `{{client.name}}` and metadata lookup while nested maps keep precedence over fallback dotted keys, and sibling literal dotted keys such as `profile.owner` still resolve after nested traversal misses that leaf; `compiler_strips_yaml_tags_from_front_matter_metadata` proves custom YAML tags and tag handles around scalars, inline maps, and `dataSources` lists are treated as decorators before JSON metadata conversion, preserving variable interpolation, numeric filters, rendered data-source tables, and include-graph evidence; `compiler_loads_project_level_variables_without_overriding_front_matter` now proves tagged `.neditor/variables.yaml` wrappers, tagged project values, compact dotted project defaults such as `profile.owner` and `layout.header`, recursive nested default merging, and explicit front-matter override precedence; `formatted_document_variables_survive_cross_target_exports` proves formatted variable output survives HTML, PDF, DOCX, PPTX, and Markdown bundle text; the References sidebar Document variable manager now lists top-level and nested scalar front matter variables with dotted paths, flags empty values, inserts filtered `{{variable | filter}}` placeholders, jumps to source variables, and appends new front matter variables; browser workflow `manages front matter data sources from the references panel` proves variable discovery, nested `account.ownerName`/`account.renewalValue` metadata surfacing, empty-value surfacing, currency/title/upper filter insertion, and UI-created front matter variables | More complex YAML metadata edge cases and native workflow proof. |
| 8 Front Matter And Variables addendum | Front matter manager YAML edge cases | Partial | `src/lib/frontMatterManagers.ts` and `tests/frontend-unit.test.ts` cover CRLF front matter, quote-aware YAML comment stripping, comma-containing inline data-source lists, compact inline `dataSources` block objects plus top-level object/list declarations, scalar path entries inside inline `dataSources` lists and direct scalar `dataSources` declarations, scalar aliases and simple inline/block-map `<<: *defaults` merges inside compact data-source objects, expanded data-source row scalar aliases such as `path: *sourcePath`, top-level `dataSources: *alias` reuse of anchored maps, scalar paths, inline source lists, and block source lists, legacy alias-section scalar, inline-list, block-list aliases, and direct scalar declarations, anchored compact objects reused by later inline-list rows, anchored source maps with nested lists preserved as map defaults, aliased block-list source rows beginning with `<<: *defaults`, expanded `dataSources` rows beginning with `- <<: *defaults`, custom tags and tag-handle forms such as `!docs!sources` ignored on compact data-source objects, inline lists, and alias lists, `yml` alias normalization, blocked URL/Windows/parent traversal paths, safe filenames containing `..`, simple and namespaced scalar anchors/aliases such as `&client.defaults`, `&business/client.defaults`, and `*urn:source/path.default`, dotted metadata keys such as `client.name` plus dotted inline-map keys, nested alias reuse, folded/literal block scalar variables, tagged scalars including `!docs!channel`, typed null/bool scalar normalization, literal `!!str null` preservation, simple `<<: *defaults` merges, list-form merge aliases, direct map aliases such as `client: *clientDefaults`, anchored block-list rows reused as direct map aliases, nested scalar merge defaults as dotted variables, inherited anchored-map defaults, explicit value override of merged defaults, simple inline YAML maps expanded as dotted variables with scalar aliases, nested inline maps, nested inline sequences and matrices, and merge defaults, inline sequences expanded as indexed dotted variables for scalar and object entries, compact objects anchored inside inline sequence items reused as direct map aliases, block sequences inventoried as indexed dotted variables, block-list items starting with `<<: *defaults` merged into indexed dotted variables, and custom tags on inline maps plus block-list rows ignored while scalar fields remain inventoried; compiler coverage also resolves literal dotted metadata keys and strips custom tag wrappers before metadata conversion. | Native workflow proof remains a release risk. |

## Twenty High-Value Business Improvements

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 9.1 Git versioning | Detect repo, branch/dirty, commit, history, diff, restore, tag | Complete | `src-tauri/src/git.rs` implements repo status, history, diff, commit, tag, and restore commands, with ref/path safety helpers in `src-tauri/src/git_support.rs`; `git_history_diff_commit_tag_and_restore_workflow` creates a real temporary Git repository, configures identity, commits a Markdown file, proves `get_git_status` detects `inside_repo`, dirty state, and changed file summary, proves `git_diff` includes the old/new document text, commits the document through `commit_document_changes`, reads history through `git_history`, creates a release tag through `tag_release`, and restores the initial revision through `restore_git_revision`; `git_restore_and_tag_reject_option_shaped_refs` rejects option-shaped tags/revisions and unsupported reflog syntax before invoking Git, and `git_restore_refuses_symlink_targets` refuses symlink restore targets; `src/App.vue` exposes Git status, branch/dirty summary, diff refresh, commit, release tag, Git history, and restore controls in the Versioning panel; the focused browser workflow `runs snapshot restore and release tagging workflows` proves the Versioning panel shows `main | clean`, renders a diff, tags release `v2.0.0`, refreshes the Git status to include `tag v2.0.0`, clears the release-tag input, restores from history revision `001122334455`, and creates a `pre-git-restore` safety snapshot before revision restore; Git-free snapshot guidance and controls are tracked separately by `9.1 Git-free snapshots` | Keep Git workflow and safety-regression tests current when changing versioning commands or UI. |
| 9.1 Git-free snapshots | Local snapshots for non-Git users | Partial | `snapshot.rs`; snapshot UI/tests; focused Chromium workflow `runs snapshot restore and release tagging workflows` now proves manual snapshot creation, visible `manual` snapshot listing, mutation after snapshot, and snapshot restore that removes the post-snapshot mutation while preserving approved metadata; `snapshot_restore_is_scoped_to_active_document_store` and `snapshot_restore_rejects_out_of_scope_and_mismatched_sources` prove restore is limited to Markdown snapshots inside the active document's configured store with matching metadata; the launched native workflow smoke now creates, lists, mutates, and restores both app-data and project-local snapshots, records app-data evidence with label `native-smoke`, records project-local evidence with label `native-project-smoke` and a `.neditor/snapshots` path, and checks restored text plus mutation removal in `.tmp/desktop-smoke/native-workflow-report.json`; the Versioning panel now exposes first-class Git-free guidance with a snapshot-first plan, storage selector, auto-recovery controls, explicit recovery snapshot action, and visible snapshot paths; the browser workflow `guides Git-free users through snapshot-first versioning` proves project-local `.neditor/snapshots` creation from the Git-free path | Cross-platform native proof and broader storage edge cases. |
| 9.2 Export snapshots | Manifest with hashes/options/app/version/status/timestamp | Partial | `manifest.rs`; `export_commands.rs`; export command tests; archived browser workflow run `26159396761` covers manifest path reporting and target-specific readiness manifest preview; manifests now include include graph evidence, an explicit readiness summary with ready/error/warning/info counts, direct-export dirty-Git readiness warnings, pre-write target/extension mismatch blocking, focused proof that Markdown bundle manifests preserve target-specific export options exactly, and `export_document_writes_optional_sidecar_manifest` proves every direct export target writes a sidecar JSON manifest that matches the returned manifest, records the exact output path, and carries the actual output SHA-256; the same direct-export test now proves `includeManifest=false` on HTML suppresses sidecar output but emits a manifest-audit info diagnostic; the launched Tauri webview smoke now writes `.tmp/desktop-smoke/native-workflow-export.html` and `.tmp/desktop-smoke/native-workflow-export.html.manifest.json`, with the manifest recording `export_target: html`, output path, and output hash | Broader rendered/manual manifest audit evidence and cross-platform native export proof. |
| 9.3 Release workflow | Status values, badge, draft export warning, approval metadata, release tagging | Partial | Review UI; titlebar release badge; validation; Git tag command; focused Chromium workflow `runs snapshot restore and release tagging workflows` proves release metadata scaffolding, status change to approved, version entry, approver entry, approval timestamp insertion, release badge update, release audit insertion, saved approved metadata, Git status/diff display, release tag submission, immediate refreshed Git status summary with `tag v2.0.0`, and cleared release-tag input; export manifests now carry first-class release audit fields `approved_by`, `approved_at`, `owner`, and `release_target`; `export_document_writes_optional_sidecar_manifest` proves those fields are written to the sidecar manifest and returned manifest for release-approved HTML export artifacts; focused Chromium export workflow proof now confirms approved PPTX readiness exposes `approved_by` and `release_target` in the manifest preview and renders the release/audit line in the export preview before writing | Native workflow execution across supported hosts and broader release-artifact visual QA. |
| 9.4 AI paste cleanup | Normalize chat output, code fences, bullets, tables, links, citations, insert modes | Complete | `src-tauri/src/ai_cleanup.rs` normalizes chat labels, duplicate headings, list numbering, Markdown tables, rich HTML clipboard text, code fences, citation follow-up markers, quote blocks, appendices, draft/provenance markers, and requested insertion metadata; `ai_cleanup_tests` covers chat artifact cleanup, preview options, list numbering, chat-label removal without changing code fences, duplicate heading removal, conservative CSV-to-table conversion, structure conversion options, rich HTML normalization, common AI `Copy code`/spaced backtick/tilde fence variants, rich `<pre><code>` clipboard conversion, and code-fence content preservation; `tests/frontend-unit.test.ts` includes `AI paste insertion modes preserve workflow-specific output`; focused browser workflows prove modal open/close focus handling, rich HTML clipboard import with guarded **Load clipboard**, stale preview clearing after input changes, cleanup preview, insertion, quote/appendix modes, full-document replacement, current-section merge, selection replacement, citation follow-up preview/insertion, draft marking, and provenance toggle workflows; app-authored native workflow smoke opens AI Paste cleanup from the native Writing Tools menu, proving the native command surface reaches the cleanup modal | Keep cleanup regression tests current when AI Paste modal, cleanup options, or insertion modes change. |
| 9.5 AI provenance | `ai-source`, AI-assisted sections, export appendix | Partial | `provenance.rs`; review/provenance tests; `compiler_collects_tilde_ai_sources_and_ignores_examples` proves semantic AI provenance collection supports backtick/tilde fences and keeps nested provenance examples inert while staying aligned with AST provenance parsing; `tests/frontend-unit.test.ts` proves the Review action helper updates tilde-fenced provenance aliases such as `llm-source` and leaves fenced Markdown examples unchanged, and proves agentic workflow evidence counts top-level tilde-fenced `llm-source` blocks while excluding nested `ai-source` examples and tilde-fenced examples from claim inventory; Review sidebar toggles for `ai-source` and AI-assisted section review state; Playwright harness lists `toggles AI review state and clears provenance readiness warnings`; AI paste governance workflow covers `ai-source` insertion and draft review markers; `prepare_for_export_reports_ai_provenance_audit_metadata` and `prepare_for_export_reports_invalid_ai_review_statuses` prove readiness/manifest diagnostics for missing provenance metadata and invalid AI review statuses; export option tests prove AI provenance appendix inclusion/exclusion across targets; `ai_provenance_appendix_is_audit_readable` proves enabled HTML provenance appendices distinguish `data-kind="source"` records from `data-kind="section"` records, carry `data-status` and section `data-line` audit hooks, render explicit source/section labels, escape prompt summaries, preserve reviewer timestamps with ISO colons, and disappear when `includeProvenance` is false; `review_provenance_tests` proves the shared parser still accepts provenance aliases after fixing inline `key=value` AI-assisted metadata with timestamp colons; `native_workflow_ai_provenance_fixture_exports_reviewed_appendix` proves the native-smoke reviewed AI source/section fixture through semantic collection, export readiness, and HTML/text provenance appendix rendering; the launched native workflow now has AI provenance authoring, Review-sidebar checks, export-readiness checks, exported-HTML appendix inspection, and validator assertions wired into the smoke report | Accepted launched native workflow report on a GUI-capable host remains pending; this host's 2026-05-28 launch attempt survived to timeout but wrote no in-app reports because macOS returned `-10827` and `com.apple.hiservices-xpcservice` connection errors before the WebView smoke code ran. |
| 9.6 Includes | Include graph, diagnostics, re-render, export single doc | Complete | `src-tauri/src/source_mapping.rs` and `src-tauri/src/front_matter.rs` expand include directives, strip child front matter, preserve source maps, and populate `include_graph`; `include_expansion_strips_child_front_matter` proves included headings compile into the master document while child front matter is removed and source-map/AST entries point back to the child file; `include_expansion_supports_documented_directive_forms` proves bang, brace, and HTML-comment include forms plus export-manifest included-file evidence; `compiler_reports_circular_and_too_deep_includes` proves circular and maximum-depth diagnostics with include graph depth evidence; `compiler_reports_unreadable_include_targets_with_context` proves unreadable include diagnostics carry source file, directive line, suggestion, target, and resolved-path context; `markdown_bundle_keeps_duplicate_include_basenames_distinct` proves Markdown bundles write flattened compiled Markdown plus `manifest.json`, `include-graph.json`, and collision-safe `include-map.json` entries for distinct child files; focused browser workflows prove clean master documents re-render when included files change, include directive edits recompute watched child paths and ignore stale child events, and References/command-palette include navigation opens both include directives and child documents; launched native workflow smoke proves the Tauri webview watches an included file and recompiles the master document after that include changes | Keep include compiler, watcher, bundle, and navigation workflows current when include syntax or source-map behavior changes. |
| 9.7 Business table editor | Visual editor, rows/cols, alignment, paste, sort, formats, readable Markdown, export | Partial | `src/lib/tables.ts`; table UI; frontend/backend tests; cursor-aware two-way table editing loads the Markdown table at the current source cursor or selection, shows the source-line range, jumps back to the source table, and applies visual-grid edits over the original Markdown range; the table editor now tracks the loaded Markdown source snapshot, auto-syncs clean visual drafts after direct source-table text edits, keeps the visual draft when the in-text source table is temporarily not parseable, rejects accidental jumps to a later parsed table that no longer overlaps or matches the original table identity, recovers the intended source table by label/caption identity when another Markdown table is inserted above it, and blocks dirty visual drafts from silently overwriting concurrent Markdown source edits until the user reloads or explicitly applies the draft over the current source; the Tables sidebar also exposes an editable Markdown source block for the loaded or newly created table, so users can type pipe-table source with or without outer pipes, preview valid source edits in the visual grid as they type, regenerate canonical source from grid edits, and apply source text into the document, while dirty source-block, invalid in-text source, or grid edits disable/guard table selection, edit-at-cursor, and new-table context switches until the current edit is applied or cancelled; `compiler_accepts_text_edited_tables_without_outer_pipes` proves backend compile/preview accepts directly typed `A | B` / `--- | ---` tables, evaluates formulas, resolves references, and preserves escaped literal pipes after canonicalization; CSV/XLSX export now also honors valid dirty Markdown source text before the visual grid is updated and blocks invalid dirty source text instead of silently exporting the stale visual draft; direct frontend unit coverage now proves source snapshot creation, table source extraction, draft Markdown serialization, source-change detection, deleted-table detection, other-document suppression, new-draft suppression, source-snapshot overlap checks, identity-based moved-table recovery, snapshot-range replacement recovery, outer-pipe-optional text-edited table parsing, and `syncTableDraftFromDocumentText` coverage for visually created tables that are inserted as Markdown, edited directly in document text, resynced into the grid, written back from the grid, and protected while text is temporarily not parseable in `src/lib/tables.ts`; focused Chromium evidence now proves a user can insert a new Markdown table above a loaded source table and still apply source edits back to the intended original table; the source-table workflow is now discoverable from the Tables panel, Writing Tools menu, command palette, toolbar, and native desktop Writing Tools menu; the Tables panel now exposes an explicit **Edit Markdown in text** action that selects the exact Markdown table range, focused Chromium evidence proves replacing that selected pipe-table text in the editor refreshes the visual grid cells, and the Tables sidebar now tracks the current Markdown editor cursor cell so users can see and apply a precise source-cell edit that updates both Markdown text and the visual grid while preserving outer-pipe-free rows, indented pipe rows, escaped literal pipes, and extra author-typed cells; the Tables panel now follows the Markdown source cursor by default through a visible **Follow source cursor** toggle, so selecting or typing a parseable table in document text automatically loads that source table into the visual editor when no dirty table draft would be overwritten, and frontend unit coverage proves the cursor-follow gating plus direct text-cell edits against actual row cell counts for author-added cells; the bounded native desktop smoke now proves native Writing Tools menu commands open the table editor, load the source table at the cursor into a draft containing the Revenue row, and jump back to the exact Markdown source table range; direct frontend unit coverage now proves Markdown source tables can be converted into table-editor drafts, replaced over the original source range, and reparsed with edited values plus escaped pipe cells intact; `table draft sorting preserves summary rows and typed ordering` proves table-editor sort semantics for text, currency, and date columns while retaining summary/formula rows at the bottom; `edited_table_permutation_exports_alignment_escapes_and_formula_rows`, `merged_table_cells_flow_through_semantic_exports`, and `imported_html_table_spans_flow_through_semantic_exports` now prove Markdown bundle/text table exports preserve readable header rows, alignment separators, escaped pipes, formula output rows, and merged-cell span annotations alongside native PDF/DOCX/PPTX table evidence; `pnpm run test:rendered-exports` now generates and validates the `review-cases/edited-tables` rendered audit case with edited-source table text, formula totals, escaped pipe cells, and alignment evidence across HTML, PDF, DOCX, PPTX, and Markdown bundle artifacts plus browser visual proof; focused Chromium evidence now proves the table workflow exports edited Markdown source text to CSV before applying it to the document; `pnpm run check:tables:manual` writes and validates a current-commit manual sign-off contract for source-to-grid, grid-to-source, concurrent edit protection, spreadsheet exchange, rendered export review, keyboard/accessibility, and supported-host table sessions; the visual table write action is now labeled **Apply table** so it is distinct from **Apply source text**; the Tables panel now exposes a dedicated two-way action panel with explicit create-in-text, edit-table-text, sync-text-to-grid, and apply-grid-to-text controls plus a dynamic status hint from direct-tested `buildTableTwoWayState` logic that tells users when text and grid are synced, dirty, invalid, or waiting for conflict resolution; release readiness and the release evidence kit now surface the table editor human-review gap explicitly; `pnpm run test:e2e` passed 49 Chromium workflows locally on 2026-05-22 through the system-Chrome fallback, including table insertion, pasted table import, numeric sorting, formula rows, merged-cell metadata, row/column add-remove behavior, column format totals, cancel-without-applying behavior, and apply-back-to-editor behavior | Completed table-editor human sign-off on supported release hosts and remaining native picker-driven/manual spreadsheet exchange proof. |
| 9.8 Calculations | Calc blocks, inline formulas, table formulas, named values/tables, dependency graph | Partial | `calculations.rs`; table tests; compiler tests; `table_formulas_reference_named_tables` proves named table/range resolution; `named_table_formulas_survive_cross_target_exports` proves resolved named table formulas across HTML, PDF, DOCX, PPTX, Markdown bundle text, and bundle AST; `calc_blocks_resolve_forward_refs_and_report_cycles` and `table_formulas_resolve_forward_refs_and_report_cycles` prove dependency/cycle diagnostics; `src/lib/transformTemplates.ts` adds dozens of business, scientific, and mathematical `calc` templates; the Templates sidebar and command palette insert built-in/custom transform templates; template cards and the custom editor expose detected fill values from literal `calc` inputs and structured transform fields, and AI template assistance guides template choice, fill-value replacement, preview verification, and reviewer handoff; frontend unit coverage verifies template-library breadth, fill-field extraction, transform-template assistance suggestions, and custom template normalization; `pnpm run test:e2e` passed 49 Chromium workflows locally on 2026-05-22, including filtering the built-in Science calc library, previewing and inserting the "Dose by weight" template, inserting a built-in chart transform template, creating/editing/duplicating/deleting a custom `calc` template, persisting the custom library, and inserting a custom template from the command palette; the app-authored native workflow smoke inserts the Science "Dose by weight" calc template into source and records rendered preview evidence; the focused Chromium command/table workflow now inserts a calc block from the command palette, verifies the live preview renders the formula output, and then continues through the same document's visual table creation, totals/formula rows, direct Markdown source edits, source-to-grid sync, edited-source CSV export, and cell-at-cursor text edit | Broader supported-host native template insertion and mixed formula/table proof. |
| 9.9 Equations | Inline/display math, numbering, references, export support | Partial | `rich_blocks.rs`; `compiler_renders_block_and_inline_equations` proves inline/display math, numbering, labels, captions, AST capture, and references; `compiler_renders_broader_latex_equation_syntax` proves sums, superscripts, subscripts, approximation/infinity/arrows, Omega, `bmatrix` rendering, preview/export CSS, AST capture, and DOCX/PPTX text evidence; `compiler_renders_extended_latex_equation_notation` proves optional-index roots, `\text{}` labels, overline/underline/hat/vector wrappers, left/right delimiters, set/logic symbols, ellipses, preview/export CSS, AST capture, and DOCX/PPTX text evidence; `compiler_renders_piecewise_and_styled_latex_equations` proves `cases` piecewise rendering, `\mathbb{}`, `\mathcal{}`, `\mathrm{}`, `\Pr`, `\lim`, preview/export CSS, AST capture, and DOCX/PPTX text fallback evidence; `captioned_equations_survive_cross_target_exports` proves equation captions and references across HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `prepare_for_export_reports_missing_caption_labels` proves readiness/manifest diagnostics for equations missing stable labels or captions; the Insert toolbar and command palette now expose a native Equation Editor dialog with display/inline modes, label/caption controls, LaTeX preview, categorized template search, and 20 business/science/mathematics templates; the focused Chromium business-document workflow proves category/search filtering, loading total-cost and molarity templates, Markdown preview updates, inline-mode switching, and inline equation insertion; `pnpm run test:rendered-exports` now requires `review-cases/equations` and verifies inline math, matrix equations, extended notation, piecewise equations, captions, and equation cross references across HTML, PDF, DOCX, PPTX, Markdown bundle, browser visual proof, Office preview proof, Poppler proof where available, and manual-review links | Remaining work is completed human/native visual sign-off and broader supported-host equation workflow proof. |
| 9.10 TOC | Automatic TOC with marker/front matter | Partial | `generated_sections.rs`; document/export tests; References sidebar Table of Contents manager now inserts `[TOC]`, enables front matter TOC generation, applies `tocDepth`, toggles `tocNumbered`, and summarizes current TOC front matter; `compiler_honors_toc_depth_and_numbering` now proves structured `toc.enabled`, `toc.depth`, and `toc.numbered` metadata generates a numbered depth-limited TOC and feeds the DOCX TOC depth field; `compiler_ignores_generated_section_markers_inside_code_fences` proves generated marker strings inside fenced examples stay literal while real outside markers still render; `pptx_export_can_include_an_agenda_from_structured_toc_metadata` proves structured `toc.enabled` metadata automatically creates a PPTX agenda slide without an explicit `includeAgenda` option; `generated_toc_exports_page_numbers_for_pdf_and_docx` proves numbered front matter TOC links and page-numbered PDF/DOCX export formatting; `pnpm run test:rendered-exports` now requires `review-cases/toc-page-numbers` and proves numbered generated TOC entries, PDF page-number leaders, native DOCX TOC-field guidance, browser screenshots/DOM proof, Office preview dashboards, and manual review links across generated audit artifacts; `pnpm run test:e2e` passed 49 Chromium workflows locally on 2026-05-22, including generated TOC preview rendering and source navigation from TOC links; app-authored native workflow smoke now renders a front matter-driven numbered `[TOC]` with `tocDepth: 2`, proves hidden depth-3 content is excluded, and clicks a TOC preview link back to the source heading; `scripts/check-desktop-smoke.mjs` validates `tocNavigationEvidence` for metadata, rendered link text, depth limiting, and source selection | Final human/native-viewer TOC sign-off and broader native host coverage. |
| 9.11 Index And Glossary | Automatic index/glossary sections and exclusions | Partial | `indexing.rs`; compiler tests; References sidebar Index manager now inserts generated indexes, enables front matter indexes, adds explicit `{#index:Term}` markers, manages `indexExclude` exclusions, and removes stale exclusions; Playwright harness covers command-palette insertion of `[INDEX]`; `compiler_generates_linked_index_with_exclusions_and_proper_terms` proves headings, bold terms, repeated proper nouns, explicit markers, exclusions, and tilde-fenced example suppression; `compiler_generates_index_from_front_matter_without_marker` proves `index.enabled: true` front matter insertion, metadata-defined `index.terms`, linked and unlinked metadata terms, structured `{term, anchor}` metadata terms, exclusions, marker stripping, and fenced-example-safe metadata term anchors; `compiler_generates_glossary_sections_from_marker_and_metadata` now proves structured `glossary.enabled: true` front matter insertion in addition to marker-driven glossary rendering; `compiler_collects_tilde_fenced_glossary_and_bibliography` proves tilde-fenced glossary blocks feed hover terms and generated glossary sections; `compiler_ignores_extension_blocks_inside_fenced_examples` proves nested glossary examples do not leak terms into the document model; `front_matter_index_survives_cross_target_exports` proves front matter generated index content, metadata-defined linked terms, and structured anchored metadata terms across HTML, PDF, DOCX, PPTX, and Markdown bundle artifacts | Native workflow execution and richer index/glossary manager manual QA. |
| 9.12 Bibliography | BibTeX/CSL JSON, citation syntax, rendered bibliography | Partial | `bibliography.rs`; citation tests; Playwright harness covers command-palette insertion of `[BIBLIOGRAPHY]`, resolved reference display, missing-key UI, and duplicate-key UI with source locations; `tests/frontend-unit.test.ts` proves bibliography-manager helpers normalize citation keys, generate locator citations, preserve resolved entry metadata, and deduplicate missing-key BibTeX stubs; the References sidebar now exposes bibliography marker/template insertion, missing-key stub insertion, repeat citation insertion, and resolved-entry copy actions; `citation_export_conformance_covers_required_cases` proves resolved, missing, multi-key, and locator citations render focusable citation-detail popovers in preview/full HTML while DOCX/PPTX citation export remains intact; `prepare_for_export_reports_missing_citation_sources_with_precise_ranges` proves no-bibliography citation readiness ranges and manifest parity; `compiler_reports_duplicate_bibliography_keys` proves duplicate-key source ranges and first-occurrence related context; `compiler_reports_csl_and_hayagriva_duplicate_key_locations` proves CSL JSON and Hayagriva YAML duplicate-key source ranges; `compiler_reports_duplicate_keys_across_external_bibliography_files` proves multiple bibliography front matter paths and duplicate diagnostics across separate external files; `compiler_parses_better_bibtex_edge_cases_without_splitting_at_symbols` proves Better BibTeX-style metadata skipping, parenthesis entries, dotted keys, protected title braces, and `@` characters inside values; `compiler_loads_csl_json_object_variants_with_full_author_dates` proves CSL JSON wrapper objects, single DOI-style keys, full given/family author names, literal authors, raw dates, and string `date-parts`; `compiler_collects_tilde_fenced_glossary_and_bibliography` proves tilde-fenced BibTeX blocks feed citation resolution and generated bibliography output; frontend unit coverage proves deterministic quality recommendations recognize top-level tilde-fenced bibliography sources instead of raising false citation-evidence warnings, and ignore nested bibliography examples as evidence; `compiler_ignores_extension_blocks_inside_fenced_examples` proves nested BibTeX examples do not leak fake references into citation resolution; `compiler_maps_common_csl_style_aliases_to_native_renderers` proves APA/Chicago/Harvard-style aliases render without unsupported-style warnings; `compiler_preserves_common_citation_style_intent_in_bibliographies` proves APA, Chicago author-date, MLA, IEEE, and Vancouver preserve distinct citation labels and generated bibliography formatting instead of collapsing every alias to generic author-year/numeric output; `compile_options_supply_csl_alias_default_citation_style` and `prepare_for_export_validates_brand_and_default_style_options` prove CSL aliases can be export defaults while unknown styles remain blocked; frontend unit coverage now proves persisted bibliography defaults preserve supported aliases such as `APA` -> `apa` and `mla` -> `mla` instead of falling back to title; References and Settings selectors expose common aliases including MLA; `compiler_renders_empty_bibliography_placeholder` proves empty `[BIBLIOGRAPHY]` markers render a visible placeholder while preserving readiness warnings; `compiler_renders_numeric_citations_and_numbered_bibliography` and `compiler_warns_and_falls_back_for_unsupported_csl_style` prove built-in numeric style rendering and unsupported CSL-style warnings | Full CSL processor fidelity beyond deterministic native aliases, native workflow execution, and deeper rendered/manual citation-manager QA. |
| 9.13 Cross references | Figures/tables/equations/headings, broken ref diagnostics, export links | Partial | `references.rs`; citation/export tests; readiness tests now warn when figure/table/equation targets lack stable labels before export; duplicate reference labels are compile errors with source ranges and first-occurrence context, and `prepare_for_export_blocks_duplicate_reference_labels_in_manifest` proves the diagnostic blocks readiness and is copied into manifests; `rich_markdown_blocks_survive_cross_target_exports` proves table, figure, and equation cross references in compiled Markdown, HTML links, PDF text, DOCX text, PPTX slide text, and Markdown bundle text/AST evidence; `heading_appendix_and_decision_references_survive_cross_target_exports` proves section, appendix, and decision reference labels across HTML, PDF, DOCX, PPTX, and Markdown bundle artifacts; References sidebar Cross reference manager now summarizes resolved/missing references, shows a label inventory across headings/captioned objects/labels, jumps to references or labels, and inserts reusable `{@label}` snippets | Native workflow execution and rendered/manual cross-reference manager QA. |
| 9.14 Captions | Figures/table captions, numbering, list support | Partial | AST/export/table/figure tests; References sidebar Captions and Lists manager now inserts `[LIST_OF_FIGURES]` and `[LIST_OF_TABLES]`, summarizes figure/table/equation caption metadata, flags items missing labels or captions, jumps to source, and inserts cross-reference text for labeled items; `compiler_generates_lists_of_figures_and_tables` covers structured `captionLists.figures`/`captionLists.tables` front matter generation, caption numbering, anchor links, fenced-example exclusion, preview HTML, and DOCX artifact text; `rich_markdown_blocks_survive_cross_target_exports` extends figure/table generated-list evidence across HTML, PDF, DOCX, PPTX, and Markdown bundle outputs; `media_export_tests` now prove Markdown bundles include `media-uses.json` with per-figure IDs, captions, alt text, source files, source ranges, bundle paths, hashes, and fit/crop metadata; Playwright harness covers command-palette insertion of both caption-list markers; `prepare_for_export_reports_missing_caption_labels` proves readiness/manifest diagnostics for missing figure/table/equation labels or captions | Native workflow execution and rendered/manual artifact QA. |
| 9.15 Advanced layout | Page size/orientation/margins/columns/breaks/headers/footers/keeps/floats | Partial | `layout.rs`; `paged_document.rs`; export tests; advanced layout directives now support explicit `columnGap`/`column-gap`/`gutter` controls with preset or length values, preserve that spacing in preview CSS, paged-document sections, export manifests, Markdown bundle AST/paged-document JSON, DOCX section column spacing, PDF column reflow, and PPTX/PDF layout summaries; `layout_pagination_controls_flow_through_exports` proves page breaks, section breaks, two-column flow, column gap, page size, orientation, margins, headers/footers, keep-with-next, and keep-together across HTML, DOCX, PPTX, PDF, manifest, and bundle evidence; `compiler_validates_layout_page_metadata` proves unsupported column gaps are warned for front matter and layout directives | Rendered/manual QA and broader overflow hardening. |
| 9.16 Brand templates | Brand name/color/logo/font/header/footer/watermark/legal disclaimer | Partial | Settings UI; export option tests; `prepare_for_export_validates_brand_and_default_style_options` proves readiness rejects malformed brand color/default profile option shapes; `export_conformance_fixture_maps_business_features` asserts watermark, headers/footers, brand package metadata, and legal disclaimer sections across export targets; example fixture proof now keeps audience metadata alongside branded export metadata across HTML, DOCX, PPTX, and Markdown bundles; named export profiles persist brand defaults with target/export/citation defaults, and `saves and reapplies reusable export profiles` proves a branded profile can be saved, reapplied after option drift, and restored after reload in the browser workflow; `compile_options_supply_brand_profile_defaults` now directly proves default brand profile values across HTML, PDF/text metadata, DOCX headers/footers/custom properties, PPTX company/header/footer/legal slides, and Markdown bundle `document.txt` plus `metadata.json`, including brand name/color/logo/font, header/footer templates, watermark, and legal disclaimer; rendered export audit `review-cases/brand-layout` covers the same brand/layout concerns across generated review artifacts | Broader supported-host native viewer and visual sign-off proof. |
| 9.17 Review comments/change notes | Comments, unresolved validation, exports | Partial | `review.rs`; review UI; export conformance tests; `export_conformance_fixture_maps_business_features` asserts release approval metadata and legal disclaimer package evidence; `prepare_for_export_reports_review_change_note_audit_metadata` proves readiness/manifest diagnostics for missing review and change-note audit metadata; browser workflow `manages review comments and change notes from the review panel` proves Review sidebar insertion of unresolved comments and change notes, semantic summary updates, source marker insertion, and comment resolution | Native target fidelity and richer rendered/manual review audit QA. |
| 9.18 Document variables | Front matter/project/data variables | Partial | `variables.rs`; project variable tests; `compiler_formats_document_variables_and_reports_bad_filters` proves document variable defaults, chained text filters, numeric filters, and filter diagnostics; `compiler_handles_document_variable_filter_edge_cases` proves uppercase/lowercase/titlecase aliases, `default=` and `default ` syntax, formatted currency strings, rounded numeric strings, and already-percent-scaled text values; `compiler_loads_project_level_variables_without_overriding_front_matter` proves tagged project-variable YAML wrappers, tagged scalar project values, nested project variables, recursive nested default merging, and front-matter override precedence; `formatted_document_variables_survive_cross_target_exports` proves formatted variables survive the export artifact family; the References sidebar Document variable manager provides first-class UI inventory and insertion for scalar front matter variables and merged project/metadata variables, including filter selection and empty-value state; browser workflow `manages front matter data sources from the references panel` proves `{{budget | currency}}` insertion, `{{projectLead | upper}}` insertion from `.neditor/variables.yaml`-style metadata, and adding `reviewer: "QA Lead"` through the UI | Data-variable UI proof and native workflow proof. |
| 9.19 Pikchr diagrams | Native fallback/external setup and diagnostics | Partial | `transforms/diagram.rs`; `external.rs`; transform tests; `pikchr_native_fallback_handles_semicolon_shapes_and_arrow_labels` proves semicolon-separated native Pikchr statements, connector labels, and box/diamond/cylinder/file shape rendering without requiring an external engine; `visual_data_transforms_survive_cross_target_exports` now carries the same native Pikchr artifact through HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `external_transform_adapters_shape_engine_specific_invocations` proves modern Pikchr stdin execution uses the required `-` marker while preserving the `pikchr-cli` source-file argument shape; `NEDITOR_TEST_PIKCHR=... pnpm run collect:engine-evidence` records a passing local Pikchr SVG smoke artifact and writes accepted `.tmp/external-engines/external/pikchr.json` proof consumed by `pnpm run check:engines` | Cross-platform optional engine proof and deeper full-Pikchr fidelity via installed engines on Windows/Linux. |
| 9.20 Validation | One-click prepare report across metadata/includes/citations/formulas/figures/transforms/settings/links/comments | Complete | `src-tauri/src/validation.rs`, `src-tauri/src/export_commands.rs`, and the Export sidebar implement the one-click **Prepare for export** report; `cargo test --manifest-path src-tauri/Cargo.toml --locked prepare_for_export --lib` passed on 2026-05-28 with 23 readiness tests, proving report/manifest parity for metadata, approval, layout, citations, comments, AI provenance, links/media, formulas, captions, transform settings, progress state, target-specific release metadata blockers, public distribution metadata/options validation, empty appendix/generated-section information, brand/default-style options, dirty-Git warnings, duplicate reference labels, citation-source gaps with precise ranges, caption/label gaps, malformed review/comment metadata, AI provenance audit metadata, and explicit readiness summaries; `cargo test --manifest-path src-tauri/Cargo.toml --locked reference --lib` passed on 2026-05-28 with 13 reference tests, keeping duplicate-label, malformed-marker, readiness-manifest, resolved-reference, citation-example, named-table, topology, trusted-transform, and cross-target reference behavior passing together; `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "runs export readiness\|publishes and hands off extended export targets" --project chromium` passed on 2026-05-28, proving the browser Export sidebar can prepare readiness, block unsafe export before write, expose manifest/readiness diagnostics, scaffold distribution metadata, and route blog, Substack, LaTeX, Google Docs, and EPUB handoff targets. | Keep validation proof current when readiness diagnostics, export targets, reference parsing, or Export sidebar workflow changes. |

## Fenced-Code Transform System

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 10.1 Architecture | Registry, option validation, artifact cache, diagnostics | Complete | `src-tauri/src/transforms/renderer.rs`, `src-tauri/src/transforms/options.rs`, and `src-tauri/src/transforms/external.rs` implement the fenced-code transform pipeline: info-string lookup, alias resolution, option validation, safe embedded renderers/parsers, trusted external execution, source-hash/cache-key artifacts, preview/export artifacts, and diagnostics; transform registry metadata declares aliases and output kinds; `cargo test --manifest-path src-tauri/Cargo.toml --locked transform_tests --lib` passed 54 tests on 2026-05-28, including required first-release registry coverage, alias rendering through canonical artifacts, tilde/backtick fence handling, nested example suppression, diagnostics mapped to source fences, document AST and Markdown bundle artifact structure, output kinds, source ranges, cache keys, execution kind, and output hashes; `cargo test --manifest-path src-tauri/Cargo.toml --locked external_transform_tests --lib` passed 16 tests on 2026-05-28, covering adapter invocation shape, Graphviz alias settings inheritance from `dot`, graph option aliases, trust gating, unconfigured-engine setup diagnostics, disabled-engine fallback, cache diagnostics, persistent cache reuse, same-path executable cache invalidation, no-shell external invocation shaping, timeout handling, and installed-engine conformance when available; `prepare_for_export_validates_transform_engine_options` covers disabled-engine option validation; `src/lib/transformTemplates.ts` and the Templates sidebar provide a reusable built-in/custom template access layer for calc, chart, diagram, data, API, and business transforms, including derived fill-field metadata for template cards and AI template assistance for choice, fill values, preview verification, and handoff; frontend unit coverage proves fill-field detection for tilde-fenced `calc`, structured transform templates, and transform-template assistance, and static UI guards prove source decorations, transform detection, and compact previews use the shared fence opener; `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "manages transform templates" --project chromium` passed on 2026-05-28 and proves built-in/custom transform template filtering, preview, insert, edit, duplicate, delete, persistence, command-palette access, and transform artifact preview routing | Keep transform architecture proof current when registry metadata, option validation, cache keys, diagnostics, external engine execution, or template management changes. |
| 10.2 Safety | No network, no shell, trust, timeout, output limits, source hash cache | Partial | External transform runner and tests; `external_transform_cache_invalidates_when_trusted_executable_changes` proves cache identity changes when a trusted executable is rewritten at the same path, using engine file size/mtime plus adapter/input/source identity; `external_transform_rejects_directory_engine_path_before_spawn` proves configured engine paths must be regular executable files and directories are rejected before process spawn; `compiler_reports_missing_external_engine_path_before_embedded_fallback` proves missing optional engine paths are reported before embedded fallback; `compiler_skips_disabled_external_transform_without_trust_warning` proves disabled external engines are skipped before trust/path execution and fall back to embedded rendering | Cross-platform process behavior proof and broader executable edge cases. |
| 10.3 Core transforms | `calc`, `mermaid`, `pikchr` | Partial | Renderer and transform tests; native Pikchr fallback now covers compact semicolon-separated statements plus common business diagram shapes and connector labels, with cross-target export evidence in `visual_data_transforms_survive_cross_target_exports`; `mermaid_transform_renders_simple_flowchart_svg` and `pikchr_native_fallback_handles_semicolon_shapes_and_arrow_labels` prove the embedded static subsets; `docs/markdown-extensions.md` now documents the first-release Mermaid subset as simple `flowchart`/`graph` edges and labels, the Pikchr subset as simple business shapes/arrows/connector labels, warning behavior for unsupported syntax, and the need for a trusted external workflow when exact Mermaid/Pikchr grammar or layout fidelity is required; the external adapter and engine probe now pass `-` for standard Pikchr stdin mode, and local macOS smoke proof records a rendered `pikchr.svg` artifact | Broader optional-engine proof beyond this macOS host. |
| 10.4.1 DOT/Graphviz | SVG diagrams and engines | Partial | Native fallback; external adapter; transform registry and trusted external execution now cover `dot`, `graphviz`, `circo`, `neato`, `fdp`, `osage`, and `twopi`; `compiler_uses_trusted_graphviz_variant_transform_preferences` proves Graphviz variant preferences route through no-shell external execution; macOS installed-engine conformance now verifies every Graphviz variant listed above | Windows installed-engine proof for every variant and rendered/manual diagram QA. |
| 10.4.2 PlantUML | SVG/PNG enterprise diagrams | Partial | External adapter and fallback; `compiler_uses_plantuml_png_fence_output_format` proves `format=png` fences request a trusted PlantUML PNG sidecar and embed it as a PNG data URL; adapter tests prove `-tsvg` and `-tpng` no-shell invocation shapes | Real PlantUML PNG installed-engine proof and cross-platform proof. |
| 10.4.3 D2 | SVG diagrams | Partial | External adapter and fallback; `d2_native_fallback_handles_labels_attributes_and_semicolons` proves the native SVG fallback handles node labels, edge labels, semicolon-separated edges, and common D2 layout/style attributes without rendering attributes as bogus nodes; `visual_data_transforms_survive_cross_target_exports` now carries a native D2 artifact through HTML, PDF, DOCX, PPTX, and Markdown bundle evidence | Cross-platform proof and deeper D2 grammar fidelity through installed engines. |
| 10.4.4 Vega-Lite | Charts | Partial | Native SVG subset in `visual_data.rs`; `vega_lite_area_mark_renders_static_svg_preview` proves `area` joins `bar`, `line`, and `point` static rendering; `vega_lite_circle_and_square_marks_render_static_scatter_previews` proves common Vega-Lite scatter aliases `circle` and `square` render as static SVG marks with axis titles, color-series metadata, and `encoding.size.field` bubble/symbol sizing metadata; `vega_lite_tick_mark_renders_static_distribution_preview` proves object-form `tick` marks for compact distribution/score previews with axis titles and color-series legends; `vega_lite_text_mark_renders_static_labels_preview` proves `text` marks with `encoding.text.field` labels, x/y placement, axis titles, and color-series legends; the built-in `vega-lite-opportunity-scatter`, `vega-lite-risk-ticks`, and `vega-lite-readiness-labels` transform templates make these plots available from the Templates panel for score/QA workflows; `vega_lite_color_encoding_renders_grouped_series_preview` proves `encoding.color.field` grouped/series rendering with legend and per-series marks; `vega_lite_preview_preserves_negative_values_aggregation_and_axis_titles` proves object-form marks, negative values with a zero baseline, axis titles, `encoding.color.field` series metadata, and y aggregates such as `sum` for business variance charts; `vega_lite_unsupported_marks_report_supported_static_subset` proves unsupported marks report the supported native subset; `visual_data_transforms_survive_cross_target_exports` proves Vega-Lite static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `docs/markdown-extensions.md` documents the native static subset | Broader Vega-Lite grammar support and rendered/manual visual QA. |
| 10.4.5 Chart | Bar/horizontal bar/line/pie/area/KPI | Partial | `chart.rs`; transform tests; `chart_transform_renders_yaml_business_chart_specs` proves YAML bar charts; `chart_transform_renders_pie_area_and_kpi_specs` proves pie, area, and KPI output; `chart_transform_handles_negative_values_targets_and_value_units` proves business variance charts with negative values, real zero baselines, target/goal/benchmark line metadata, and value prefix/suffix/unit labels; `chart_transform_renders_multi_series_business_specs` proves multi-series grouped bar and line charts with legends, `data-series` metadata, and value units for budget-vs-actual and segment comparisons; `chart_transform_renders_horizontal_business_comparisons` proves `horizontal-bar`/`barh` charts for long-label ranked business comparisons with negative values, vertical target lines, unit labels, and grouped multi-series horizontal bars; `visual_data_transforms_survive_cross_target_exports` proves chart static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `src/lib/transformTemplates.ts` includes a horizontal risk-comparison chart starter template | Rendered/manual chart QA and broader chart styling/options. |
| 10.4.6 GeoJSON | Static map preview | Partial | `visual_data.rs`; transform tests; `geojson_transform_preserves_geometry_types_in_static_svg_preview` proves FeatureCollection, Polygon, and MultiPoint render as typed SVG shapes; `geojson_transform_warns_for_projection_assumptions` proves native previews expose `linear-wgs84-fit`/longitude-latitude projection metadata and warn when legacy `crs` metadata or out-of-range projected coordinates are detected; `visual_data_transforms_survive_cross_target_exports` proves GeoJSON static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `docs/markdown-extensions.md` documents the supported native subset and CRS/projection assumptions | Rendered/manual map QA and broader map projection fidelity beyond the native static preview subset. |
| 10.4.7 TopoJSON | Static map preview | Partial | `visual_data.rs`; transform tests; `topojson_transform_resolves_object_arc_references` proves Topology objects resolve polygon arc references, including reversed arcs; `topojson_transform_applies_quantized_point_geometry_transform` proves TopoJSON `Point` and `MultiPoint` object coordinates apply `transform.scale`/`transform.translate` so mixed point/arc layers align in the static SVG preview; `visual_data_transforms_survive_cross_target_exports` proves TopoJSON static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `docs/markdown-extensions.md` documents the supported native subset | Rendered/manual map QA and broader topology edge cases. |
| 10.4.8 STL | Static preview | Partial | `visual_data.rs`; transform tests; `stl_transform_renders_ascii_static_svg_preview` proves ASCII STL projection; `stl_transform_renders_depth_aware_isometric_preview` proves static STL previews now preserve XYZ coordinates through depth-aware isometric projection, sorted triangle drawing, per-triangle `data-depth`, opacity variation, and `z-depth` summary metadata; `visual_data_transforms_survive_cross_target_exports` proves STL static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence | Full 3D orbit/lighting fidelity, binary STL support, and rendered/manual visual QA. |
| 10.4.9 CSV | Tables and formulas | Complete | `src-tauri/src/transforms/renderer.rs` renders `csv` fences through `render_delimited_table`, and `src-tauri/src/tables.rs` provides table parsing, formula evaluation, numeric formatting, diagnostics, and export-safe HTML metadata; `cargo test --manifest-path src-tauri/Cargo.toml --locked csv_and_tsv --lib` passed on 2026-05-28 and proves CSV/TSV formula cells evaluate, CSV numeric columns and cells receive export-safe alignment classes plus `data-format`/`data-value` metadata for plain numbers, currencies, percentages, and negative values while preserving authored text, and ragged CSV rows emit artifact/document diagnostics, generate placeholder headers such as `Column 3`, pad short rows, preserve extra cells, and keep numeric metadata after normalization; `compiler_loads_front_matter_csv_data_sources` proves local CSV front matter data-source loading with formula evaluation and manifest include evidence; `compiler_blocks_data_sources_outside_document_folder` and `compiler_blocks_symlinked_data_sources_outside_document_folder` prove local data-source path boundaries; the References sidebar Local data source manager lists CSV-capable sources, surfaces ready/blocked states, inserts a data-source template, and appends new `dataSources` entries; browser workflow `manages front matter data sources from the references panel` proves CSV rows render in the UI and can be extended without hand-editing YAML | Keep CSV transform proof current when delimited parsing, formula evaluation, table export metadata, or data-source UI changes. |
| 10.4.10 TSV | Tables and formulas | Complete | `src-tauri/src/transforms/renderer.rs` renders `tsv` fences through `render_delimited_table`, and `src-tauri/src/tables.rs` shares the same formula, formatting, diagnostics, and export-safe table pipeline as CSV; `cargo test --manifest-path src-tauri/Cargo.toml --locked csv_and_tsv --lib` passed on 2026-05-28 and proves TSV formula cells evaluate, numeric cells receive export-safe alignment and `data-format`/`data-value` metadata, ragged TSV rows emit artifact/document diagnostics, placeholder headers, padded short rows, preserved extra cells, and numeric metadata after normalization; `compiler_loads_front_matter_json_and_yaml_data_sources` proves the front matter data-source collector accepts TSV aliases alongside local structured file sources; `compiler_blocks_data_sources_outside_document_folder` and `compiler_blocks_symlinked_data_sources_outside_document_folder` prove local data-source path boundaries; the References sidebar Local data source manager exposes a TSV selector and appends TSV `dataSources` entries; browser workflow `manages front matter data sources from the references panel` proves adding `data/targets.tsv` from the UI | Keep TSV transform proof current when delimited parsing, formula evaluation, table export metadata, or data-source UI changes. |
| 10.4.11 JSON | Structured rendering | Complete | `src-tauri/src/transforms/structured.rs` renders `json` fences as structured tables or trees through `render_structured_data_html`; `cargo test --manifest-path src-tauri/Cargo.toml --locked structured_data --lib` passed on 2026-05-28 and proves JSON top-level arrays, `{ "data": [...] }` containers, nested business row objects flattened into stable dot-path columns such as `account.name` and `metrics.revenue`, scalar arrays as readable comma-separated cells, and keyed object maps such as account dictionaries rendered as tables with stable key columns instead of opaque trees; `cargo test --manifest-path src-tauri/Cargo.toml --locked compiler_loads_front_matter_json_and_yaml_data_sources --lib` proves JSON files referenced from front matter render through structured table artifacts, transform artifacts, include graph entries, and export manifest included-file evidence; `cargo test --manifest-path src-tauri/Cargo.toml --locked front_matter_data_sources_survive_cross_target_exports --lib` proves JSON data-source rows survive HTML, PDF, DOCX, PPTX, Markdown bundle text, and manifest evidence; `compiler_blocks_data_sources_outside_document_folder` and `compiler_blocks_symlinked_data_sources_outside_document_folder` prove local data-source path boundaries; the References sidebar Local data source manager lists `jsonFiles` aliases and flags parent-directory JSON paths as blocked before export | Keep JSON structured rendering proof current when table/tree rendering, data-source collection, or References data-source UI changes. |
| 10.4.12 YAML | Structured rendering | Complete | `src-tauri/src/transforms/structured.rs` renders `yaml` fences as structured tables or trees through `render_structured_data_html`; `cargo test --manifest-path src-tauri/Cargo.toml --locked structured_data --lib` passed on 2026-05-28 and proves YAML tree rendering, common nested business row containers such as `records:` rendered as captioned `transform-yaml` tables, nested YAML business rows flattened into dot-path columns such as `customer.tier` and `contract.renewal`, scalar YAML settings maps rendered as readable two-column field tables, and mixed hierarchical documents still rendered as structured trees; `cargo test --manifest-path src-tauri/Cargo.toml --locked compiler_loads_front_matter_json_and_yaml_data_sources --lib` proves YAML/YML files referenced from front matter render through structured tree artifacts, transform artifacts, include graph entries, and export manifest included-file evidence, including export-safe numeric metadata for TSV aliases in the same data-source collector path; `cargo test --manifest-path src-tauri/Cargo.toml --locked front_matter_data_sources_survive_cross_target_exports --lib` proves YAML data-source content survives HTML, PDF, DOCX, PPTX, Markdown bundle text, and manifest evidence; `compiler_blocks_data_sources_outside_document_folder` and `compiler_blocks_symlinked_data_sources_outside_document_folder` prove local data-source path boundaries; the References sidebar Local data source manager supports YAML/YML front matter aliases and provides the same path-safety status surface as CSV/TSV/JSON | Keep YAML structured rendering proof current when table/tree rendering, data-source collection, or References data-source UI changes. |
| 10.4.13 OpenAPI | API docs | Partial | `structured.rs`; `document_ast.rs`; `compiler_renders_openapi_and_json_schema_tables` covers title/version, terms of service, contact, license identifier/URL, top-level external docs, tag descriptions/external docs, top-level servers with server-variable defaults/enums/descriptions, operation-level server overrides, path-level server fallbacks, global security requirements, operation IDs, deprecated operations, operation external docs, operation tags, path and operation parameters, parameter serialization/content/examples, request bodies, response media examples, response headers, links, nested callbacks, webhook path-level and operation parameters, discriminators, security schemes, media types, component schemas, and OpenAPI nullable schema fields rendered as `type | null` plus `nullable: true` constraints; `api_schema_transforms_survive_cross_target_exports` proves OpenAPI transform artifacts, output hashes, source ranges, semantic table caption preservation, metadata presence in native HTML artifacts, security/header/link/example/callback/webhook/discriminator content, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; browser workflow `manages transform templates and inserts reusable workflows` inserts the OpenAPI endpoint template from the Templates sidebar and verifies it appears in the transform artifact preview | Broader OpenAPI edge cases beyond current callback/webhook coverage, rendered/manual QA, and native host execution. |
| 10.4.14 JSON Schema | Schema docs | Partial | `structured.rs`; `document_ast.rs`; `compiler_renders_openapi_and_json_schema_tables` covers title/description, nested field paths, array item paths, required flags, descriptions, refs, enums, formats, defaults, constraints, nullable types, pattern properties, tuple `prefixItems`, draft-07 tuple `items: [...]`, boolean schemas such as `true`, `false`, and boolean `additionalItems`, object `additionalProperties`, dependent required/dependent schemas, `oneOf` composition, conditionals, `$defs`, `propertyNames`, `contains` with `minContains`/`maxContains`, `unevaluatedItems`/`unevaluatedProperties`, and content annotations with `contentEncoding`, `contentMediaType`, and nested `contentSchema`; `api_schema_transforms_survive_cross_target_exports` proves JSON Schema transform artifacts, output hashes, source ranges, semantic table caption preservation, composition/object-keyword/conditional/definition/content-schema content, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; browser workflow `manages transform templates and inserts reusable workflows` inserts the JSON Schema object template from the Templates sidebar and verifies it appears in the transform artifact preview | Broader schema dialect edge cases, rendered/manual QA, and native host execution. |
| 10.4.15 BibTeX | Bibliography rendering | Partial | `business.rs`; `bibliography.rs`; `bibtex_transform_renders_bibliography_preview` proves inline BibTeX title/author/year/date plus entry type, publisher, journal, volume, issue, pages, DOI, URL, edition, ISBN, ISSN, and abstract metadata parsing and transform preview rendering; `bibtex_transform_survives_cross_target_exports_with_metadata` proves BibTeX transform artifacts, output hashes, source ranges, structured bibliography metadata fields, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `compiler_parses_better_bibtex_edge_cases_without_splitting_at_symbols` proves the bibliography service handles Better BibTeX-style metadata records, parenthesis-delimited entries, dotted keys, protected title braces, and `@` characters inside values; `compiler_preserves_scientific_and_medical_csl_alias_intent` proves Nature, AMA, and Elsevier-Vancouver aliases remain distinct supported numeric styles with journal, volume, issue, page, year, and DOI output instead of collapsing to generic Vancouver; frontend unit coverage proves citation-style defaults preserve `nature`, `ama`, and `elsevier-vancouver`; browser workflow `runs command palette citation glossary and index navigation` proves command-palette citation routing, resolved/missing/duplicate citation manager surfacing, and source navigation for BibTeX-backed references | Native workflow execution and deeper rendered/manual citation-manager QA. |
| 10.4.16 Glossary | Definitions and term rendering | Partial | `business.rs`; glossary/index tests; `compiler_generates_glossary_sections_from_marker_and_metadata` proves `[GLOSSARY]` marker replacement, front matter-driven generated glossary insertion, preview hover preservation, and DOCX glossary artifact text; References sidebar includes a Glossary manager for generated glossary insertion, glossary definition templates, export glossary option enablement, term navigation, index-marker insertion, glossary readiness summary, and an insertable glossary audit table for reviewer handoff; Playwright harness lists command-palette insertion plus manager workflows for glossary/index controls and proves glossary audit insertion from detected terms | Native workflow execution and deeper rendered/manual glossary UX QA. |
| 10.4.17 Timeline | Timeline SVG | Partial | `business.rs`; `timeline_transform_renders_static_svg_preview`; `visual_data_transforms_survive_cross_target_exports` proves timeline static SVG artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; browser workflow `manages transform templates and inserts reusable workflows` now inserts the Launch timeline template from the Templates sidebar and verifies it appears in the transform artifact preview with source navigation metadata | Rendered/manual timeline QA and native workflow execution. |
| 10.5 Later transforms | roadmap, ADR, diff, QR, SQL, etc. | Partial | `roadmap`, `adr`, `diff`, and `qr` are first-release native safe transforms; SQL is now a trust-gated SQLite transform that requires a configured `sqlite3` executable, read-only `SELECT`/`WITH` queries, a database path option, and no shell interpolation before rendering query results as tables; document-relative SQL database paths resolve from the active Markdown file's folder and `..` escape attempts are blocked before sqlite3 is invoked; `business_workflow_transforms_render_static_html` proves roadmap/ADR/diff/QR renderer classes, roadmap metadata, ADR key classes, and diff summaries; `safe_business_transforms_survive_cross_target_exports` proves roadmap/ADR/diff/QR artifacts, output hashes, source ranges, HTML, PDF, DOCX, PPTX, and Markdown bundle evidence; `pnpm run test:rendered-exports` now requires `review-cases/business-transforms` and verifies roadmap, ADR, diff, and QR rendered audit artifacts across HTML, PDF, DOCX, PPTX, Markdown bundle, browser visual proof, Office preview proof, Poppler proof where available, artifact hashes, manual-review links, ADR decision text, diff additions, and QR target evidence; `sql_transform_requires_read_only_trusted_queries` proves SQL mutation blocking, stacked `SELECT; DELETE` and `WITH ... SELECT; UPDATE` rejection, quoted-keyword and identifier false-positive avoidance, and trust gating; `sql_transform_runs_trusted_sqlite_query_against_document_relative_database` creates a real SQLite database, configures the installed `sqlite3` executable as trusted, resolves `database="data/revenue.sqlite"` relative to the Markdown file, runs a live read-only query, and renders the result as a `transform-sql` table; `sql_transform_blocks_document_relative_database_escape` proves escaped relative database paths are rejected; browser workflow `manages transform templates and inserts reusable workflows` now inserts roadmap, ADR, and QR built-in templates from the Templates sidebar and verifies each appears in the transform artifact preview | Remaining work is completed human/native visual sign-off and broader supported-host execution for transform workflows. |

## AI, Versioning, Tables, Equations, Citations

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 11 AI workflow | Paste cleanup preview, voice-guided drafting, and governance | Partial | AI modal, store actions, backend tests; AI cleanup now proves chat labels, headings, lists, CSV/TSV tables, rich HTML links/tables, rich HTML code blocks, common AI code fence variants, citation TODO insertion, canonical and aliased provenance blocks/comments, and draft markers; Docs Live adds an intent brief for audience, outcome, owner, deadline, and distribution target plus a structured placeholder manager that preserves names, dates, numbers, sources, reviewers, client values, repeated variables, type/category metadata, evidence/source notes, and review status in the plain-text placeholder block and generated draft inputs table, plus a Writing-toolbar, Outline-panel, command-palette, visible in-app Writing Tools menu, native Writing Tools menu, and toolbar wizard buttons for voice/freeform/context-driven first drafts from outline plus placeholders, generated questionnaire prompts, document-type blueprints including tutorials, proposals, RFP responses, RFQ responses, tender responses, lesson plans, lesson content, technical textbooks, novels, podcast scripts, and movie scripts, technical-textbook and novel workflows that explicitly lock textbook architecture or plot architecture before prose, draft chapters sequentially, and finish with instructional or narrative quality review, an explicit outline-to-section draft workflow, section-by-section Markdown drafting, AI provenance, needs-review section markers, per-section QA gates, humanization cleanup tasks, reviewer handoff questions, insertable/copyable review packets, bounded local draft history with command-palette recovery actions, remove/clear controls, and copy/append actions for generated drafts before replacement-mode apply; the Quality menu, Review sidebar, and Quality toolbar group now expose deterministic quality-improvement recommendations through `src/lib/qualityRecommendations.ts`, scanning compiler diagnostics, placeholders, citation gaps, unresolved comments, AI provenance, missing document identity, weak heading structure, long paragraphs, and generic AI phrasing, then routing to QA review, AI quality assistance with editable triage/evidence/humanization/handoff notes, insertable QA reports, and agentic improvement plans; Agent Workspace now offers command-palette AI plan previews for detected lanes, targets, and missing inputs plus route suggestions that can send natural-language commands directly to Docs Live, AI Paste cleanup, Review governance, Export readiness, Outline mode, or provider handoff, plus searchable and filterable workflow playbooks for board approvals, client proposals, SOPs, strategy memos, policies, release notes, grant applications, technical/LaTeX papers, blog/Substack publishing, and executive revision passes, and generates auditable agent packets for creation, composition, selection-aware revision, review, and distribution with live copy/append packet actions, AI provenance, blockers, QA gates, named reviewer agents for editorial quality, evidence grounding, risk, citations, governance, and export readiness, deterministic current-document evidence scans for placeholders, citation TODOs, claims, humanization findings, review comments, unreviewed AI markers, approval metadata gaps, suspicious links, and cross-reference integrity, an agentic lifecycle task board with lane, status, owner, section, target, evidence-state, and search filters plus owned runnable tasks and reusable Markdown task briefs across creation, composition, editing, revision, review, and distribution, a first-class claim inventory panel with source jumps, insertable/copyable audit tables, and one-click citation TODO creation, a section-by-section work queue with per-section drafting depth, drafting instructions, completion criteria, assigned reviewers, per-section Markdown work-brief insertion, focused Docs Live handoff for individual queued sections, target-specific distribution runbooks, safe apply modes, and an AI Control Center for readiness score, runnable next actions in Agent Workspace and the Review sidebar, source grounding, governance state, and distribution state, plus insertable and copyable release evidence audit packages, an Agent Audit Trail with run ID, deterministic fingerprints, rollback plan, review events, and searchable and filterable persisted local run history with bounded packet snapshots, per-run removal, full-history clearing, plus insertable/copyable filtered audit packages that can be appended, copied, or replanned against current document context; the lifecycle task cap protects every target-specific distribution task and the final release-readiness task from truncation when long outlines, evidence findings, reviewer agents, and many export targets compete for the bounded task board; the guided demo persists onboarding progress, marks steps complete, and inserts or copies a Markdown demo checklist covering AI creation, playbooks, lifecycle tasks, provider governance, outline, composition, templates, review, and export; the Templates panel adds a persisted business identity setup dialog for repeated name, email, phone, role, company, address, website, industry, default client, and brand voice values; common business-development builders for tutorials, proposals, RFPs, RFQs, tenders, statements of work, capability statements, case studies, business cases, and executive briefs now expose context-aware AI step assistance for identity, intent, outline approval, sequential drafting, QA, and humanization; the native RFP response wizard imports Markdown/text, DOCX, PDF, or URL sources, offers AI step assistance for source intake, requirement analysis, buyer intent, response drafting, evidence QA, and handoff with accepted guidance stored in editable response-context notes, surfaces stated and implied buyer intent, extracts requirements, capabilities, timelines, budget hints, evaluation criteria, mandatory attachments, risks, and verification questions, generates requirement-by-requirement compliance rows with suggested response answers, groups those answers into response-section drafts, inserts a compliance matrix, creates a responsive RFP response draft, and hands the analyzed package to Docs Live or Claude Code/Codex/OpenCode/Google Antigravity; reusable semi-template document parts for contact blocks, company overview, executive summaries, scope, pricing assumptions, compliance matrices, tender checklists, tutorial steps, risk registers, and review handoffs; plus provider handoff packages that generate redacted system/user prompts with lifecycle task-board, reviewer-agent, section-queue context, provider-style JSON bodies, headers, cURL starters, local-agent handoff commands, and safety checklists for approved AI providers, Claude Code, Codex, OpenCode, Google Antigravity, localhost gateways, or private-network model gateways without storing secrets in Markdown; credentialed provider execution supports session-only API keys, provider response extraction, response preview, and apply-as-review-material flow that wraps imported provider output in local ai-source and ai-assisted needs-review provenance and records the wrapped review draft in provider-applied run history while the Tauri CSP permits approved HTTPS and local model endpoints; `pnpm run check:ai-provider` emits a live provider endpoint evidence template, validates accepted provider evidence for current app version, current source commit, clean source tree, 2xx response, prompt/response hashes, the `NEDITOR_PROVIDER_EVIDENCE_OK` marker, and absence of API-key-looking secrets; `pnpm run collect:ai-provider` can collect validator-ready evidence from approved OpenAI-compatible, Anthropic-compatible, Gemini-compatible, local HTTP, localhost OpenAI-compatible, or private-network OpenAI-compatible endpoints without storing the API key; Docs Live runtime readiness records secure context, SpeechRecognition, microphone permission, and clipboard read/write capability without storing clipboard content; `pnpm run check:ai-runtime` emits a real-device runtime evidence template and validates current-source proof for secure context, SpeechRecognition availability, granted microphone permission or stream-opened proof without stored audio, and clipboard read/write proof without stored clipboard content; `tests/frontend-unit.test.ts` covers AI runtime readiness success/failure reports, Docs Live structured placeholder extraction/serialization, questionnaire generation, auditable draft output, business identity/template/snippet generation, business wizard step-assistance generation/UI wiring, expanded document-wizard/template metadata, textbook and novel outline-or-plot-first sequencing, RFP requirement/intent/compliance analysis, local-agent handoff profiles including Google Antigravity, agentic plan/run generation, lifecycle task boards and reusable task briefs, lifecycle cap preservation for long multi-target release runs, provider package generation/execution with lifecycle task-board handoff and governed response wrapping, native local-agent handoff support for Google Antigravity under `.neditor/agent-handoffs`, provider-applied history fidelity, control-center state, guided-demo checklist/progress UI, command-palette AI route suggestions, agent claim-inventory actions, direct quality recommendation classification, QA summary/report generation, AI quality step-assistance suggestions, AI transform-template assistance suggestions, static quality UI/actions, and static UI/native-menu wiring; browser workflow `routes natural language command palette instructions to AI workflow surfaces` proves command-palette route suggestions for Docs Live, Review, Export, provider handoff, and direct routing to Export readiness plus provider package generation; browser workflow `turns agent claim inventory findings into citation TODOs` proves claim inventory surfacing for number/commitment claims and citation TODO insertion from a finding; `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "Docs Live" --project chromium` proves the browser workflow from toolbar launch through mocked runtime readiness, mocked Web Speech dictation, generated Markdown application, and rendered review-preparation output; focused browser workflow `builds business documents from saved identity snippets and local-agent handoff` writes `.tmp/e2e-browser/business-document-wizard-report.json` with `workflowEvidence.rfpResponseWizard: true` after proving lesson/textbook/movie wizard access, pasted RFP analysis, stated/implied intent surfacing, requirement verification, full response generation, and compliance matrix rendering; `cargo test --locked rfp_import --lib` proves native DOCX text extraction, DOCX/XML entity decoding, URL script/style/noise stripping, table/list text preservation, numeric entity decoding, page-title extraction, and non-ASCII-safe case-insensitive URL HTML cleanup helpers; `pnpm run test:e2e` passed 64 Chromium workflows locally on 2026-05-26 and `.tmp/e2e-browser/report.json` records schema `neditor.e2e-browser-workflow.v1`, 64 passed tests, zero failures/timeouts, and `workflowEvidence.docsLiveDraft: true`; `NEDITOR_DESKTOP_SMOKE_LAUNCH=1 pnpm run test:desktop-smoke` records native Writing Tools menu proof for Docs Live modal opening, section-draft generation, workflow preview, draft application, Review sidebar routing, and generated Markdown markers in `.tmp/desktop-smoke/native-workflow-report.json` | Live provider endpoint proof with real credentials remains pending until a credentialed host supplies accepted evidence; real-device AI runtime proof remains pending until a microphone/clipboard host supplies accepted evidence; richer rendered/manual QA. |
| 11 AI workflow addendum | Long-form wizard planning gates | Partial | `tests/frontend-unit.test.ts` directly verifies textbook and novel wizard output includes architecture/plot approval gates, planning-artifact tables, sequential chapter draft queues, per-chapter acceptance criteria, final instructional or narrative quality review checklists, and template-level gates before chapters are fleshed out; focused Chromium workflow `keeps long-form Docs Live wizards outline-first before sequential prose` opens Docs Live in technical-textbook and novel modes, verifies generated architecture/plot plans block prose with `needs-input` workflow state and planning-only Markdown, then supplies approved textbook/plot outlines and verifies sequential chapter drafting becomes ready without the prose-blocking warning; focused Chromium workflow `keeps production Docs Live wizards structure-first before script drafting` opens Docs Live in podcast-script and movie-script modes, verifies suggested episode/screen-story architecture blocks prose with `needs-input` workflow state, then supplies approved structures and verifies sequential segment/beat drafting becomes ready without the prose-blocking warning. | Broader rendered/manual QA for complete long-form drafts remains pending. |
| 11.4 Agentic step assistance | Context-aware optimal answers at each workflow step | Complete | `src/lib/agenticWorkflows.ts` now adds `stepAssistance` to every agentic plan, with one suggestion per creation, composition, edit/revision, review, or distribution step. Suggestions use the document intent sheet, context completeness, source-pack counts, reusable document memory, outline variants, revision passes, quality gates, open input questions, selected-text/document state, and target exports, and generated agent packets include an `AI Step Assistance` table plus context signals. The Agent Workspace renders an `AI step-by-step assistance` section and now uses an explicit `Add answer and replan` action that appends the selected suggestion into editable context answers and immediately rebuilds the plan instead of making replanning a separate recovery step. The shared `appendAgenticStepAssistanceContext` helper preserves existing context and appends the suggested answer, rationale, and context signals. Docs Live workflow steps now carry per-stage AI assistance and context signals for outline approval, context capture, sequential drafting, QA, humanization, and review handoff, with the same guidance rendered in the systematic workflow UI and Drafting Plan Markdown table; Docs Live questionnaire suggestions now expose rationale and context signals and append those review notes into editable answers when accepted. The Review sidebar quality workflow now adds AI quality assistance for QA triage, evidence review, humanization, and reviewer handoff, with suggested answers grounded in current findings, document title, export target, document length, and editable review notes. The Templates sidebar now adds AI transform-template assistance for template choice, fill values, preview verification, and reviewer handoff, grounded in template filters, document context, custom-template drafts, and editable transform notes. `tests/frontend-unit.test.ts` verifies the plan/run output, helper behavior, accept-and-replan context flow, Docs Live workflow-stage assistance, generated Markdown evidence, and static UI wiring. Focused Chromium workflow `offers searchable contextual help with workflow actions` now proves the visible Agent Workspace **Add answer and replan** click appends the suggested answer, rationale, and context signals into editable context answers, immediately replans, and preserves downstream packet/provider workflow usability. | Keep browser evidence current as Agent Workspace steps evolve. |
| 12 Versioning | Snapshots, Git integration, version metadata | Partial | Snapshot/Git modules and UI; Versioning panel now groups Git status, diff, history, release tagging, snapshot creation, snapshot listing, and snapshot restore; focused Chromium workflow `runs snapshot restore and release tagging workflows` proves approved release metadata, manual snapshot creation/listing, snapshot restore, Git status/diff display, release tag submission, immediate Git status refresh after tagging, cleared release-tag input, Git-history revision restore, and pre-restore safety snapshot creation; browser workflow `guides Git-free users through snapshot-first versioning` proves non-Git guidance and project-local `.neditor/snapshots` recovery snapshot creation | Native Git execution and broader supported-host proof. |
| 13 Tables/data | Table editing, formulas, data sources | Partial | Table lib/UI/backend tests; frontend unit coverage now proves table line-range matching for source-table editing, source-snapshot overlap checks that prevent a temporarily invalid in-text table from being confused with a later table, same-start in-place document-text edits that change headers, identity-based recovery when another table is inserted above the loaded table, snapshot-range replacement recovery, shared paste/spreadsheet draft construction for headers, fallback captions, alignments, inferred formats, empty data rows, outer-pipe-optional text-edited table parsing, cursor-cell edits that preserve no-outer-pipe rows, indented pipe rows, escaped pipes, and extra author-typed cells, direct author-added cell editing based on actual row cell counts, cursor-follow gating for automatically loading the table under the Markdown source cursor, and table-editor sort semantics for text, currency, and date formats with summary/formula rows retained at the bottom; the Tables sidebar, Writing Tools menu, command palette, toolbar, and native desktop Writing Tools menu expose source-table editing, source jumps, table import/export, and SQL transform insertion so users can reach two-way table workflows from multiple command surfaces; the Tables sidebar now makes the source side explicit with an editable Markdown source block that previews valid typed pipe-table source in the visual grid, round-trips text back into the document, accepts directly typed `A | B` / `--- | ---` table source, follows selected or typed source tables through the visible **Follow source cursor** toggle when no dirty table draft would be overwritten, and dirty source/grid edits or temporarily invalid source-table text guard table context switching so typed table text is not lost by selecting another table, editing at cursor, or starting a new table; `compiler_accepts_text_edited_tables_without_outer_pipes` proves backend compile/preview/export preparation canonicalizes direct text-edited tables without outer pipes before formula evaluation and reference resolution; the explicit **Edit Markdown in text** action selects the loaded table source range and focused Chromium proof now confirms replacing that selected pipe-table text in the editor refreshes the visual grid; the Tables panel now has a two-way editing strip plus **New table in text**/**Insert draft in text** actions so a visual draft can become selected Markdown source immediately; focused Chromium proof now inserts another Markdown table above a loaded table and confirms edits still apply to the intended source table; table export now uses valid dirty editable Markdown source text for CSV/XLSX before applying it to the document and refuses invalid dirty source instead of falling back to stale draft data; bounded native desktop smoke proof now records `nativeMenuCommandEvidence.tableEditor`, `tableSourceEdit`, and `tableSourceJump` for native menu-driven two-way table editing; `spreadsheet_table_import_export_round_trips_csv_and_xlsx` proves native CSV import, XLSX export, XLSX import, and CSV export through `import_spreadsheet_table` and `export_markdown_tables`; `compiler_loads_front_matter_xlsx_data_sources` now proves XLSX front matter data sources and `xlsxFiles` aliases render the first worksheet as Markdown table content while preserving include graph/manifest evidence and cached-formula review diagnostics; `sql_transform_requires_read_only_trusted_queries` proves SQL mutation blocking, stacked statement rejection, quoted-keyword and identifier false-positive avoidance, and trust gating; `sql_transform_runs_trusted_sqlite_query_against_document_relative_database` proves a live trusted SQLite query against a document-relative database path and renders the result as a `transform-sql` table, while `sql_transform_blocks_document_relative_database_escape` proves escaped relative database paths are rejected; `named_table_formulas_survive_cross_target_exports` proves named table/range formulas survive the export artifact family; `compiler_loads_front_matter_json_and_yaml_data_sources` and `front_matter_data_sources_survive_cross_target_exports` prove front matter CSV/TSV/JSON/YAML local data source flows through compile, transform artifacts, manifests, and export targets; `compiler_reports_malformed_front_matter_data_sources` proves missing path, unsupported type, and unreadable source diagnostics; `compiler_blocks_data_sources_outside_document_folder` and `compiler_blocks_symlinked_data_sources_outside_document_folder` prove data source imports stay inside the document folder; the References sidebar Local data source manager now gives UI proof for front matter data-source inventory, path-safety state, template insertion, and CSV/TSV/JSON/YAML/XLSX addition, with browser workflow `manages front matter data sources from the references panel`; `pnpm run check:tables:manual` now provides a release evidence contract for manual table source/grid/spreadsheet/export/supported-host review | Completed table-editor human sign-off on supported release hosts, native workflow proof for picker-driven spreadsheet exchange, and more edge-case validation. |
| 14 Equations | Math authoring/render/export | Partial | Rich block parsing/export tests; `compiler_renders_broader_latex_equation_syntax` proves broader native math syntax including matrices and common operators; `compiler_renders_extended_latex_equation_notation` proves optional-index roots, text spans, accent/line wrappers, delimiters, set/logic symbols, ellipses, CSS, AST, and DOCX/PPTX text evidence; `compiler_renders_piecewise_and_styled_latex_equations` proves piecewise `cases` equations, styled math identifiers, probability/limit operators, CSS, AST, and DOCX/PPTX text evidence; `captioned_equations_survive_cross_target_exports` proves captioned equation references and caption text across HTML, PDF, DOCX, PPTX, and Markdown bundle artifacts; the native Equation Editor inserts inline or display math with stable labels/captions and fillable business/scientific templates from the toolbar or command palette; `pnpm run test:rendered-exports` now requires `review-cases/equations` and verifies inline math, matrix equations, extended notation, piecewise equations, captions, and equation cross references across HTML, PDF, DOCX, PPTX, Markdown bundle, browser visual proof, Office preview proof, Poppler proof where available, and manual-review links | Remaining work is completed human/native visual sign-off and broader supported-host equation workflow proof. |
| 15 Bibliography/citations | BibTeX/CSL/citation rendering | Partial | Bibliography/citation tests; `compiler_parses_better_bibtex_edge_cases_without_splitting_at_symbols` and `compiler_loads_csl_json_object_variants_with_full_author_dates` prove richer BibTeX and CSL JSON import compatibility; `compiler_maps_common_csl_style_aliases_to_native_renderers` proves deterministic CSL alias rendering for common author-year and numeric style families; `compiler_preserves_common_citation_style_intent_in_bibliographies` proves APA, Chicago author-date, MLA, IEEE, and Vancouver now keep distinct generated bibliography formatting and citation labels; `citation_export_conformance_covers_required_cases` proves citation preview popovers carry resolved bibliography titles, locator context, multi-key details, and missing-entry messages in preview/full HTML while DOCX/PPTX export evidence remains intact; frontend unit coverage proves citation-manager repair snippet generation and the References sidebar exposes marker/template/stub/cite/copy actions for bibliography repair; frontend persistence now preserves supported CSL aliases in bibliography defaults and the References/Settings selectors expose common aliases including MLA; duplicate reference label diagnostics block ambiguous anchors; `malformed_reference_markers_are_reported_with_source_ranges` proves malformed labels/cross references reject empty keys, spaces, slash characters, and unclosed markers with source-ranged errors | Full CSL processor fidelity beyond native aliases, native workflow execution, and deeper rendered/manual citation-manager QA. |
| 16 Index/glossary | Index and glossary generation | Partial | Indexing/glossary modules/tests; generated glossary marker and front matter section proof; front matter-generated index proof; export conformance covers glossary appendix output and index artifacts; `prepare_for_export_reports_empty_generated_reference_sections` proves readiness warns when generated index/glossary sections are requested without terms or definitions; References sidebar includes Index and Glossary manager controls for generated section insertion, front matter index enablement, term navigation, glossary-to-index marker creation, term/exclusion summaries, export/glossary state summaries, and insertable glossary/index audit tables; Playwright harness lists the citation/glossary/index workflow with manager assertions and now proves glossary export-state summary changes, glossary audit insertion, index marker/front-matter summary changes, exclusion management, and index audit insertion | Native workflow execution and deeper rendered/manual UX QA. |
| 17 Layout/reflow | Layout model/directives/export mapping | Partial | Layout, paged document, export tests; rich block export tests now include block quotes, callouts, lists, code blocks, tables, figures, equations, and generated sections across target artifacts | Rendered visual QA and overflow/performance work. |
| 18 Export system | Targets, options, manifest | Partial | Export modules/tests and UI; the Export panel now provides AI Export Assistance for target metadata, readiness diagnostics, and artifact evidence with accepted guidance stored in editable export-readiness notes; `export_option_matrix_is_preserved_across_targets_and_bundle_evidence` proves one explicit disabled export option matrix across every target artifact family plus Markdown bundle manifest evidence; `enabled_export_option_matrix_survives_cross_target_artifacts` proves enabled cover, page numbers, styles, syntax highlighting, watermark, layout preset, appendices, PPTX agenda, text output, and Markdown bundle manifest evidence across HTML/PDF/DOCX/PPTX/Markdown bundle artifacts; `pnpm run test:rendered-exports` now adds rendered audit `reviewCases` for rich block content, option-heavy export settings, and edited-table output, with HTML/PDF/DOCX/PPTX/Markdown-bundle artifacts plus package/text proof, generated DOCX/PPTX Office preview dashboards and screenshots, including macOS `textutil` extraction across primary, nested Google Docs, and review-case DOCX files; `prepare_for_export_reports_target_specific_release_metadata_blockers` proves target-aware readiness blocks PPTX, blog, Substack, and Google Docs exports without approved release ownership metadata; `prepare_for_export_reports_target_specific_option_info` proves target-aware option diagnostics for non-PPTX agenda options and Markdown bundle render-only options; `prepare_for_export_reports_markdown_bundle_manifest_sidecar_info` proves target-aware readiness distinguishes the Markdown bundle embedded manifest from optional sidecar manifests; `prepare_for_export_reports_empty_appendix_options_as_info` proves content-sensitive appendix option diagnostics for glossary/comment/provenance exports; `prepare_for_export_validates_brand_and_default_style_options` proves malformed export option shapes are blocked before writing; `prepare_for_export_reports_empty_generated_reference_sections` proves generated reference-section readiness diagnostics are copied into export manifests; `prepare_for_export_blocks_malformed_reference_markers_in_manifest` proves malformed reference marker errors are copied into export manifests; `export_document_blocks_target_extension_mismatches_before_writing` proves direct exports refuse mismatched target/output extensions before writing artifacts or sidecar manifests; `export_document_writes_optional_sidecar_manifest` proves direct HTML/PDF/DOCX/PPTX/Markdown bundle exports return and write matching manifests with exact output paths, hashes, readiness, and progress steps, and now proves Markdown bundles with sidecar manifests disabled still embed `manifest.json`; `rich_markdown_blocks_survive_cross_target_exports` proves common Markdown/business blocks across every export target artifact family; `example_project_fixtures_compile_and_export` proves README-linked business examples carry title, audience, feature markers, package metadata, source hash custom properties, semantic metadata, and bundle evidence across target artifacts | Live cross-platform viewer/import audit, release packaging proof, and remaining artifact-level completion audit. |

## Workspace, Commands, Preferences, Security, Accessibility, Performance

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 19 Workspace/tab groups | Tabs, groups, pinning, recents, restore | Partial | `src/App.vue`; store persistence; Playwright harness covers folder/document-set tab groups, drag-to-document-set front matter updates, close-group behavior, accessible move-left/move-right tab reordering, persisted tab-order restore, pinning, recents, and restore; launched desktop smoke records `workspaceTabEvidence` with real `.tmp/desktop-smoke/native-workspace-*` files proving document-set grouping, pinning, drag-to-set assignment via the production drop handler, close-group behavior, recently closed reopening, and restore of active/pinned/scroll state | Remaining native pointer reorder proof, picker-driven workspace flows, and broader OS-host proof. |
| 20 Command palette | Search commands/headings/citations/glossary/index/documents/workspace files | Partial | `commands` computed in `src/App.vue`; dynamic command entries now attach searchable descriptions/keywords for open documents, document sets, dirty/pinned state, workspace files, include graph edges, heading levels/anchors, citation locators/resolved bibliography titles, glossary definitions, index terms, diagnostics with suggestions/related evidence, configuration setup, and read-aloud commands for selected text, full documents, and stop; Playwright table insertion command test; archived browser workflow run `26154535588` covers heading command navigation; archived browser workflow run `26155535210` covers citation, glossary, and index command navigation; archived browser workflow run `26156393184` covers open-document switching and workspace-file opening; current browser keyboard workflow exercises command-palette result selection plus direct shortcuts for search, AI Agent Workspace, Docs Live, Review readiness, Export readiness, and shortcut help | Non-sandboxed/native execution and broader editor-specific shortcut edge cases. |
| 21 Preferences | Theme, typography, export, Git, AI, transforms, recents | Partial | Settings UI; schema-versioned persisted workspace migration in `src/lib/workspacePersistence.ts`; Settings now exposes a single Configuration Center for overview, appearance/editor, files/history, exports/brand, AI/agents/voice, and transforms; the Transform section includes a managed handler installer that lists every registered external handler group, exposes exact coverage for SQLite, Pikchr, Graphviz/DOT variants, D2, and PlantUML, starts allowlisted Homebrew/winget/Cargo steps where supported, and keeps Linux/admin shell setup copy-only; Settings also exposes non-secret LLM provider defaults plus text-to-speech preferences for browser speech, macOS Say, and Supertonic CLI; model-backed Supertonic usage is consent-gated with an explicit model-download panel that states model name, approximate size, editable storage location, download source, and command before `download_tts_model` or `read_text_aloud` can trigger Supertonic, and focused Chromium workflow `gates Supertonic TTS model downloads from the configuration center` proves download/playback controls stay disabled until acknowledgement, then exercise copy-details, mock model-download, read-aloud, and runtime-inspection paths; the configuration setup wizard routes identity, LLM access, local agents, Docs Live runtime, read-aloud, exports, transforms, and release gates, and each setup area now shows AI setup assistance with a suggested answer, rationale, context signals, and editable setup-note capture; Settings -> Files exposes the `ned` command-line init/open/inspect/convert workflow, `.neditor` scaffold setup, workspace-aware doctor diagnostics, multi-target delivery-pack conversion, no-write document inventory, terminal template/target/handler discovery commands, profile field discovery/single-value retrieval commands for reusable business identity setup, headless `ned rfp-response` / `ned analyze-rfp` procurement analysis examples, shell completion generation, and a default-Markdown-reader setup checkbox with platform commands/manual steps; native TTS inspection checks browser speech delegation, macOS Say availability, and configured Supertonic command availability without launching arbitrary shell commands; `tests/frontend-unit.test.ts` covers migration/schema normalization for legacy aliases, clamped settings, records, transform settings including disabled external engines, AI cleanup defaults, AI provider defaults, TTS preferences, model-download gating UI wiring, setup UI wiring, Configuration Center navigation wiring, CLI guidance for profile field discovery/value retrieval and headless RFP response/matrix generation, and scroll positions; `cargo test --locked tts --lib` covers TTS command shaping, explicit Supertonic model-download acknowledgement, and native runtime inspection; archived browser workflow run `26157446711` covers external transform path changes, input mode, timeout, trust prompt, denied trust reset, successful probe UI, and missing-executable diagnostics | Live audible TTS playback proof, actual model download completion proof, and broader native runtime proof. |
| 22 Security/privacy | Local-first, trust-gated executable transforms, no shell | Partial | `docs/security-threat-model.md`; external transform runner/tests; local file design; Git tag/revision validation and symlink restore guards in `git_support.rs`; snapshot restore store/source validation in `snapshot.rs`; `git_restore_and_tag_reject_option_shaped_refs`; `git_restore_refuses_symlink_targets`; `snapshot_restore_is_scoped_to_active_document_store`; `snapshot_restore_rejects_out_of_scope_and_mismatched_sources`; `compiler_skips_disabled_external_transform_without_trust_warning` proves disabled engines do not execute or emit trust-failure noise; `sql_transform_requires_read_only_trusted_queries` proves trust-gated SQL blocks mutation statements and stacked statement injection while still allowing quoted mutation words and mutation-like identifier names; `prepare_for_export_validates_transform_engine_paths_before_export` proves readiness rejects missing, directory, and non-executable configured engine paths before export while allowing disabled engines to remain ignored; archived browser workflow run `26157446711` covers transform trust prompts, path-change trust clearing, denied trust reset, and missing executable diagnostics; `pnpm run check:security-review` writes a schema-backed independent review report/template, validates returned reviewer metadata, trust-boundary scope, reviewed artifacts, finding counts, report hashes, and release sign-off, and routes accepted evidence through release readiness, evidence kit, and ingest | Completed independent security review evidence and cross-platform process proof. |
| 23 Accessibility | Keyboard, ARIA, contrast, reduced motion | Partial | Some labels/roles/settings in UI; `pnpm run check:a11y` statically checks Vue template button names, delegated button hover/focus help, form-control labels, dialog labeling, dialog focusability/keyboard handling, required skip-link targets, status/progress live regions, diagnostic list labels, conflict diff cell group labels, table editor grid/cell/total/action labels, high-contrast CSS contracts, reduced-motion CSS contracts, and primary editor/preview surface labels while writing `.tmp/accessibility/report.json` with 11 named pass/fail checks; modal close buttons, command-palette search, conflict merge-line controls, merge composition reorder/remove controls, diagnostics, conflict diff rows, table editor controls, CodeMirror editor content, and the rendered preview document now have explicit labels/roles; the workbench now exposes visible-on-focus skip links to commands, workspace, sidebar, source, preview, and status landmarks; the delegated button-help tooltip derives hover/focus help from `data-help`, `title`, `aria-label`, visible text, and disabled-state guidance so dynamically rendered buttons keep contextual help, and the focused Chromium workflow now proves hover help, keyboard focus help handoff, hide-on-nonbutton focus, disabled-button guidance for table actions, stable `button-help-tooltip` identity, and `aria-describedby` relationship creation/cleanup on the active button; AI paste, command palette, and conflict dialogs now receive initial focus, trap Tab at the dialog edge, close on Escape, and restore focus to the invoking control; the status bar now exposes status messages, watch status, compile progress, export progress, and errors as labeled live regions; `pnpm run test:e2e` passed 66 Chromium workflows locally on 2026-05-27 through the workspace-local Playwright Chromium cache, including skip links, modal focus/Escape return paths, status/progress live regions, pending preview compile cancellation/resume, accessible diagnostic/conflict/table labels, high-contrast/reduced-motion computed state, named source/preview surfaces, delegated button-help tooltip semantics, and table editing workflows; the focused browser workflow `supports keyboard-only operation for deep workbench controls` now verifies keyboard activation/focus across document tabs, command-palette result selection, diagnostics source jumps, preview document focus, table-editor cells/actions, stale-save conflict compare, conflict modal Tab wrapping, merge-line add/reorder/remove controls, Escape close, and focus restoration; `pnpm run check:a11y:runtime` now validates and executes seven focused Chromium accessibility workflows for skip links, primary regions across desktop/narrow viewports, modal focus/Escape return, delegated button help, keyboard-only deep controls, status/progress live regions, and editor settings/search/heading commands while writing `.tmp/accessibility/runtime-report.json` with command, attempt, output-tail, and issue evidence; `pnpm run check:a11y:manual` writes `.tmp/accessibility/manual-review-template.json` and `.tmp/accessibility/manual-review-summary.json`, and `NEDITOR_ACCESSIBILITY_SIGNOFF=/path/to/signoff.json` validates reviewer metadata, platform, assistive-technology details, passing checklist notes, zero unresolved blockers, passing static/runtime accessibility prerequisite reports, and matching prerequisite report SHA-256 hashes before accepting a completed manual sign-off | Actual completed screen-reader/native assistive-technology review and broader real-platform accessibility review remain. |
| 24 Performance | Large docs, debounced preview, transform cache, progress | Partial | Debounce/cache/progress code; `performance_tests::compiler_stress_handles_large_documents_with_many_artifacts` stress-compiles nested includes, many tables, formulas, transform artifacts, source maps, and diagnostics; `performance_tests::repeated_export_loop_keeps_large_artifacts_stable` repeatedly renders a large compiled document across HTML, PDF, DOCX, PPTX, and Markdown bundle outputs with structural and artifact-size assertions; `performance_tests::repeated_editing_sessions_reuse_external_transform_cache` repeatedly recompiles edited document text while proving a stable trusted external DOT transform is served from cache after the first run; `performance_tests::repeated_compile_export_cycles_keep_memory_growth_bounded` repeats large compile/export cycles while retaining only summaries and bounding process RSS growth on macOS/Linux hosts; `export_command_tests::export_readiness_and_manifest_report_progress_steps` proves readiness/export manifests carry compile, transform, readiness, render, and manifest progress steps; the export UI now displays readiness and last-export progress steps; `tests/frontend-unit.test.ts` covers latest-document task cancellation so stale/cancelled compile results cannot overwrite newer editor state and covers preview debounce timing/coalescing within the small-document preview target; `pnpm run test:e2e` passed 64 Chromium workflows locally on 2026-05-26 through the workspace-local Playwright Chromium cache, including the pending preview compile cancellation/resume workflow, inactive-tab external refresh, and the large-document edit/preview responsiveness workflow; `pnpm run test:performance-audit` writes `.tmp/performance-audit/report.json` after running the Rust performance stress suite and focused browser large-document workflow, with the focused Playwright evidence isolated at `.tmp/performance-audit/e2e-large-document-report.json`; release readiness validates both the performance report and its linked focused e2e report before accepting the performance gate; `.tmp/desktop-smoke/native-command-report.json` records native command workflow duration evidence; `pnpm run check:performance-profile` emits a release-device native profile template, validates returned 30+ minute Tauri release-device evidence for current app version, current commit, clean source tree, profiler artifact hashes, bounded interaction latency, export success, soak stability, and required startup/large-document/export/file-watch/Agent Workspace scenarios, and is routed through release readiness, evidence kit, and ingest | Supply completed release-device native profile evidence from a real sustained profiling session. |

## Architecture, Storage, Phases, Acceptance

| Spec section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| 25.1 Frontend architecture | Views/stores/components | Partial | App/store exist; extracted local workflow modules now include document outline, markdown editing, workspace persistence, business documents, Docs Live, agentic workflows, AI provider packages, TTS setup in `src/lib/ttsSetup.ts`, configuration setup in `src/lib/configurationSetup.ts`, `src/lib/vimKeybindings.ts`, `src/lib/qualityRecommendations.ts`, `src/lib/releaseReadiness.ts`, `src/lib/exportMetadataChecklist.ts`, `src/lib/frontMatter.ts`, `src/lib/commandPalette.ts`, and table source/draft helpers in `src/lib/tables.ts`; the Vim keybinding extraction removes modal editor command handling and word-motion logic from `src/App.vue` after behavior was locked by focused and full browser workflows, the quality extraction moves deterministic QA/QI scoring plus report Markdown into a direct unit-tested module, the release-readiness extraction moves publish/release checklist scoring, summaries, Help guidance, and audit Markdown into a direct unit-tested module, the export-metadata extraction moves publishing/EPUB distribution preflight checklist logic into a direct unit-tested module, the front matter extraction consolidates exact-key scalar/list reads plus scalar/list upserts for the workbench and checklist modules, the command-palette extraction moves metadata compaction/description/search-text helpers into a direct unit-tested module, the TTS setup extraction moves engine labels, runtime summaries, Supertonic consent-gated model-download plans, copyable download details, acknowledgement prompts, and native read-aloud request values into direct unit-tested helpers, the configuration setup extraction moves setup-step inventory, readiness scoring, context-aware suggested answers, setup-note block formatting, and configuration center section metadata into direct unit-tested helpers, the Docs Live extraction moves review-packet Markdown formatting and audit-inline sanitization into direct unit-tested helpers, and the table extraction moves source-table draft conversion, source-range replacement, draft Markdown serialization, source snapshot creation, source text extraction, source-change detection, editable source-text parsing, default draft creation, paste/spreadsheet text-to-draft construction, table editor accessibility labels/options, table text/grid two-way status and hint state, row/column mutation helpers, summary formula insertion, custom formula-row construction, and merged-cell span preview/apply/clear behavior into direct unit-tested helpers; frontend unit coverage now includes pure Vim word-helper semantics, direct quality recommendation classification/summary/report tests including fenced-example suppression, direct release-readiness missing/approved-state tests including case-insensitive approved/published status handling, direct export metadata checklist tests, direct front matter helper tests, direct command-palette metadata/search tests, direct TTS setup and Supertonic model-download consent tests, direct configuration setup readiness/assistance tests, direct Docs Live review-packet Markdown tests, direct table source round-trip, source-sync snapshot, editable source normalization, default table scaffold, row-to-draft tests, paste/import draft helper tests, table accessibility label helper tests, table two-way state tests, table mutation/formula helper tests, and table span helper tests, and static wiring guards | Continue modularizing oversized frontend surfaces after workflow tests, especially large Agent Workspace, Docs Live, export, table editing, and reference-manager UI blocks. |
| 25.1.1 Tauri contract | File ops, compile, export, snapshots, Git, transform commands | Partial | `src-tauri/src/lib.rs`; `docs/ipc-command-coverage.md`; store invokes commands; `ipc_command_tests::spec_25_4_ipc_commands_are_registered_and_documented` covers the spec IPC subset and the full registered Tauri command surface; the coverage table now records implementation/evidence rows for file, watcher, workspace, compile, export, snapshot, Git, transform, and AI cleanup commands; `git_history_diff_commit_tag_and_restore_workflow` directly exercises `get_git_status`, diff, commit, history, tag, and restore | Broader UI workflow proof for every extended command path. |
| 25.2 State stores | Documents/workspace/editor/preview/compiler/diagnostics/exports/preferences/versioning/transforms/bibliography/AI | Partial | Mostly centralized in `documents.ts`; schema-versioned persistence and normalization live in `src/lib/workspacePersistence.ts`, async compile guards live in `src/lib/asyncGuards.ts`, recent-file/recent-folder/recently-closed list ordering now uses direct unit-tested helpers in `src/lib/recentItems.ts`, and file-watch path normalization/role-map construction lives in direct unit-tested helpers in `src/lib/watchPaths.ts`; frontend unit coverage proves recent item deduplication, limit enforcement, whitespace trimming, forget behavior, Windows/POSIX watch-path normalization, path equivalence, and role lookup key construction | Split additional store domains by responsibility after behavior is locked. |
| 25.3 Rust backend | Filesystem, watcher, compiler, transform, export, Git, snapshot, diagnostics, external runner | Partial | Rust modules exist; full registered IPC command coverage audit is executable via `ipc_command_tests::spec_25_4_ipc_commands_are_registered_and_documented`; command rows map each registered native command to implementation and current direct or registration evidence; `rfp_import::tests::import_rfp_source_accepts_pasted_markdown_aliases` and `rfp_import::tests::import_rfp_source_rejects_unsupported_source_types` directly prove the registered RFP import command path accepts text/Markdown aliases, trims metadata, normalizes pasted source text, and rejects unsupported source types; `local_agents::tests::prepares_handoff_file_inside_workspace`, `local_agents::tests::document_workspace_paths_resolve_to_their_parent_folder`, and `local_agents::tests::rejects_empty_local_agent_handoff_prompts` directly prove the local-agent handoff command writes governed handoff Markdown under `.neditor/agent-handoffs`, resolves document paths to their workspace folder, and rejects empty prompts before file output; `filesystem_watch::tests::stop_file_watcher_clears_active_watcher_state` proves watcher shutdown releases active native-watch state, and `file_command_tests::reveal_command_for_existing_path_is_platform_specific_and_argument_safe` proves reveal-command shaping before OS launch | Continue modularization and direct behavior coverage for remaining commands that still rely mostly on registration or UI-smoke evidence. |
| 25.4 IPC commands | Required command list | Complete | `docs/ipc-command-coverage.md`; `src-tauri/src/lib.rs`; `ipc_command_tests::spec_25_4_ipc_commands_are_registered_and_documented` parses the spec, coverage table, and Tauri handler registration, proves all spec-required commands are registered/documented, and proves the table exactly matches every registered command | Keep the table and guardrail test current if the spec command list changes. |
| 26 Data storage | Preferences, recents, brand profiles, transform paths, snapshots, sidecars | Partial | `docs/storage-model.md`; Store plugin usage; schema-versioned workspace persistence migration; frontend unit migration/schema test; snapshot/export manifest modules; snapshot restore source/store scope tests; `export_document_writes_optional_sidecar_manifest` proves sidecar manifests across HTML, PDF, DOCX, PPTX, Markdown bundle, and disabled-manifest behavior; the app-authored native workflow smoke now proves the launched Tauri webview can invoke the real export command, write an HTML artifact, and write the matching sidecar manifest under `.tmp/desktop-smoke/`; the same launched native smoke now saves a branded PDF export profile, reapplies it after option drift, reloads it from the Tauri settings store, and records `exportProfileEvidence`; it also creates, lists, and restores app-data and project-local snapshots while recording nested `snapshotEvidence` in `.tmp/desktop-smoke/native-workflow-report.json` | Broader sidecar workflow proof across supported platforms. |
| 27 Implementation phases | Phase deliverables | Partial | Most phase surfaces exist | Phase completion must be proven by matrix and tests. |
| 28 Acceptance criteria | Concrete app acceptance | Partial | Current build/test surfaces; `pnpm run verify:local -- --list` now includes the browser workflow environment preflight through `node scripts/check-e2e-environment.mjs`, `pnpm run verify:local:full -- --list` now includes the full browser workflow suite through `node scripts/run-e2e.mjs`, the focused runtime accessibility audit through `pnpm run check:a11y:runtime`, the manual accessibility review contract through `pnpm run check:a11y:manual`, bounded macOS GUI launch smoke through `NEDITOR_DESKTOP_SMOKE_LAUNCH=1 pnpm run test:desktop-smoke`, the Google Docs import evidence contract through `pnpm run check:google-docs-import`, the external platform evidence contract through `pnpm run check:platform-evidence`, the release signing evidence contract through `pnpm run check:release-signing`, and final release-readiness aggregation through `pnpm run check:release-readiness`, while `pnpm run check:e2e-env` passes the Playwright browser launch preflight with workspace-local `.tmp/ms-playwright` cache support plus per-attempt transient launch retry evidence and records whether it used project-local Chromium or a system Chrome-compatible fallback in `.tmp/e2e-environment/report.json`; `pnpm run test:e2e`/`node scripts/run-e2e.mjs` passes 72 Chromium browser workflow tests locally on 2026-05-27, including first-class outline mode CRUD and inactive-tab external refresh, and records `.tmp/e2e-browser/report.json` with schema `neditor.e2e-browser-workflow.v1`, `scope: "full-suite"`, parsed test totals, stdout/stderr tails, browser source, and workflow flags for Docs Live, outline planning, outline CRUD, and export workflows; focused browser audits now write separate reports through `NEDITOR_E2E_REPORT_PATH`, and release readiness validates the linked runtime accessibility and performance focused e2e reports before accepting those gates; `pnpm run test:desktop-smoke` checks desktop build artifacts and the native command workflow smoke; `NEDITOR_DESKTOP_SMOKE_LAUNCH=1 pnpm run test:desktop-smoke` proves bounded macOS native GUI startup and writes `.tmp/desktop-smoke/launch-report.json` with PID, elapsed window, captured output, and `processAlive: true` evidence plus `.tmp/desktop-smoke/native-window-report.json` with app-authored package, identifier, main-window title, visibility, size, and scale-factor evidence; the same launch smoke now writes `.tmp/desktop-smoke/native-ui-report.json` proving the native Vue workbench rendered command, sidebar, source, preview, status, document identity, preview label, status text, toolbar display, and viewport evidence, and `.tmp/desktop-smoke/native-workflow-report.json` proving native mode switching, command-palette opening, native View/Writing Tools menu command execution including Docs Live section-draft generation/application, workspace/tab grouping and restore evidence, calc-template insertion into source, rendered preview output, dirty-title mutation, HTML export readiness progress evidence, a real app-initiated HTML export write with sidecar manifest/output-hash proof, guarded native menu-event routing for direct HTML export, native export-profile settings persistence through a Tauri store reload, native outline-sidebar navigation to CodeMirror source, rendered outline-mode structure evidence, and native app-data plus project-local snapshot create/list/restore; macOS System Events process evidence is recorded as limited when host accessibility does not expose a window; `pnpm run test:tauri-webdriver` is wired into full verification and provides a Windows/Linux Tauri-driver harness for native title, main command rendering, mode switching, command-palette opening, dirty-title state, Templates-panel calc insertion to source/preview, real Markdown save/open/rename/duplicate/reveal, export readiness, real HTML export writing with sidecar manifest/output-hash validation, and preference persistence across a desktop restart, while `.tmp/desktop-webdriver/report.json` records the supported workflow plan on macOS unsupported skips and now attaches `fallbackProof` from `.tmp/desktop-smoke/native-command-report.json` only when the bounded native launch proof is fresh for the built binary, including `freshForBinary`, `binaryMtime`, `reportMtime`, `launchStatus`, and `processAlive` fields; release readiness independently re-stats the desktop binary, native smoke report, launch report, and WebDriver report to reject stale fallback proof after rebuilds, rejects release evidence kit reports whose embedded source commit differs from the current Git HEAD or whose current tree is dirty, and platform/provider/runtime/security/performance/Google Docs evidence validators now classify prior-commit external evidence as stale pending proof while still rejecting malformed or bad current-commit evidence; rendered export/package proof includes `representative_rendered_export_artifacts_are_package_inspectable`, and release readiness now validates rendered audit targets, review cases, per-case artifacts, manual checklist structure, structured browser workflow evidence, linked focused browser audit evidence, current native smoke/WebDriver fallback freshness, and current macOS app/DMG report structure instead of accepting file presence or exit status alone; `.tmp/release-readiness/report.json` records accepted current-host checks plus explicit external evidence gaps, now fed by `.tmp/google-docs-import/report.json` for Google Docs live import/readback, `.tmp/platform-evidence/report.json` for Windows/Linux proof contracts, and `.tmp/release-signing/report.json` for credentialed distribution proof | Execute the WebDriver harness on supported Windows/Linux hosts and broaden platform/rendered proof. |
| 29 Non-goals | Cloud collaboration and overreach controls | Complete | `docs/specification.md` lists first-release non-goals; `README.md` now states what NEditor is not; `docs/user-guide.md` documents product boundaries around local-first files, no cloud sync, no real-time multiplayer collaboration, no mobile/web app, no server-side rendering, no full WYSIWYG editor, no enterprise identity layer, and no arbitrary plugin marketplace; `pnpm run check:docs` verifies the linked docs | Revisit only if the first-release scope changes. |
| 30 Architecture decisions | Licensing, editor, parser, PDF/DOCX/PPTX/citations/formulas/transforms/snapshots/dependency gate | Partial | `LICENSE`; `package.json`; `src-tauri/Cargo.toml`; `src-tauri/tauri.conf.json`; implementation follows many decisions; dependency doc exists; `pnpm run check:deps` guards manifest dependency admission coverage, canonical MIT license text, and MIT package metadata | Keep dependency admission current, document deviations, and broaden rendered/workflow proof for the remaining architecture decisions. |
| 31 First milestone | Prove architecture with representative features | Complete | The recommended milestone scope in `docs/specification.md` is now covered by direct implementation and current verification: Tauri app shell (`package.json`, `src-tauri/Cargo.toml`, `src/App.vue`), editor and preview (`pnpm run test:e2e` source/preview workflows, native smoke UI evidence), file open/save (`opens, saves, duplicates, renames, reveals, and reverts mocked files`, native real-file save/open/revert smoke), front matter/includes/TOC (`generated_toc_exports_page_numbers_for_pdf_and_docx`, include graph/browser/native watcher workflows), transform registry with Mermaid/Graphviz/CSV (`transforms` modules, external-engine checks, table/data-source workflows), HTML/PDF exports and manifests (`representative_rendered_export_artifacts_are_package_inspectable`, rendered export audit, sidecar manifest tests), and external file refresh (`watch open files`, inactive-tab refresh, included-file native smoke); fresh baseline evidence is recorded by `pnpm run test:e2e` with 64 Chromium workflows, `pnpm run verify:local`, `pnpm run test:rendered-exports`, and `pnpm run check:release-readiness` | Keep milestone proof current as foundational architecture changes. |

## External Transform Setup Matrix

| Setup doc section | Requirement area | Current status | Evidence | Remaining gap |
| --- | --- | --- | --- | --- |
| Safety model | Real executable paths, per-engine trust, bounded execution, cache keys, fallback | Partial | `transforms/external.rs`; external transform tests; `prepare_for_export_validates_transform_engine_paths_before_export`; `compiler_skips_disabled_external_transform_without_trust_warning`; `external_transform_rejects_directory_engine_path_before_spawn`; `external_transform_cache_invalidates_when_trusted_executable_changes`; archived browser workflow run `26157446711` covers settings-level path change trust clearing, trust prompts, denied trust reset, input mode/timeout probe details, cache identity display, and missing-executable diagnostics | Cross-platform process proof and deeper executable edge cases. |
| macOS setup | Graphviz, D2, Pikchr, Java/PlantUML paths | Partial | `docs/external-transform-platform-evidence.md`; `NEDITOR_TEST_PIKCHR=/Users/nyimbiodero/src/pjs/tooling/neditor/.tmp/pikchr-build/pikchr-trunk/pikchr pnpm run collect:engine-evidence` reports Graphviz `dot`, `circo`, `neato`, `fdp`, `osage`, `twopi`, D2, PlantUML, Java, SQLite, and Pikchr on this Darwin arm64 host, writes passing adapter-shaped SVG/CSV smoke artifacts for every installed engine under `.tmp/external-engines/artifacts/`, emits external evidence templates under `.tmp/external-engines/templates/`, writes accepted `neditor.external-engine-evidence.v1` proof under `.tmp/external-engines/external/`, accepts valid copied proof from `NEDITOR_EXTERNAL_ENGINE_EVIDENCE_DIR`, and fails malformed supplied evidence; `NEDITOR_TEST_PIKCHR=/Users/nyimbiodero/src/pjs/tooling/neditor/.tmp/pikchr-build/pikchr-trunk/pikchr cargo test --locked external_transform_conformance_runs_installed_engines --lib -- --nocapture` verified Graphviz engines, D2, PlantUML, and Pikchr through the external transform execution path | Keep cross-platform optional-engine proof current, especially Windows/Linux evidence and packaged-host path proof. |
| Linux setup | Packages and optional engines | Partial | Historical workflow installed Linux engines; current proof requires local installed-engine checks | Keep installed-engine conformance stable. |
| Windows setup | Winget paths and shim guidance | Partial | `docs/external-transforms.md` documents Windows `winget` setup for Graphviz, D2, Java, PlantUML, and Rust/Pikchr CLI, explicit `.exe` path examples for Graphviz layout engines, D2, PlantUML, and Pikchr, package-manager shim guidance, and a PowerShell `NEDITOR_TEST_PIKCHR=... pnpm run check:engines` proof command; `ned handlers --platform windows --commands-only` exposes copyable winget/Cargo setup commands for support desks without starting installers; `pnpm run check:external-transform-docs` guards that Windows setup contract and is included in `pnpm run verify:local` | Add accepted Windows-host external-engine evidence for installed executables and package-manager shims. |
| Engine defaults | stdin/file modes by engine | Partial | Adapter profiles and tests; `compiler_uses_dot_settings_for_graphviz_alias` proves `graphviz` fences inherit the configured `dot` path, trust, and input mode; macOS installed-engine conformance verified Graphviz stdin, D2 stdin, PlantUML file-mode, SQLite CSV mode, and Pikchr stdin smoke artifacts in `docs/external-transform-platform-evidence.md`; Graphviz variant metadata now exposes separate default commands/version probes for `circo`, `neato`, `fdp`, `osage`, and `twopi`; external evidence templates and `collect:engine-evidence` records per-engine smoke kind, version arguments, SVG/CSV smoke hashes, and required markers for copied proof, and the release evidence kit now routes optional engine proof back through ingest plus `check:engines` | Cross-platform confirmation for Windows/Linux executable paths, package-manager shims, and Pikchr executable-shape variants. |
| Troubleshooting | Permission, empty output, timeout, trust disabled, cache stale | Partial | Diagnostics/failure hints; external transform tests now cover missing engine path, directory-as-engine rejection, permission, timeout, stderr, missing output, output limit, disabled-engine, and cache-stale diagnostics; Help topic `External transform troubleshooting` gives platform-specific macOS/Windows/Linux path and permission guidance plus timeout, empty-output, disabled-trust, stale-cache, PlantUML file-mode, Graphviz/D2 stdin, and Pikchr native-fallback guidance; browser workflow `offers searchable contextual help with workflow actions` proves the topic is searchable by stale-cache/Windows symptoms and routes to Engine settings | Remaining proof is copied platform evidence from Windows/Linux optional-engine hosts and manual engine troubleshooting sign-off. |

## Verification Coverage Summary

Current direct evidence:

- Backend Rust tests cover many compiler/export/transform/table/file/Git/snapshot
  behaviors under `src-tauri/src/tests/`.
- Backend Rust stress coverage now includes a large-document compiler stress
  case for nested includes, many tables, formulas, transform artifacts, source
  maps, and diagnostics.
- Frontend unit tests cover table parsing/serialization, conflict diff
  alignment, and conflict merge composition under `tests/frontend-unit.test.ts`.
- Local full verification covers frontend checks/tests, frontend production
  build, dependency/docs/accessibility guards, Rust formatting/checks/clippy,
  native-watch check, 213 Rust tests, optional engine probing, Tauri no-bundle
  release compile, macOS `.app` bundle build/smoke on this host, strengthened
  desktop smoke, and the desktop WebDriver harness step on the current host.
- `pnpm run check:docs` now discovers README plus all top-level docs Markdown
  files and checks local links, including the user guide, Markdown extensions
  reference, and `docs/specification.md` architecture figure target.
- `docs/ai-first-platform-roadmap.md` captures the 50-change AI-first roadmap;
  `pnpm run check:ai-roadmap` verifies the exact 50-item structure, README and
  matrix linkage, and implemented/tested AI surfaces for Docs Live, Agent
  Workspace, document intent sheets with persisted run-history audit snapshots,
  reusable document memory packs, outline variants, section contract cards,
  composable section draft history, Agent-Selected Transforms for source-grounded
  calc/chart/table/diagram/timeline/schema/equation/publishing blocks,
  Data-to-Narrative Bridge dependency links between structured values, claims,
  source signals, affected sections, and review actions, Approval Metadata Gate
  blockers for status, reviewer, owner, approvedAt, release target, source
  confidence, comments, and AI provenance, automation scheduler execution state
  with run-safe-queue controls, persisted run-history status breakdowns, and
  insertable/copyable audit evidence, pre-review rehearsal,
  playbooks, lifecycle tasks, provider handoff, runtime readiness, and guided
  demo.
- `pnpm run check:a11y` now checks static Vue template accessibility guardrails
  for accessible control names, dialog labeling, dialog focusability/keyboard
  handling, required skip-link targets, and status/progress live regions.
- `pnpm run check:a11y:runtime` now executes the focused browser accessibility
  workflows and records `.tmp/accessibility/runtime-report.json`.
- `pnpm run check:a11y:manual` now records the manual assistive-technology
  review template and validates completed sign-off files when supplied.

Current major verification gaps:

- Archived remote workflow evidence on commit `02e832a` was green across browser workflows and
  Ubuntu/macOS/Windows desktop builds. The earlier Windows path-sensitive
  Rust-test failures, Ubuntu installed Pikchr conformance failure, and Ubuntu
  fake-`d2` stdin fixture failure were resolved there. This is historical
  evidence only, not the current verification source.
- Browser-level workflow harness previously passed in Linux Actions run `26159396761` with 28
  Chromium tests, including advanced table editor coverage, mocked file
  lifecycle coverage, save-as, recently closed reopening, and stale-save
  conflict copy/merge/keep-local/accept-external recovery plus watcher-originated
  clean reload, dirty root-conflict coverage, and AI paste insert/quote/
  appendix/replace-document/section-merge/replace-selection coverage, clean
  included-file recompile, dirty included-file conflict handling, and
  restart-style workspace restore coverage for open tabs, active tab, pinned
  state, mode/sidebar, workspace root, recent files, scroll-position restore,
  missing-restored-file warning coverage, tab activation, dirty close
  confirmation, renamed recent cleanup, deleted recently-closed pruning, recent
  folder reopen/prune behavior, and externally moved recently-closed path
  pruning, synchronized editor/preview scrolling, preview heading
  click-to-source, persisted editor word-wrap and line-number settings,
  CodeMirror find/replace, smart list continuation, bracket auto-pairing, and
  command-palette heading, citation, glossary, index, open-document, and
  workspace-file navigation, plus transform engine settings trust/probe
  diagnostics, fold/unfold controls, pending preview compile cancellation/resume,
  generated TOC preview/source navigation, target-specific export readiness
  manifest preview, export output/manifest path reporting, export success/failure
  diagnostics, blocked export diagnostics before file write, and built-in/custom
  transform template filtering, preview, persistence, and command-palette insertion. The
  current full browser baseline passed all 64 Chromium tests locally through
  `pnpm run test:e2e`, including the
  additional Docs Live workflow and inactive-tab external refresh workflow, and
  `node scripts/run-e2e.mjs e2e/app-workflows.spec.ts --grep "Docs Live" --project chromium`
  passed on this host through the workspace-local Playwright Chromium cache.
- `pnpm run test:desktop-smoke` now checks the built Vite artifact, Tauri
  configuration, package/license metadata, release desktop binary after
  `./node_modules/.bin/tauri build --no-bundle`, and a native command workflow
  smoke that saves, opens, watches, compiles, checks readiness, exports, and
  reveals real local files; the bounded native GUI launch smoke also passes on
  the current macOS host and writes `.tmp/desktop-smoke/launch-report.json`
  with PID, elapsed window, captured output, and `processAlive: true` evidence,
  plus `.tmp/desktop-smoke/native-window-report.json` with native package,
  identifier, main-window title, visibility, size, and scale-factor evidence,
  and `.tmp/desktop-smoke/native-ui-report.json` with app-authored proof that
  the Vue workbench rendered commands, source, preview, sidebar, status,
  document identity, preview label, and viewport dimensions. On hosts that
  permit System Events automation, the same launch report records native
  process/window evidence; otherwise it classifies the System Events result as
  limited and keeps the app-authored reports authoritative.
- macOS packaging now has current executable evidence: `./node_modules/.bin/tauri
  build --bundles app` builds `NEditor.app`, and `pnpm run test:desktop-bundle`
  verifies Info.plist metadata, bundle identifier, version, executable, icon,
  copyright, and high-resolution flag while writing
  `.tmp/desktop-bundle/macos-app-report.json`. `pnpm run test:desktop-dmg`
  now classifies this host's sandboxed `hdiutil create`/`hdiejectd` failure and
  writes `.tmp/desktop-bundle/macos-dmg-report.json`, so DMG failure is tracked
  as host-specific instead of an unclassified packaging note.
- Current committed browser workflow tests now execute locally on this host
  through the workspace-local Playwright Chromium cache at `.tmp/ms-playwright`.
  A Tauri-driver WebDriver
  harness exists for supported Windows/Linux hosts; macOS records the official
  unsupported skip because the Tauri WebDriver stack has no WKWebView driver
  there. The supported-platform harness now reaches beyond shell smoke by
  checking dirty native titles, outline-mode structural editing, Templates-panel
  calc insertion to source and preview, real Markdown save/open/rename/duplicate/reveal,
  export readiness, real HTML export writing through the guarded dialog-free
  smoke path, and preference persistence across a desktop restart.
  `pnpm run test:tauri-webdriver` writes
  `.tmp/desktop-webdriver/report.json` with the supported workflow plan plus
  outline, export artifact, and pass/skip evidence.
- Export tests now include package/text assertions plus a representative
  rendered export audit that inspects HTML, PDF object structure, DOCX/PPTX
  package anatomy, custom/core properties, comments/provenance appendices,
  transform artifacts, Markdown bundle manifests, blog/Substack packages,
  LaTeX source, and Google Docs packages.
  `pnpm run test:rendered-exports` also writes reviewable artifacts,
  `manual-review.html`, `visual-review-summary.json`, `visual-review-signoff.template.json`, a manual checklist, and executable viewer/package proof under
  `.tmp/rendered-export-audit`; that proof now includes publishing/import
  workflow metadata checks, LaTeX source checks, nested Google Docs DOCX package
  checks, visual-summary mapping from targets/review cases to screenshot/raster/native-tool evidence with explicit pending or validated human sign-off, a structured `NEDITOR_RENDERED_EXPORT_SIGNOFF` plus `--validate-signoff-only` validation path for completed manual native-viewer review against an existing audit bundle that requires the `neditor.rendered-export.visual-signoff.v1` schema, reviewer/native-viewer metadata, ISO review timestamp, and artifact byte/SHA-256 matches against the current audit outputs, and macOS `textutil` extraction for the primary DOCX, nested Google
  Docs DOCX, and rendered review-case DOCX files. On macOS it also attempts
  Quick Look thumbnails for primary PDF/DOCX/PPTX artifacts and records either
  thumbnail evidence or host limitations. When Chromium can launch, it captures
  body-scoped browser-rendered screenshot proof for the primary HTML export,
  `manual-review.html`, and the rich-block/option-heavy HTML review cases.
  Broader cross-platform native viewer/platform manual QA remains under-proven.
- Optional external transform engine evidence now includes current macOS
  Graphviz/DOT variants, D2, PlantUML, Pikchr, and SQLite version plus SVG or
  CSV smoke artifact proof via `pnpm run check:engines`,
  `.tmp/external-engines/probe-report.json`, and
  `.tmp/external-engines/artifacts/`; the release evidence kit now packages a
  Pikchr and SQLite template plus an optional external-engine runbook, and
  `pnpm run ingest:evidence` validates returned optional-engine proof through
  `pnpm run check:engines`. Windows optional-engine evidence remains open when
  a Windows host lacks accepted returned proof.
- Accessibility has a structured static guard report at
  `.tmp/accessibility/report.json`, a focused runtime workflow report at
  `.tmp/accessibility/runtime-report.json`, a manual review template and
  sign-off summary at `.tmp/accessibility/manual-review-template.json` and
  `.tmp/accessibility/manual-review-summary.json`, and current local browser
  workflow execution, but a completed real screen-reader/native
  assistive-technology review is still missing. Performance now has
  compiler, repeated export loop, repeated edit/cache, export progress
  reporting, compile-result cancellation coverage, preview debounce
  timing/coalescing coverage, current local browser large-document workflow
  execution, a structured performance audit report at
  `.tmp/performance-audit/report.json`, a bounded repeated compile/export
  memory-growth stress test, and native command workflow timing evidence in
  `.tmp/desktop-smoke/native-command-report.json`. `pnpm run
  check:performance-profile` now turns sustained release-device native profiling
  into validator-backed evidence, release-readiness gaps, evidence-kit
  templates/runbooks, and ingest support; completed real-device profile evidence
  remains pending.
- Release-readiness aggregation now records the current state in
  `.tmp/release-readiness/report.json`: all required current-host reports are
  accepted, while Windows/Linux package artifacts, Windows/Linux WebDriver
  execution, signing/notarization, AI provider live endpoint proof, Google Docs
  live import/readback, independent security review sign-off, release-device
  native performance profiling, optional engines without accepted host proof,
  and human reviewer sign-offs remain explicit external evidence gaps.
- The spec-completion matrix contract now writes
  `.tmp/spec-completion/report.json` and
  `.tmp/spec-completion/gap-plan.md`, validates every matrix row for a
  recognized status, evidence, and substantive remaining gap when still Partial,
  Unverified, or Missing, classifies open rows into local implementation/proof,
  documentation-proof, manual review, external evidence, cross-platform
  evidence, release credentials, and distribution-artifact buckets, and feeds
  open matrix rows into release readiness as explicit production risks plus a
  release evidence kit closure runbook instead of letting the matrix drift as
  prose.
- The AI provider evidence contract now writes
  `.tmp/ai-provider-evidence/report.json`, validates live endpoint evidence for
  current source, response hashes, required markers, and secret-free reports,
  and emits a provider evidence template. This does not replace credentialed
  provider execution, but it turns approved-provider proof into validated
  readiness evidence and prevents malformed or secret-bearing provider claims
  from satisfying readiness.
- The AI runtime evidence contract now writes
  `.tmp/ai-runtime-evidence/report.json`, validates real-device microphone and
  clipboard capability evidence without accepting recorded audio or clipboard
  text, and emits a runtime evidence template. This does not replace a real
  microphone/clipboard session, but it turns that proof into validated release
  evidence and prevents malformed runtime claims from satisfying readiness.
- The independent security review evidence contract now writes
  `.tmp/security-review/report.json` and a security review template, accepts
  returned independent-review evidence from `NEDITOR_SECURITY_REVIEW_EVIDENCE`,
  `NEDITOR_SECURITY_REVIEW_EVIDENCE_DIR`, or the release evidence ingest path,
  validates reviewer independence, trust-boundary scope, reviewed artifacts,
  unresolved findings, report hashes, and release sign-off, and prevents
  malformed security claims from satisfying readiness.
- The Google Docs import evidence contract now writes
  `.tmp/google-docs-import/report.json`, validates the local Google Docs handoff
  DOCX/package hashes from rendered export audit, and emits a live Drive
  import/readback template. This does not replace a refreshed Google Drive
  connector session, but it turns live import/readback proof into validated
  readiness evidence and prevents malformed import claims from satisfying
  readiness.
- The optional external engine evidence contract now writes diagram SVG smoke
  artifacts, SQLite CSV smoke artifacts, and engine-specific templates under
  `.tmp/external-engines/`, accepts returned
  proof from `NEDITOR_EXTERNAL_ENGINE_EVIDENCE_DIR` or the release evidence
  ingest path, and prevents malformed engine claims from satisfying readiness.
- The release-device performance profile contract now writes
  `.tmp/performance-profile/report.json` and a native-profile template, accepts
  returned 30+ minute Tauri release profiling evidence from
  `NEDITOR_PERFORMANCE_PROFILE_EVIDENCE`, `NEDITOR_PERFORMANCE_PROFILE_EVIDENCE_DIR`,
  or the release evidence ingest path, and prevents malformed performance
  claims from satisfying readiness.
- The external platform evidence contract now writes
  `.tmp/platform-evidence/report.json` and templates for Windows/Linux package
  artifact plus Tauri WebDriver proof. This does not replace supported-host
  execution, but it turns copied Windows/Linux reports into validated release
  evidence and prevents malformed reports from satisfying readiness.
- The release signing evidence contract now writes
  `.tmp/release-signing/report.json` and templates for macOS codesign/
  notarization, Windows Authenticode/timestamp, and Linux package signing/
  checksum proof. This does not replace credentialed signing, but it turns
  copied release-host verification into validated readiness evidence and
  prevents malformed signing claims from satisfying readiness.

## Next Matrix Work

1. Expand each "Partial" row with item-level checklist entries as the
   corresponding feature area is actively worked.
2. Link each row to exact test names once the fresh verification baseline is
   run.
3. Move rows to "Complete" only after direct evidence exists and the evidence is
   current.
4. Keep this matrix synchronized with `docs/todo.md` and `docs/progress.md`.
